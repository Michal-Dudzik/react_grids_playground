import "./chunk-5WRI5ZAA.js";

// node_modules/@toolbox-web/grid/index.js
var e = { STRETCH: "stretch", FIXED: "fixed" };
var t = { sortApplied: (e2, t2) => `Sorted by ${e2}, ${t2}`, sortCleared: () => "Sort cleared", filterApplied: (e2) => `Filter applied on ${e2}`, filterCleared: (e2) => `Filter cleared from ${e2}`, allFiltersCleared: () => "All filters cleared", groupExpanded: (e2, t2) => `Group ${e2} expanded, ${t2} rows`, groupCollapsed: (e2) => `Group ${e2} collapsed`, selectionChanged: (e2) => `${e2} rows selected`, editingStarted: (e2) => `Editing row ${e2 + 1}`, editingCommitted: (e2) => `Row ${e2 + 1} saved`, dataLoaded: (e2) => `${e2} rows loaded` };
var o = { mode: "reduced-motion", duration: 200, easing: "ease-out" };
var i = '<svg viewBox="0 0 16 16" width="12" height="12"><path fill="currentColor" d="M6 10.5a.5.5 0 0 1 .5-.5h3a.5.5 0 0 1 0 1h-3a.5.5 0 0 1-.5-.5zm-2-3a.5.5 0 0 1 .5-.5h7a.5.5 0 0 1 0 1h-7a.5.5 0 0 1-.5-.5zm-2-3a.5.5 0 0 1 .5-.5h11a.5.5 0 0 1 0 1h-11a.5.5 0 0 1-.5-.5z"/></svg>';
var n = { expand: "▶", collapse: "▼", sortAsc: "▲", sortDesc: "▼", sortNone: "⇅", submenuArrow: "▶", dragHandle: "⋮⋮", toolPanel: "☰", filter: i, filterActive: i, print: "🖨️" };
function r(e2, t2) {
  if (!e2) return;
  const o2 = "effectiveConfig" in e2 ? e2.effectiveConfig : void 0;
  if (false === o2?.a11y?.announcements) return;
  const i2 = e2.querySelector?.(".tbw-sr-only");
  i2 && (i2.textContent = "", requestAnimationFrame(() => {
    i2.textContent = t2;
  }));
}
function s(e2, o2, ...i2) {
  const n2 = e2 && "effectiveConfig" in e2 ? e2.effectiveConfig : void 0, r2 = n2?.a11y?.messages?.[o2];
  return r2 ? r2(...i2) : t[o2](...i2);
}
function l(e2, t2) {
  return `[tbw-grid${e2 ? `#${e2}` : ""}${t2 ? `:${t2}` : ""}]`;
}
var a = "TBW041";
var c = "TBW061";
var d = "TBW062";
var h = "TBW080";
var u = "TBW090";
var g = "TBW091";
var f = "TBW092";
var p = "TBW093";
var w = "TBW100";
var m = "TBW110";
var b = "TBW111";
var v = "TBW112";
var C = "TBW113";
var _ = "TBW114";
var y = "TBW140";
var R = "TBW141";
var S = "TBW142";
var E = "TBW143";
function x(e2, t2, o2, i2) {
  return `${l(o2, i2)} ${e2}: ${t2}

  → More info: ${(function(e3) {
    return `https://toolboxjs.com/grid/errors#${e3.toLowerCase()}`;
  })(e2)}`;
}
function T(e2, t2, o2, i2) {
  throw new Error(x(e2, t2, o2, i2));
}
function A(e2, t2, o2, i2) {
  console.warn(x(e2, t2, o2, i2));
}
function P(e2, t2, o2, i2) {
  console.debug(x(e2, t2, o2, i2));
}
function H(e2, t2, o2, i2) {
  console.error(x(e2, t2, o2, i2));
}
function M(e2, t2) {
  if (!(e2 && e2.length || t2 && t2.length)) return [];
  if (!e2 || !e2.length) return t2 || [];
  if (!t2 || !t2.length) return e2;
  const o2 = {}, i2 = t2;
  for (let s2 = 0; s2 < i2.length; s2++) {
    const e3 = i2[s2], t3 = o2[e3.field];
    if (t3) {
      e3.header && !t3.header && (t3.header = e3.header), e3.type && !t3.type && (t3.type = e3.type), e3.sortable && (t3.sortable = true), e3.editable && (t3.editable = true), e3.resizable && (t3.resizable = true), null != e3.width && null == t3.width && (t3.width = e3.width), null != e3.minWidth && null == t3.minWidth && (t3.minWidth = e3.minWidth), e3.__viewTemplate && (t3.__viewTemplate = e3.__viewTemplate), e3.__editorTemplate && (t3.__editorTemplate = e3.__editorTemplate), e3.__headerTemplate && (t3.__headerTemplate = e3.__headerTemplate);
      const o3 = e3.renderer || e3.viewRenderer, i3 = t3.renderer || t3.viewRenderer;
      o3 && !i3 && (t3.viewRenderer = o3, e3.renderer && (t3.renderer = o3)), e3.editor && !t3.editor && (t3.editor = e3.editor);
    } else o2[e3.field] = { ...e3 };
  }
  const n2 = e2.map((e3) => {
    const t3 = o2[e3.field];
    if (!t3) return e3;
    const i3 = { ...e3 };
    t3.header && !i3.header && (i3.header = t3.header), t3.type && !i3.type && (i3.type = t3.type), i3.sortable = e3.sortable || t3.sortable, true !== e3.resizable && true !== t3.resizable || (i3.resizable = true), i3.editable = e3.editable || t3.editable, null != t3.width && null == i3.width && (i3.width = t3.width), null != t3.minWidth && null == i3.minWidth && (i3.minWidth = t3.minWidth), t3.__viewTemplate && (i3.__viewTemplate = t3.__viewTemplate), t3.__editorTemplate && (i3.__editorTemplate = t3.__editorTemplate), t3.__headerTemplate && (i3.__headerTemplate = t3.__headerTemplate);
    const n3 = t3.renderer || t3.viewRenderer, r3 = i3.renderer || i3.viewRenderer;
    return n3 && !r3 && (i3.viewRenderer = n3, t3.renderer && (i3.renderer = n3)), t3.editor && !i3.editor && (i3.editor = t3.editor), delete o2[e3.field], i3;
  }), r2 = Object.keys(o2);
  for (let s2 = 0; s2 < r2.length; s2++) n2.push(o2[r2[s2]]);
  return n2;
}
function k(e2, t2) {
  try {
    e2.part?.add?.(t2);
  } catch {
  }
  const o2 = e2.getAttribute("part");
  o2 ? o2.split(/\s+/).includes(t2) || e2.setAttribute("part", o2 + " " + t2) : e2.setAttribute("part", t2);
}
function L(t2) {
  const o2 = t2.effectiveConfig?.fitMode || t2.fitMode || e.STRETCH;
  if (o2 !== e.STRETCH && o2 !== e.FIXED) return;
  if (t2.__didInitialAutoSize) return;
  if (!t2.isConnected) return;
  const i2 = Array.from(t2._headerRowEl?.children || []);
  if (!i2.length) return;
  let n2 = false;
  const r2 = t2._visibleColumns;
  for (let e2 = 0; e2 < r2.length; e2++) {
    const o3 = r2[e2];
    if (o3.width) continue;
    const s2 = i2[e2];
    let l2 = s2 ? s2.scrollWidth : 0;
    for (let i3 = 0; i3 < t2._rowPool.length; i3++) {
      const o4 = t2._rowPool[i3].children[e2];
      if (o4) {
        const e3 = o4.scrollWidth;
        e3 > l2 && (l2 = e3);
      }
    }
    l2 > 0 && (o3.width = l2 + 2, o3.__autoSized = true, n2 = true);
  }
  n2 && I(t2), t2.__didInitialAutoSize = true;
}
var O = /^(?:\d+(?:\.\d+)?(?:px|%|fr|em|rem|ch|vw|vh|vmin|vmax)|calc\(.+\)|min-content|max-content|minmax\(.+\)|fit-content\(.+\)|auto)$/i;
function D(e2, t2) {
  return "number" == typeof e2 ? `${e2}px` : (O.test(e2) || A("TBW050", `Column '${t2 ?? "?"}' has an invalid CSS width value: '${e2}'. Expected a number (px) or a valid CSS unit string (e.g. '30%', '2fr', 'calc(...)').`), e2);
}
function I(t2) {
  const o2 = t2.effectiveConfig?.fitMode || t2.fitMode || e.STRETCH;
  t2._gridTemplate = o2 === e.STRETCH ? t2._visibleColumns.map((e2) => {
    if (null != e2.width) return D(e2.width, e2.field);
    const t3 = e2.minWidth;
    return null != t3 ? `minmax(${t3}px, 1fr)` : "1fr";
  }).join(" ").trim() : t2._visibleColumns.map((e2) => null != e2.width ? D(e2.width, e2.field) : "max-content").join(" "), t2.style.setProperty("--tbw-column-template", t2._gridTemplate);
}
var z = { ROOT: "tbw-grid-root", HEADER: "header", HEADER_ROW: "header-row", HEADER_CELL: "header-cell", ROWS_VIEWPORT: "rows-viewport", ROWS_SPACER: "rows-spacer", ROWS_CONTAINER: "rows", DATA_ROW: "data-row", GROUP_ROW: "group-row", DATA_CELL: "data-cell", SELECTED: "selected", FOCUSED: "focused", EDITING: "editing", EXPANDED: "expanded", COLLAPSED: "collapsed", DRAGGING: "dragging", RESIZING: "resizing", SORTABLE: "sortable", SORTED_ASC: "sorted-asc", SORTED_DESC: "sorted-desc", HIDDEN: "hidden", STICKY_LEFT: "sticky-left", STICKY_RIGHT: "sticky-right", PINNED_TOP: "pinned-top", PINNED_BOTTOM: "pinned-bottom", TREE_TOGGLE: "tree-toggle", TREE_INDENT: "tree-indent", GROUP_TOGGLE: "group-toggle", GROUP_LABEL: "group-label", GROUP_COUNT: "group-count", RANGE_SELECTION: "range-selection", SELECTION_OVERLAY: "selection-overlay" };
var N = { FIELD: "data-field", GROUP_KEY: "data-group-key" };
var q = { ROOT: `.${z.ROOT}`, HEADER: `.${z.HEADER}`, HEADER_ROW: `.${z.HEADER_ROW}`, HEADER_CELL: `.${z.HEADER_CELL}`, ROWS_VIEWPORT: `.${z.ROWS_VIEWPORT}`, ROWS_CONTAINER: `.${z.ROWS_CONTAINER}`, DATA_ROW: `.${z.DATA_ROW}`, DATA_CELL: `.${z.DATA_CELL}`, GROUP_ROW: `.${z.GROUP_ROW}`, CELL_BY_FIELD: (e2) => `.${z.DATA_CELL}[${N.FIELD}="${e2}"]`, SELECTED_ROWS: `.${z.DATA_ROW}.${z.SELECTED}`, EDITING_CELL: `.${z.DATA_CELL}.${z.EDITING}` };
var $ = { COLOR_BG: "--tbw-color-bg", COLOR_FG: "--tbw-color-fg", COLOR_FG_MUTED: "--tbw-color-fg-muted", COLOR_BORDER: "--tbw-color-border", COLOR_ACCENT: "--tbw-color-accent", COLOR_HEADER_BG: "--tbw-color-header-bg", COLOR_HEADER_FG: "--tbw-color-header-fg", COLOR_SELECTION: "--tbw-color-selection", COLOR_ROW_HOVER: "--tbw-color-row-hover", COLOR_ROW_ALT: "--tbw-color-row-alt", ROW_HEIGHT: "--tbw-row-height", HEADER_HEIGHT: "--tbw-header-height", CELL_PADDING: "--tbw-cell-padding", FONT_FAMILY: "--tbw-font-family", FONT_SIZE: "--tbw-font-size", BORDER_RADIUS: "--tbw-border-radius", FOCUS_OUTLINE: "--tbw-focus-outline" };
var W = /{{\s*([^}]+)\s*}}/g;
var F = "__DG_EMPTY__";
var B = /^[\w$. '?+\-*/%:()!<>=,&|]+$/;
var V = "__otorp__|__retteGenifed__|__retteSenifed__|rotcurtsnoc|wodniw|sihTlabolg|labolg|ssecorp|noitcnuF|tropmi|lave|tcelfeR|yxorP|rorrE|stnemugra|tnemucod|noitacol|eikooc|egarotSlacol|egarotSnoisses|BDdexedni|hctef|tseuqeRpttHLMX|tekcoSbeW|rekroW|rekroWderahS|rekroWecivreS|renepo|tnerap|pot|semarf|fles".split("|").map((e2) => e2.split("").reverse().join(""));
var U = new RegExp(`__(proto|defineGetter|defineSetter)|${V.slice(3).join("|")}|this\\b`);
var G = new Set("script|iframe|object|embed|form|input|button|textarea|select|link|meta|base|style|template|slot|portal|frame|frameset|applet|noscript|noembed|plaintext|xmp|listing".split("|"));
var j = /^on\w+$/i;
var Y = new Set("href|src|action|formaction|data|srcdoc|xlink:href|poster|srcset".split("|"));
var X = /^\s*(javascript|vbscript|data|blob):/i;
function K(e2) {
  if (!e2 || "string" != typeof e2) return "";
  if (-1 === e2.indexOf("<")) return e2;
  const t2 = document.createElement("template");
  return t2.innerHTML = e2, (function(e3) {
    const t3 = [], o2 = e3.querySelectorAll("*");
    for (const i2 of o2) {
      const e4 = i2.tagName.toLowerCase();
      if (G.has(e4)) {
        t3.push(i2);
        continue;
      }
      if ("svg" === e4 || "http://www.w3.org/2000/svg" === i2.namespaceURI) {
        if (Array.from(i2.attributes).some((e5) => j.test(e5.name) || "href" === e5.name || "xlink:href" === e5.name)) {
          t3.push(i2);
          continue;
        }
      }
      const o3 = [];
      for (const t4 of i2.attributes) {
        const e5 = t4.name.toLowerCase();
        j.test(e5) ? o3.push(t4.name) : (Y.has(e5) && X.test(t4.value) || "style" === e5 && /expression\s*\(|javascript:|behavior\s*:/i.test(t4.value)) && o3.push(t4.name);
      }
      o3.forEach((e5) => i2.removeAttribute(e5));
    }
    t3.forEach((e4) => e4.remove());
  })(t2.content), t2.innerHTML;
}
function Z(e2, t2) {
  const o2 = (function(e3) {
    const t3 = [];
    let o3 = 0;
    for (; o3 < e3.length; ) {
      const i3 = e3[o3];
      if (" " !== i3) {
        if (i3 >= "0" && i3 <= "9" || "." === i3 && o3 + 1 < e3.length && e3[o3 + 1] >= "0" && e3[o3 + 1] <= "9") {
          const i4 = o3;
          for (; o3 < e3.length && (e3[o3] >= "0" && e3[o3] <= "9" || "." === e3[o3]); ) o3++;
          t3.push({ type: "num", v: Number(e3.slice(i4, o3)) });
          continue;
        }
        if ("'" === i3) {
          let i4 = "";
          for (o3++; o3 < e3.length && "'" !== e3[o3]; ) i4 += e3[o3++];
          if (o3 >= e3.length) return null;
          o3++, t3.push({ type: "str", v: i4 });
          continue;
        }
        if (i3 >= "a" && i3 <= "z" || i3 >= "A" && i3 <= "Z" || "_" === i3 || "$" === i3) {
          const i4 = o3;
          for (; o3 < e3.length && /[\w$.]/.test(e3[o3]); ) o3++;
          t3.push({ type: "id", v: e3.slice(i4, o3) });
          continue;
        }
        if (o3 + 2 < e3.length) {
          const i4 = e3.slice(o3, o3 + 3);
          if ("===" === i4 || "!==" === i4) {
            t3.push({ type: "op", v: i4 }), o3 += 3;
            continue;
          }
        }
        if (o3 + 1 < e3.length) {
          const i4 = e3.slice(o3, o3 + 2);
          if ("==" === i4 || "!=" === i4 || "<=" === i4 || ">=" === i4 || "&&" === i4 || "||" === i4) {
            t3.push({ type: "op", v: i4 }), o3 += 2;
            continue;
          }
        }
        if (!"+-*/%<>!?:(),".includes(i3)) return null;
        t3.push({ type: "op", v: i3 }), o3++;
      } else o3++;
    }
    return t3;
  })(e2);
  if (!o2) return;
  const i2 = o2;
  let n2 = 0;
  function r2() {
    return i2[n2];
  }
  function s2() {
    return i2[n2++];
  }
  function l2(e3, t3) {
    const o3 = r2();
    return !!o3 && (o3.type === e3 && (void 0 === t3 || o3.v === t3));
  }
  function a2() {
    const e3 = (function() {
      let e4 = c2();
      for (; l2("op", "||"); ) s2(), e4 = e4 || c2();
      return e4;
    })();
    if (l2("op", "?")) {
      s2();
      const t3 = a2();
      if (!l2("op", ":")) return;
      s2();
      const o3 = a2();
      return e3 ? t3 : o3;
    }
    return e3;
  }
  function c2() {
    let e3 = d2();
    for (; l2("op", "&&"); ) s2(), e3 = e3 && d2();
    return e3;
  }
  function d2() {
    let e3 = h2();
    for (; ; ) {
      const t3 = r2();
      if (!t3 || "op" !== t3.type) break;
      if ("===" === t3.v) s2(), e3 = e3 === h2();
      else if ("!==" === t3.v) s2(), e3 = e3 !== h2();
      else if ("==" === t3.v) s2(), e3 = e3 == h2();
      else {
        if ("!=" !== t3.v) break;
        s2(), e3 = e3 != h2();
      }
    }
    return e3;
  }
  function h2() {
    let e3 = u2();
    for (; ; ) {
      const t3 = r2();
      if (!t3 || "op" !== t3.type) break;
      if ("<" === t3.v) s2(), e3 = e3 < u2();
      else if (">" === t3.v) s2(), e3 = e3 > u2();
      else if ("<=" === t3.v) s2(), e3 = e3 <= u2();
      else {
        if (">=" !== t3.v) break;
        s2(), e3 = e3 >= u2();
      }
    }
    return e3;
  }
  function u2() {
    let e3 = g2();
    for (; ; ) {
      const t3 = r2();
      if (!t3 || "op" !== t3.type) break;
      if ("+" === t3.v) s2(), e3 += g2();
      else {
        if ("-" !== t3.v) break;
        s2(), e3 -= g2();
      }
    }
    return e3;
  }
  function g2() {
    let e3 = f2();
    for (; ; ) {
      const t3 = r2();
      if (!t3 || "op" !== t3.type) break;
      if ("*" === t3.v) s2(), e3 *= f2();
      else if ("/" === t3.v) s2(), e3 /= f2();
      else {
        if ("%" !== t3.v) break;
        s2(), e3 %= f2();
      }
    }
    return e3;
  }
  function f2() {
    return l2("op", "!") ? (s2(), !f2()) : p2();
  }
  function p2() {
    const e3 = r2();
    if (e3) {
      if ("num" === e3.type) return s2(), e3.v;
      if ("str" === e3.type) return s2(), e3.v;
      if ("id" === e3.type) return s2(), (function(e4) {
        if ("value" === e4) return t2.value;
        if (e4.startsWith("row.")) {
          const o3 = e4.slice(4);
          return t2.row ? t2.row[o3] : void 0;
        }
        return "row" === e4 ? t2.row : void 0;
      })(e3.v);
      if ("op" === e3.type && "(" === e3.v) {
        s2();
        const e4 = a2();
        return l2("op", ")") && s2(), e4;
      }
      return "op" === e3.type && "-" === e3.v ? (s2(), -p2()) : void 0;
    }
  }
  const w2 = a2();
  return n2 < i2.length ? void 0 : w2;
}
function Q(e2, t2) {
  if (!e2 || -1 === e2.indexOf("{{")) return e2;
  const o2 = [], i2 = e2.replace(W, (e3, i3) => {
    const n3 = (function(e4, t3) {
      if (e4 = (e4 || "").trim(), !e4) return F;
      if (J.test(e4)) return F;
      if ("value" === e4) return null == t3.value ? F : String(t3.value);
      if (e4.startsWith("row.") && !/[()?]/.test(e4) && !e4.includes(":")) {
        const o4 = e4.slice(4), i4 = t3.row ? t3.row[o4] : void 0;
        return null == i4 ? F : String(i4);
      }
      if (e4.length > 80) return F;
      if (!B.test(e4) || U.test(e4)) return F;
      const o3 = e4.match(/\./g);
      if (o3 && o3.length > 1) return F;
      try {
        const o4 = Z(e4, t3), i4 = null == o4 ? "" : String(o4);
        return J.test(i4) ? F : i4 || F;
      } catch {
        return F;
      }
    })(i3, t2);
    return o2.push({ expr: i3.trim(), result: n3 }), n3;
  }), n2 = (r2 = i2) ? r2.replace(new RegExp(F, "g"), "").replace(/Reflect\.[^<>{}\s]+|\bProxy\b|ownKeys\([^)]*\)/g, "") : r2;
  var r2;
  const s2 = o2.length && o2.every((e3) => "" === e3.result || e3.result === F);
  return J.test(e2) || s2 ? "" : n2;
}
var J = /Reflect|Proxy|ownKeys/;
function ee(e2) {
  if (J.test(e2.textContent || "")) {
    for (const t2 of e2.childNodes) t2.nodeType === Node.TEXT_NODE && J.test(t2.textContent || "") && (t2.textContent = "");
    J.test(e2.textContent || "") && (e2.textContent = "");
  }
}
function te(e2) {
  if (J.test(e2)) {
    const e3 = () => "";
    return e3.__blocked = true, e3;
  }
  const t2 = (t3) => Q(e2, t3);
  return t2.__blocked = false, t2;
}
function oe(e2) {
  return e2.replace(/([A-Z])/g, "-$1").toLowerCase();
}
var ie = class {
  static dependencies;
  static manifest;
  aliases;
  version = "2.1.1";
  styles;
  cellRenderers;
  headerRenderers;
  cellEditors;
  grid;
  config;
  userConfig;
  #e;
  get defaultConfig() {
    return {};
  }
  constructor(e2 = {}) {
    this.userConfig = e2;
  }
  attach(e2) {
    this.#e?.abort(), this.#e = new AbortController(), this.grid = e2, this.config = { ...this.defaultConfig, ...this.userConfig };
  }
  detach() {
    this.#e?.abort(), this.#e = void 0;
  }
  getPlugin(e2) {
    return this.grid?.getPlugin(e2);
  }
  emit(e2, t2) {
    this.grid?.dispatchEvent?.(new CustomEvent(e2, { detail: t2, bubbles: true }));
  }
  emitCancelable(e2, t2) {
    const o2 = new CustomEvent(e2, { detail: t2, bubbles: true, cancelable: true });
    return this.grid?.dispatchEvent?.(o2), o2.defaultPrevented;
  }
  on(e2, t2) {
    this.grid?._pluginManager?.subscribe(this, e2, t2);
  }
  off(e2) {
    this.grid?._pluginManager?.unsubscribe(this, e2);
  }
  emitPluginEvent(e2, t2) {
    this.grid?._pluginManager?.emitPluginEvent(e2, t2);
  }
  broadcast(e2, t2) {
    this.emitPluginEvent(e2, t2), this.emit(e2, t2);
  }
  requestRender() {
    this.grid?.requestRender?.();
  }
  requestColumnsRender() {
    this.grid?.requestColumnsRender?.();
  }
  requestRenderWithFocus() {
    this.grid?.requestRenderWithFocus?.();
  }
  requestAfterRender() {
    this.grid?.requestAfterRender?.();
  }
  requestVirtualRefresh() {
    this.grid?.requestVirtualRefresh?.();
  }
  get rows() {
    return this.grid?.rows ?? [];
  }
  get sourceRows() {
    return this.grid?.sourceRows ?? [];
  }
  get columns() {
    return this.grid?.columns ?? [];
  }
  get visibleColumns() {
    return this.grid?._visibleColumns ?? [];
  }
  get gridElement() {
    return this.grid?._hostElement;
  }
  get disconnectSignal() {
    return this.#e?.signal ?? this.grid?.disconnectSignal;
  }
  get gridIcons() {
    const e2 = this.grid?.gridConfig?.icons ?? {};
    return { ...n, ...e2 };
  }
  get isAnimationEnabled() {
    const e2 = this.grid?.effectiveConfig?.animation?.mode ?? "reduced-motion";
    if (false === e2 || "off" === e2) return false;
    if (true === e2 || "on" === e2) return true;
    const t2 = this.gridElement;
    if (t2) {
      return "0" !== getComputedStyle(t2).getPropertyValue("--tbw-animation-enabled").trim();
    }
    return true;
  }
  get animationDuration() {
    const e2 = this.gridElement;
    if (e2) {
      const t2 = getComputedStyle(e2).getPropertyValue("--tbw-animation-duration").trim(), o2 = parseInt(t2, 10);
      if (!isNaN(o2)) return o2;
    }
    return 200;
  }
  setIcon(e2, t2, o2) {
    e2.dataset.icon = oe(t2), "collapse" === t2 ? e2.dataset.expanded = "" : "expand" === t2 && delete e2.dataset.expanded;
    const i2 = this.#t(t2, o2);
    void 0 !== i2 ? "string" == typeof i2 ? e2.innerHTML = K(i2) : i2 instanceof HTMLElement && (e2.innerHTML = "", e2.appendChild(i2.cloneNode(true))) : e2.innerHTML = "";
  }
  #t(e2, t2) {
    return void 0 !== t2 ? t2 : this.grid?.gridConfig?.icons?.[e2];
  }
  updateSortIndicator(e2, t2) {
    e2.querySelector('[part~="sort-indicator"], .sort-indicator')?.remove();
    const o2 = document.createElement("span");
    o2.setAttribute("part", "sort-indicator"), o2.className = "sort-indicator", t2 ? (e2.setAttribute("aria-sort", "asc" === t2 ? "ascending" : "descending"), e2.setAttribute("data-sort", t2), this.setIcon(o2, "asc" === t2 ? "sortAsc" : "sortDesc")) : (e2.setAttribute("aria-sort", "none"), e2.removeAttribute("data-sort"), this.setIcon(o2, "sortNone"));
    const i2 = e2.querySelector(".tbw-filter-btn") ?? e2.querySelector(".resize-handle");
    return i2 ? e2.insertBefore(o2, i2) : e2.appendChild(o2), o2;
  }
  warn(e2, t2) {
    void 0 !== t2 ? console.warn(x(e2, t2, this.gridElement.id, this.name)) : console.warn(`${l(this.gridElement.id, this.name)} ${e2}`);
  }
  throwDiagnostic(e2, t2) {
    throw new Error(x(e2, t2, this.gridElement.id, this.name));
  }
};
function ne(e2, t2) {
  return null == e2 && null == t2 ? 0 : null == e2 ? -1 : null == t2 || e2 > t2 ? 1 : e2 < t2 ? -1 : 0;
}
function re(e2, t2, o2) {
  const i2 = o2.find((e3) => e3.field === t2.field), n2 = i2?.sortComparator, { field: r2, direction: s2 } = t2, l2 = [...e2];
  return n2 ? l2.sort((e3, t3) => n2(e3[r2], t3[r2], e3, t3) * s2) : se(l2, r2, s2), l2;
}
function se(e2, t2, o2) {
  e2.sort((e3, i2) => {
    const n2 = e3[t2], r2 = i2[t2];
    return null == n2 && null == r2 ? 0 : null == n2 ? -o2 : null == r2 || n2 > r2 ? o2 : n2 < r2 ? -o2 : 0;
  });
}
function le(e2, t2, o2, i2) {
  const n2 = i2.find((e3) => e3.field === t2), r2 = n2?.sortComparator;
  r2 ? e2.sort((e3, i3) => r2(e3[t2], i3[t2], e3, i3) * o2) : se(e2, t2, o2);
}
function ae(e2, t2, o2, i2) {
  e2._rows = t2, e2.__rowRenderEpoch++;
  for (let n2 = 0; n2 < e2._rowPool.length; n2++) e2._rowPool[n2].__epoch = -1;
  be(e2), e2.refreshVirtualWindow(true), ce(e2, o2, i2);
}
function ce(e2, t2, o2) {
  e2.dispatchEvent(new CustomEvent("sort-change", { detail: { field: t2.field, direction: o2 } })), r(e2, s(e2, 0 === o2 ? "sortCleared" : "sortApplied", t2.header ?? t2.field, 1 === o2 ? "ascending" : "descending")), e2.requestStateChange?.();
}
function de(e2) {
  return e2._pluginManager?._hasRowStructurePlugins ?? false;
}
function he(e2, t2) {
  if (e2._sortState && e2._sortState.field === t2.field) if (1 === e2._sortState.direction) ue(e2, t2, -1);
  else {
    if (e2._sortState = null, de(e2)) return e2._requestSchedulerPhase(4, "sort-clear"), void ce(e2, t2, 0);
    e2.__rowRenderEpoch++;
    for (let t3 = 0; t3 < e2._rowPool.length; t3++) e2._rowPool[t3].__epoch = -1;
    e2._rows = e2.__originalOrder.slice(), e2.__originalOrder = [], be(e2);
    const o2 = e2._headerRowEl?.querySelectorAll('[role="columnheader"].sortable');
    if (o2) for (let t3 = 0; t3 < o2.length; t3++) {
      const i2 = o2[t3];
      i2.getAttribute("aria-sort") && ("ascending" !== i2.getAttribute("aria-sort") && "descending" !== i2.getAttribute("aria-sort") || e2._sortState) || i2.setAttribute("aria-sort", "none");
    }
    e2.refreshVirtualWindow(true), ce(e2, t2, 0);
  }
  else e2._sortState || (e2.__originalOrder = e2._rows.slice()), ue(e2, t2, 1);
}
function ue(e2, t2, o2) {
  if (e2._sortState = { field: t2.field, direction: o2 }, de(e2)) return e2._requestSchedulerPhase(4, "sort-apply"), void ce(e2, t2, o2);
  const i2 = { field: t2.field, direction: o2 }, n2 = e2._columns, r2 = e2.effectiveConfig?.sortHandler ?? re;
  if (r2 === re) return le(e2._rows, i2.field, o2, n2), void ae(e2, e2._rows, t2, o2);
  const s2 = r2(e2._rows, i2, n2);
  s2 && "function" == typeof s2.then ? s2.then((i3) => {
    ae(e2, i3, t2, o2);
  }) : ae(e2, s2, t2, o2);
}
function ge(e2, t2) {
  return false !== e2.effectiveConfig?.sortable && true === t2.sortable;
}
function fe(e2, t2) {
  return false !== e2.effectiveConfig?.resizable && false !== t2.resizable;
}
function pe(e2, t2) {
  const o2 = document.createElement("span");
  k(o2, "sort-indicator");
  const i2 = e2._sortState?.field === t2.field ? e2._sortState.direction : 0, n2 = 1 === i2 ? "sortAsc" : -1 === i2 ? "sortDesc" : "sortNone";
  o2.dataset.icon = oe(n2);
  const r2 = e2.icons?.[n2];
  return void 0 !== r2 && (function(e3, t3) {
    "string" == typeof t3 ? e3.innerHTML = K(t3) : t3 instanceof HTMLElement && (e3.innerHTML = "", e3.appendChild(t3.cloneNode(true)));
  })(o2, r2), o2;
}
function we(e2, t2, o2) {
  const i2 = document.createElement("div");
  return i2.className = "resize-handle", i2.setAttribute("aria-hidden", "true"), i2.addEventListener("mousedown", (i3) => {
    i3.stopPropagation(), i3.preventDefault(), e2._resizeController.start(i3, t2, o2);
  }), i2.addEventListener("dblclick", (o3) => {
    o3.stopPropagation(), o3.preventDefault(), e2._resizeController.resetColumn(t2);
  }), i2;
}
function me(e2, t2, o2, i2) {
  i2.classList.add(z.SORTABLE), i2.tabIndex = 0;
  const n2 = e2._sortState?.field === t2.field ? e2._sortState.direction : 0;
  i2.setAttribute("aria-sort", 0 === n2 ? "none" : 1 === n2 ? "ascending" : "descending"), i2.addEventListener("click", (o3) => {
    e2._resizeController?.isResizing || e2._dispatchHeaderClick?.(o3, t2, i2) || he(e2, t2);
  }), i2.addEventListener("keydown", (o3) => {
    if ("Enter" === o3.key || " " === o3.key) {
      if (o3.preventDefault(), e2._dispatchHeaderClick?.(o3, t2, i2)) return;
      he(e2, t2);
    }
  });
}
function be(e2) {
  e2._headerRowEl = e2.findHeaderRow();
  const t2 = e2._headerRowEl;
  t2 && (t2.innerHTML = "", e2._visibleColumns.forEach((o2, i2) => {
    const n2 = document.createElement("div");
    n2.className = "cell", k(n2, "header-cell"), n2.setAttribute("role", "columnheader"), n2.setAttribute("aria-colindex", String(i2 + 1)), n2.setAttribute("data-field", o2.field), n2.setAttribute("data-col", String(i2)), o2.type && n2.setAttribute("data-type", o2.type);
    const r2 = o2.header ?? o2.field, s2 = e2._sortState?.field === o2.field ? e2._sortState.direction : 0, l2 = 1 === s2 ? "asc" : -1 === s2 ? "desc" : null;
    if (o2.headerRenderer) {
      const t3 = { column: o2, value: r2, sortState: l2, filterActive: false, cellEl: n2, renderSortIcon: () => ge(e2, o2) ? pe(e2, o2) : null, renderFilterButton: () => null }, s3 = o2.headerRenderer(t3);
      !(function(e3, t4) {
        if (null != t4) if ("string" == typeof t4) {
          const o3 = document.createElement("span");
          for (o3.innerHTML = K(t4); o3.firstChild; ) e3.appendChild(o3.firstChild);
        } else t4 instanceof Node && e3.appendChild(t4);
      })(n2, s3), ge(e2, o2) && me(e2, o2, 0, n2), fe(e2, o2) && (n2.classList.add("resizable"), n2.appendChild(we(e2, i2, n2)));
    } else if (o2.headerLabelRenderer) {
      const t3 = { column: o2, value: r2 }, s3 = o2.headerLabelRenderer(t3), l3 = document.createElement("span");
      null == s3 ? l3.textContent = r2 : "string" == typeof s3 ? l3.innerHTML = K(s3) : s3 instanceof Node && l3.appendChild(s3), n2.appendChild(l3), ge(e2, o2) && (me(e2, o2, 0, n2), n2.appendChild(pe(e2, o2))), fe(e2, o2) && (n2.classList.add("resizable"), n2.appendChild(we(e2, i2, n2)));
    } else if (o2.__headerTemplate) Array.from(o2.__headerTemplate.childNodes).forEach((e3) => n2.appendChild(e3.cloneNode(true))), ge(e2, o2) && (me(e2, o2, 0, n2), n2.appendChild(pe(e2, o2))), fe(e2, o2) && (n2.classList.add("resizable"), n2.appendChild(we(e2, i2, n2)));
    else {
      const t3 = document.createElement("span");
      t3.textContent = r2, n2.appendChild(t3), ge(e2, o2) && (me(e2, o2, 0, n2), n2.appendChild(pe(e2, o2))), fe(e2, o2) && (n2.classList.add("resizable"), n2.appendChild(we(e2, i2, n2)));
    }
    t2.appendChild(n2);
  }), t2.querySelectorAll(`.cell.${z.SORTABLE}`).forEach((e3) => {
    e3.getAttribute("aria-sort") || e3.setAttribute("aria-sort", "none");
  }), t2.children.length > 0 ? (t2.setAttribute("role", "row"), t2.setAttribute("aria-rowindex", "1")) : (t2.removeAttribute("role"), t2.removeAttribute("aria-rowindex")));
}
function ve(e2, t2) {
  const o2 = e2[0] || {}, i2 = Object.keys(o2).map((e3) => {
    const t3 = o2[e3], i3 = null == (n3 = t3) ? "string" : "number" == typeof n3 ? "number" : "boolean" == typeof n3 ? "boolean" : n3 instanceof Date || "string" == typeof n3 && /\d{4}-\d{2}-\d{2}/.test(n3) && !isNaN(Date.parse(n3)) ? "date" : "string";
    var n3;
    return { field: e3, header: e3.charAt(0).toUpperCase() + e3.slice(1), type: i3 };
  }), n2 = {};
  return i2.forEach((e3) => {
    n2[e3.field] = e3.type || "string";
  }), { columns: i2, typeMap: n2 };
}
var Ce;
var _e = ((Ce = _e || {})[Ce.STYLE = 1] = "STYLE", Ce[Ce.VIRTUALIZATION = 2] = "VIRTUALIZATION", Ce[Ce.HEADER = 3] = "HEADER", Ce[Ce.ROWS = 4] = "ROWS", Ce[Ce.COLUMNS = 5] = "COLUMNS", Ce[Ce.FULL = 6] = "FULL", Ce);
var ye = class {
  #o;
  #i = 0;
  #n = 0;
  #r = null;
  #s = null;
  #l = null;
  #a = false;
  constructor(e2) {
    this.#o = e2;
  }
  requestPhase(e2, t2) {
    e2 > this.#i && (this.#i = e2), 0 === this.#n && (this.#c(), this.#n = requestAnimationFrame(() => this.#d()));
  }
  whenReady() {
    return this.#r ? this.#r : Promise.resolve();
  }
  setInitialReadyResolver(e2) {
    this.#l = e2;
  }
  cancel() {
    0 !== this.#n && (cancelAnimationFrame(this.#n), this.#n = 0), this.#i = 0, this.#s && (this.#s(), this.#s = null, this.#r = null);
  }
  get isPending() {
    return 0 !== this.#i;
  }
  get pendingPhase() {
    return this.#i;
  }
  #c() {
    this.#r || (this.#r = new Promise((e2) => {
      this.#s = e2;
    }));
  }
  #d() {
    if (this.#n = 0, !this.#o._schedulerIsConnected) return this.#i = 0, void (this.#s && (this.#s(), this.#s = null, this.#r = null));
    const e2 = this.#i;
    this.#i = 0, e2 >= 5 && this.#o._schedulerMergeConfig(), e2 >= 4 && this.#o._schedulerProcessRows(), e2 >= 5 && (this.#o._schedulerProcessColumns(), this.#o._schedulerUpdateTemplate()), e2 >= 3 && this.#o._schedulerRenderHeader(), e2 >= 2 && this.#o.refreshVirtualWindow(true, true), e2 >= 1 && this.#o._schedulerAfterRender(), !this.#a && this.#l && (this.#a = true, this.#l()), this.#s && (this.#s(), this.#s = null, this.#r = null);
  }
};
var Re = class {
  #h;
  #u;
  #g;
  #f;
  #p;
  #w = {};
  #m = {};
  #b = true;
  #v = [];
  #C;
  #_;
  #y;
  #R;
  #o;
  #S;
  constructor(e2) {
    this.#o = e2;
  }
  get original() {
    return this.#w;
  }
  get effective() {
    return this.#m;
  }
  get columns() {
    return this.#m.columns ?? [];
  }
  set columns(e2) {
    this.#m.columns = e2;
  }
  get lightDomColumnsCache() {
    return this.#f;
  }
  set lightDomColumnsCache(e2) {
    this.#f = e2;
  }
  get originalColumnNodes() {
    return this.#p;
  }
  set originalColumnNodes(e2) {
    this.#p = e2;
  }
  get lightDomTitle() {
    return this.#S;
  }
  set lightDomTitle(e2) {
    this.#S = e2;
  }
  get initialColumnState() {
    return this.#R;
  }
  set initialColumnState(e2) {
    this.#R = e2;
  }
  get sourcesChanged() {
    return this.#b;
  }
  markSourcesChanged() {
    this.#b = true;
  }
  setGridConfig(e2) {
    this.#h = e2, this.#b = true, this.#f = void 0;
  }
  getGridConfig() {
    return this.#h;
  }
  setColumns(e2) {
    this.#u = e2, this.#b = true;
  }
  getColumns() {
    return this.#u;
  }
  setFitMode(e2) {
    this.#g = e2, this.#b = true;
  }
  getFitMode() {
    return this.#g;
  }
  merge() {
    const e2 = (this.#m.columns?.length ?? 0) > 0;
    if (!this.#b && e2) return;
    const t2 = this.#E();
    this.#b = false, this.#w = t2, Object.freeze(this.#w), this.#w.columns && Object.freeze(this.#w.columns), this.#m = this.#x(this.#w), this.#T();
  }
  #x(e2) {
    const t2 = { ...e2 };
    return e2.columns && (t2.columns = e2.columns.map((e3) => ({ ...e3 }))), e2.shell && (t2.shell = { ...e2.shell, header: e2.shell.header ? { ...e2.shell.header } : void 0, toolPanel: e2.shell.toolPanel ? { ...e2.shell.toolPanel } : void 0, toolPanels: e2.shell.toolPanels?.map((e3) => ({ ...e3 })), headerContents: e2.shell.headerContents?.map((e3) => ({ ...e3 })) }), t2;
  }
  #T() {
    const e2 = this.#m;
    if (this.#A(), "number" == typeof e2.rowHeight && e2.rowHeight > 0 && (this.#o._virtualization.rowHeight = e2.rowHeight), "fixed" === e2.fitMode) {
      this.columns.forEach((e3) => {
        null == e3.width && (e3.width = 80);
      });
    }
    this.#o._applyAnimationConfig(e2);
  }
  #A() {
    const e2 = this.#m.typeDefaults;
    if (!e2) return;
    const t2 = this.columns;
    for (const o2 of t2) {
      if (!o2.type) continue;
      const t3 = e2[o2.type];
      t3 && (o2.renderer || o2.viewRenderer || !t3.renderer || (o2.renderer = t3.renderer), !o2.format && t3.format && (o2.format = t3.format), !o2.editor && t3.editor && (o2.editor = t3.editor), !o2.editorParams && t3.editorParams && (o2.editorParams = t3.editorParams));
    }
  }
  #E() {
    const e2 = this.#h ? { ...this.#h } : {}, t2 = Array.isArray(e2.columns) ? [...e2.columns] : [], o2 = (this.#f ?? []).map((e3) => ({ ...e3 }));
    let i2 = M(t2, o2);
    this.#u && this.#u.length && (i2 = M(this.#u, o2));
    const n2 = this.#o.sourceRows;
    if (0 === i2.length && n2.length) {
      i2 = ve(n2).columns;
    }
    return i2.length && (i2.forEach((e3) => {
      void 0 === e3.sortable && (e3.sortable = true), void 0 === e3.resizable && (e3.resizable = true), void 0 === e3.__originalWidth && "number" == typeof e3.width && (e3.__originalWidth = e3.width);
    }), i2.forEach((e3) => {
      e3.__viewTemplate && !e3.__compiledView && (e3.__compiledView = te(e3.__viewTemplate.innerHTML)), e3.__editorTemplate && !e3.__compiledEditor && (e3.__compiledEditor = te(e3.__editorTemplate.innerHTML));
    }), e2.columns = i2), this.#g && (e2.fitMode = this.#g), e2.fitMode || (e2.fitMode = "stretch"), this.#P(e2), e2.columnState && !this.#R && (this.#R = e2.columnState), e2;
  }
  #P(e2) {
    e2.shell = e2.shell ? { ...e2.shell } : {}, e2.shell.header = e2.shell.header ? { ...e2.shell.header } : {};
    const t2 = this.#o._shellState.lightDomTitle;
    t2 && (this.#S = t2), this.#S && !e2.shell.header.title && (e2.shell.header.title = this.#S);
    const o2 = this.#o._shellState.lightDomHeaderContent;
    o2?.length > 0 && (e2.shell.header.lightDomContent = o2), this.#o._shellState.hasToolButtonsContainer && (e2.shell.header.hasToolButtonsContainer = true);
    const i2 = this.#o._shellState.toolPanels;
    if (i2.size > 0) {
      const t3 = Array.from(i2.values());
      t3.sort((e3, t4) => (e3.order ?? 100) - (t4.order ?? 100)), e2.shell.toolPanels = t3;
    }
    const n2 = this.#o._shellState.headerContents;
    if (n2.size > 0) {
      const t3 = Array.from(n2.values());
      t3.sort((e3, t4) => (e3.order ?? 100) - (t4.order ?? 100)), e2.shell.headerContents = t3;
    }
    const r2 = this.#o._shellState.toolbarContents, s2 = Array.from(r2.values()), l2 = this.#h?.shell?.header?.toolbarContents ?? [], a2 = new Set(l2.map((e3) => e3.id)), c2 = [...l2];
    for (const d2 of s2) a2.has(d2.id) || c2.push(d2);
    c2.sort((e3, t3) => (e3.order ?? 0) - (t3.order ?? 0)), e2.shell.header.toolbarContents = c2;
  }
  collectState(e2) {
    const t2 = this.columns, o2 = this.#H();
    return { columns: t2.map((t3, i2) => {
      const n2 = { field: t3.field, order: i2, visible: !t3.hidden }, r2 = t3;
      void 0 !== r2.__renderedWidth ? n2.width = r2.__renderedWidth : void 0 !== t3.width && (n2.width = "string" == typeof t3.width ? parseFloat(t3.width) : t3.width);
      const s2 = o2.get(t3.field);
      s2 && (n2.sort = s2);
      for (const o3 of e2) if (o3.getColumnState) {
        const e3 = o3.getColumnState(t3.field);
        e3 && Object.assign(n2, e3);
      }
      return n2;
    }) };
  }
  applyState(e2, t2) {
    if (!e2.columns || 0 === e2.columns.length) return;
    const o2 = this.columns, i2 = new Map(e2.columns.map((e3) => [e3.field, e3])), n2 = o2.map((e3) => {
      const t3 = i2.get(e3.field);
      if (!t3) return e3;
      const o3 = { ...e3 };
      return void 0 !== t3.width && (o3.width = t3.width, o3.__renderedWidth = t3.width), void 0 !== t3.visible && (o3.hidden = !t3.visible), o3;
    });
    n2.sort((e3, t3) => (i2.get(e3.field)?.order ?? 1 / 0) - (i2.get(t3.field)?.order ?? 1 / 0)), this.columns = n2;
    const r2 = e2.columns.filter((e3) => void 0 !== e3.sort).sort((e3, t3) => (e3.sort?.priority ?? 0) - (t3.sort?.priority ?? 0));
    if (r2.length > 0) {
      const e3 = r2[0];
      e3.sort && (this.#o._sortState = { field: e3.field, direction: "asc" === e3.sort.direction ? 1 : -1 });
    } else this.#o._sortState = null;
    for (const s2 of t2) if (s2.applyColumnState) for (const t3 of e2.columns) s2.applyColumnState(t3.field, t3);
  }
  resetState(e2) {
    this.#R = void 0, this.#o._sortState = null, this.#m = this.#x(this.#w), this.#T();
    for (const t2 of e2) if (t2.applyColumnState) for (const e3 of this.columns) t2.applyColumnState(e3.field, { field: e3.field, order: 0, visible: true });
    this.requestStateChange(e2);
  }
  #H() {
    const e2 = /* @__PURE__ */ new Map(), t2 = this.#o._sortState;
    return t2 && e2.set(t2.field, { direction: 1 === t2.direction ? "asc" : "desc", priority: 0 }), e2;
  }
  requestStateChange(e2) {
    this.#_ && clearTimeout(this.#_), this.#_ = setTimeout(() => {
      this.#_ = void 0;
      const t2 = this.collectState(e2);
      this.#o._emit("column-state-change", t2);
    }, 100);
  }
  setColumnVisible(e2, t2) {
    const o2 = this.columns, i2 = o2.find((t3) => t3.field === e2);
    if (!i2) return false;
    if (!t2 && i2.lockVisible) return false;
    if (!t2) {
      if (0 === o2.filter((t3) => !t3.hidden && t3.field !== e2).length) return false;
    }
    return !!i2.hidden != !t2 && (i2.hidden = !t2, this.#o._emit("column-visibility", { field: e2, visible: t2, visibleColumns: o2.filter((e3) => !e3.hidden).map((e3) => e3.field) }), this.#o._clearRowPool(), this.#o._setup(), true);
  }
  toggleColumnVisibility(e2) {
    const t2 = this.columns.find((t3) => t3.field === e2);
    return !!t2 && this.setColumnVisible(e2, !!t2.hidden);
  }
  isColumnVisible(e2) {
    const t2 = this.columns.find((t3) => t3.field === e2);
    return !!t2 && !t2.hidden;
  }
  showAllColumns() {
    const e2 = this.columns;
    e2.some((e3) => e3.hidden) && (e2.forEach((e3) => e3.hidden = false), this.#o._emit("column-visibility", { visibleColumns: e2.map((e3) => e3.field) }), this.#o._clearRowPool(), this.#o._setup());
  }
  getAllColumns() {
    return this.columns.map((e2) => ({ field: e2.field, header: e2.header || e2.field, visible: !e2.hidden, lockVisible: e2.lockVisible, utility: true === e2.meta?.utility }));
  }
  getColumnOrder() {
    return this.columns.map((e2) => e2.field);
  }
  setColumnOrder(e2) {
    if (!e2.length) return;
    const t2 = new Map(this.columns.map((e3) => [e3.field, e3])), o2 = [];
    for (const i2 of e2) {
      const e3 = t2.get(i2);
      e3 && (o2.push(e3), t2.delete(i2));
    }
    for (const i2 of t2.values()) o2.push(i2);
    this.columns = o2, this.#o._invalidateVisibleColumnsCache(), be(this.#o), I(this.#o), this.#o._requestSchedulerPhase(_e.COLUMNS, "configManager");
  }
  parseLightDomColumns(e2) {
    this.#f || (this.#p = Array.from(e2.querySelectorAll("tbw-grid-column")), this.#f = this.#p.length ? (function(e3) {
      return Array.from(e3.querySelectorAll("tbw-grid-column")).map((e4) => {
        const t2 = e4.getAttribute("field") || "";
        if (!t2) return null;
        const o2 = e4.getAttribute("type") || void 0, i2 = { field: t2, type: o2 && (/* @__PURE__ */ new Set(["number", "string", "date", "boolean", "select"])).has(o2) ? o2 : void 0, header: e4.getAttribute("header") || void 0, sortable: e4.hasAttribute("sortable"), editable: e4.hasAttribute("editable") }, n2 = e4.getAttribute("width");
        if (n2) {
          const e5 = parseFloat(n2);
          !isNaN(e5) && /^\d+(\.\d+)?$/.test(n2.trim()) ? i2.width = e5 : i2.width = n2;
        }
        const r2 = e4.getAttribute("minWidth") || e4.getAttribute("min-width");
        if (r2) {
          const e5 = parseFloat(r2);
          isNaN(e5) || (i2.minWidth = e5);
        }
        e4.hasAttribute("resizable") && (i2.resizable = true);
        const s2 = e4.getAttribute("editor"), l2 = e4.getAttribute("renderer");
        s2 && (i2.__editorName = s2), l2 && (i2.__rendererName = l2);
        const a2 = e4.getAttribute("options");
        a2 && (i2.options = a2.split(",").map((e5) => {
          const [t3, o3] = e5.includes(":") ? e5.split(":") : [e5.trim(), e5.trim()];
          return { value: t3.trim(), label: o3?.trim() || t3.trim() };
        }));
        const c2 = e4.querySelector("tbw-grid-column-view"), d2 = e4.querySelector("tbw-grid-column-editor"), h2 = e4.querySelector("tbw-grid-column-header");
        c2 && (i2.__viewTemplate = c2), d2 && (i2.__editorTemplate = d2), h2 && (i2.__headerTemplate = h2);
        const u2 = globalThis.DataGridElement, g2 = u2?.getAdapters?.() ?? [], f2 = c2 ?? e4, p2 = g2.find((e5) => e5.canHandle(f2));
        if (p2) {
          const e5 = p2.createRenderer(f2);
          e5 && (i2.viewRenderer = e5);
        }
        const w2 = d2 ?? e4, m2 = g2.find((e5) => e5.canHandle(w2));
        if (m2) {
          const e5 = m2.createEditor(w2);
          e5 && (i2.editor = e5);
        }
        return i2;
      }).filter((e4) => !!e4);
    })(e2) : []);
  }
  clearLightDomCache() {
    this.#f = void 0;
  }
  #M = /* @__PURE__ */ new Map();
  registerLightDomHandler(e2, t2) {
    this.#M.set(e2.toLowerCase(), t2);
  }
  unregisterLightDomHandler(e2) {
    this.#M.delete(e2.toLowerCase());
  }
  observeLightDOM(e2) {
    this.#C && this.#C.disconnect();
    const t2 = /* @__PURE__ */ new Set(), o2 = () => {
      this.#y = void 0;
      for (const e3 of t2) {
        const t3 = this.#M.get(e3);
        t3?.();
      }
      t2.clear();
    };
    this.#C = new MutationObserver((e3) => {
      for (const o3 of e3) {
        for (const e4 of o3.addedNodes) {
          if (e4.nodeType !== Node.ELEMENT_NODE) continue;
          const o4 = e4.tagName.toLowerCase();
          this.#M.has(o4) && t2.add(o4);
        }
        if ("attributes" === o3.type && o3.target.nodeType === Node.ELEMENT_NODE) {
          const e4 = o3.target.tagName.toLowerCase();
          this.#M.has(e4) && t2.add(e4);
        }
      }
      t2.size > 0 && !this.#y && (this.#y = setTimeout(o2, 0));
    }), this.#C.observe(e2, { childList: true, subtree: true, attributes: true, attributeFilter: ["title", "field", "header", "width", "hidden", "id", "icon", "tooltip", "order"] });
  }
  onChange(e2) {
    this.#v.push(e2);
  }
  notifyChange() {
    for (const e2 of this.#v) e2();
  }
  dispose() {
    this.#C?.disconnect(), this.#v = [], this.#_ && clearTimeout(this.#_), this.#y && (clearTimeout(this.#y), this.#y = void 0);
  }
};
function Se() {
  if ("undefined" != typeof window && window.location) {
    const e2 = window.location.hostname;
    if ("localhost" === e2 || "127.0.0.1" === e2 || "::1" === e2) return true;
  }
  return "undefined" != typeof process && true;
}
function Ee(e2) {
  return `<span role="checkbox" aria-checked="${e2}" aria-label="${e2}">${e2 ? "&#x1F5F9;" : "&#9744;"}</span>`;
}
function xe(e2) {
  if (null == e2 || "" === e2) return "";
  if (e2 instanceof Date) return isNaN(e2.getTime()) ? "" : e2.toLocaleDateString();
  if ("number" == typeof e2 || "string" == typeof e2) {
    const t2 = new Date(e2);
    return isNaN(t2.getTime()) ? "" : t2.toLocaleDateString();
  }
  return "";
}
function Te(e2) {
  if (!e2) return -1;
  const t2 = e2.getAttribute("data-row");
  if (t2) return parseInt(t2, 10);
  const o2 = e2.closest(".data-grid-row");
  if (!o2) return -1;
  const i2 = o2.parentElement;
  if (!i2) return -1;
  const n2 = i2.querySelectorAll(":scope > .data-grid-row");
  for (let r2 = 0; r2 < n2.length; r2++) if (n2[r2] === o2) return r2;
  return -1;
}
function Ae(e2) {
  e2 && e2.querySelectorAll(".cell-focus").forEach((e3) => e3.classList.remove("cell-focus"));
}
function Pe(e2) {
  try {
    if ("rtl" === getComputedStyle(e2).direction) return "rtl";
  } catch {
  }
  try {
    const t2 = e2.closest?.("[dir]")?.getAttribute("dir");
    if ("rtl" === t2) return "rtl";
  } catch {
  }
  return "ltr";
}
function He(e2) {
  return "rtl" === Pe(e2);
}
function Me(e2, t2) {
  return "left" === e2 || "right" === e2 ? e2 : "rtl" === t2 ? "start" === e2 ? "right" : "left" : "start" === e2 ? "left" : "right";
}
function ke(e2, t2) {
  const o2 = t2.renderer || t2.viewRenderer;
  if (o2) return o2;
  if (!t2.type) return;
  const i2 = e2.__frameworkAdapter;
  if (i2?.getTypeDefault) {
    const o3 = i2.getTypeDefault(t2.type, e2._hostElement);
    if (o3?.renderer) return o3.renderer;
  }
}
function Le(e2, t2) {
  if (t2.format) return t2.format;
  if (!t2.type) return;
  const o2 = e2.__frameworkAdapter;
  if (o2?.getTypeDefault) {
    const i2 = o2.getTypeDefault(t2.type, e2._hostElement);
    if (i2?.format) return i2.format;
  }
}
var Oe = 'input,select,textarea,[contenteditable="true"],[contenteditable=""],[tabindex]:not([tabindex="-1"])';
function De(e2) {
  return (e2.__editingCellCount ?? 0) > 0;
}
function Ie(e2) {
  e2.__editingCellCount = 0, e2.removeAttribute("data-has-editing");
  e2.querySelectorAll(`.cell.${z.EDITING}`).forEach((e3) => e3.classList.remove(z.EDITING));
}
var ze = document.createElement("template");
ze.innerHTML = '<div class="cell" role="gridcell" part="cell"></div>';
var Ne = document.createElement("template");
function qe() {
  return ze.content.firstElementChild.cloneNode(true);
}
function $e() {
  return Ne.content.firstElementChild.cloneNode(true);
}
function We(e2) {
  e2.__cellDisplayCache = void 0, e2.__cellCacheEpoch = void 0, e2.__hasSpecialColumns = void 0;
}
function Fe(e2, t2, o2, i2) {
  const n2 = t2.children, r2 = e2._visibleColumns, s2 = r2.length, l2 = n2.length, a2 = s2 < l2 ? s2 : l2, h2 = e2._focusRow, u2 = e2._focusCol, g2 = e2._hasAfterCellRenderHook?.() ?? false;
  let f2 = e2.__hasSpecialColumns;
  if (void 0 === f2) {
    f2 = false;
    const t3 = e2.__frameworkAdapter;
    for (let o3 = 0; o3 < s2; o3++) {
      const i3 = r2[o3];
      if (i3.__viewTemplate || i3.__compiledView || i3.renderer || i3.viewRenderer || i3.externalView || i3.format || i3.cellClass || "date" === i3.type || "boolean" === i3.type || i3.type && t3?.getTypeDefault?.(i3.type, e2._hostElement)?.renderer || i3.type && t3?.getTypeDefault?.(i3.type, e2._hostElement)?.format) {
        f2 = true;
        break;
      }
    }
    e2.__hasSpecialColumns = f2;
  }
  const p2 = String(i2);
  if (f2) {
    for (let s3 = 0; s3 < a2; s3++) {
      if (r2[s3].externalView) {
        if (!n2[s3].querySelector("[data-external-view]")) return void Be(e2, t2, o2, i2);
      }
    }
    for (let s3 = 0; s3 < a2; s3++) {
      const l3 = r2[s3], a3 = n2[s3];
      a3.getAttribute("data-row") !== p2 && a3.setAttribute("data-row", p2);
      const f3 = a3.classList.contains(z.EDITING);
      if (!f3) {
        const e3 = h2 === i2 && u2 === s3;
        e3 !== a3.classList.contains("cell-focus") && (a3.classList.toggle("cell-focus", e3), a3.setAttribute("aria-selected", String(e3)));
      }
      const m2 = l3.cellClass;
      if (m2) {
        const t3 = a3.getAttribute("data-dynamic-classes");
        if (t3) {
          const e3 = t3.split(" ");
          for (let t4 = 0; t4 < e3.length; t4++) e3[t4] && a3.classList.remove(e3[t4]);
        }
        try {
          const e3 = m2(o2[l3.field], o2, l3), t4 = "string" == typeof e3 ? e3.split(/\s+/) : e3;
          if (t4 && t4.length > 0) {
            let e4 = "";
            for (let o3 = 0; o3 < t4.length; o3++) {
              const i3 = t4[o3];
              i3 && "string" == typeof i3 && (a3.classList.add(i3), e4 += (e4 ? " " : "") + i3);
            }
            a3.setAttribute("data-dynamic-classes", e4);
          } else a3.removeAttribute("data-dynamic-classes");
        } catch (w2) {
          A(c, `cellClass callback error for column '${l3.field}': ${w2}`, e2.id), a3.removeAttribute("data-dynamic-classes");
        }
      }
      if (f3) continue;
      const b2 = ke(e2, l3);
      if (b2) {
        const n3 = o2[l3.field], r3 = b2({ row: o2, value: n3, field: l3.field, column: l3, grid: e2, cellEl: a3 });
        "string" == typeof r3 ? (e2.__frameworkAdapter?.releaseCell?.(a3), a3.innerHTML = K(r3)) : r3 instanceof Node ? r3.parentElement !== a3 && (e2.__frameworkAdapter?.releaseCell?.(a3), a3.innerHTML = "", a3.appendChild(r3)) : null == r3 && (e2.__frameworkAdapter?.releaseCell?.(a3), a3.textContent = null == n3 ? "" : String(n3)), g2 && e2._afterCellRender?.({ row: o2, rowIndex: i2, column: l3, colIndex: s3, value: n3, cellElement: a3, rowElement: t2 });
        continue;
      }
      if (l3.__compiledView) {
        const n3 = o2[l3.field], r3 = l3.__compiledView({ row: o2, value: n3, field: l3.field, column: l3 });
        l3.__compiledView.__blocked ? a3.textContent = "" : (a3.firstElementChild && e2.__frameworkAdapter?.releaseCell?.(a3), a3.innerHTML = K(r3), ee(a3)), g2 && e2._afterCellRender?.({ row: o2, rowIndex: i2, column: l3, colIndex: s3, value: n3, cellElement: a3, rowElement: t2 });
        continue;
      }
      if (l3.__viewTemplate) {
        const n3 = o2[l3.field], r3 = l3.__viewTemplate.innerHTML;
        /Reflect\.|\bProxy\b|ownKeys\(/.test(r3) ? a3.textContent = "" : (a3.firstElementChild && e2.__frameworkAdapter?.releaseCell?.(a3), a3.innerHTML = K(Q(r3, { row: o2, value: n3 })), ee(a3)), g2 && e2._afterCellRender?.({ row: o2, rowIndex: i2, column: l3, colIndex: s3, value: n3, cellElement: a3, rowElement: t2 });
        continue;
      }
      if (l3.externalView) continue;
      const v2 = o2[l3.field];
      let C2;
      a3.firstElementChild && e2.__frameworkAdapter?.releaseCell?.(a3);
      const _2 = Le(e2, l3);
      if (_2) {
        try {
          const e3 = _2(v2, o2);
          C2 = null == e3 ? "" : String(e3);
        } catch (w2) {
          A(d, `Format error in column '${l3.field}': ${w2}`, e2.id), C2 = null == v2 ? "" : String(v2);
        }
        a3.textContent = C2;
      } else "date" === l3.type ? (C2 = xe(v2), a3.textContent = C2) : "boolean" === l3.type ? a3.innerHTML = Ee(!!v2) : (C2 = null == v2 ? "" : String(v2), a3.textContent = C2);
      g2 && e2._afterCellRender?.({ row: o2, rowIndex: i2, column: l3, colIndex: s3, value: v2, cellElement: a3, rowElement: t2 });
    }
  } else for (let c2 = 0; c2 < a2; c2++) {
    const s3 = n2[c2];
    if (s3.classList.contains(z.EDITING)) continue;
    s3.firstElementChild && e2.__frameworkAdapter?.releaseCell?.(s3);
    const l3 = r2[c2], a3 = o2[l3.field];
    s3.textContent = null == a3 ? "" : String(a3), s3.getAttribute("data-row") !== p2 && s3.setAttribute("data-row", p2);
    const d2 = h2 === i2 && u2 === c2;
    d2 !== s3.classList.contains("cell-focus") && (s3.classList.toggle("cell-focus", d2), s3.setAttribute("aria-selected", String(d2))), g2 && e2._afterCellRender?.({ row: o2, rowIndex: i2, column: l3, colIndex: c2, value: a3, cellElement: s3, rowElement: t2 });
  }
}
function Be(e2, t2, o2, i2) {
  t2.classList.remove("tbw-row-loading"), t2.removeAttribute("aria-busy");
  const n2 = e2.__frameworkAdapter;
  if (n2?.releaseCell) {
    const e3 = t2.children;
    for (let t3 = e3.length - 1; t3 >= 0; t3--) n2.releaseCell(e3[t3]);
  }
  t2.innerHTML = "";
  const r2 = e2._visibleColumns, s2 = r2.length, l2 = e2._focusRow, a2 = e2._focusCol, h2 = e2._hasAfterCellRenderHook?.() ?? false, u2 = document.createDocumentFragment();
  for (let f2 = 0; f2 < s2; f2++) {
    const n3 = r2[f2], s3 = qe();
    s3.setAttribute("aria-colindex", String(f2 + 1)), s3.setAttribute("data-col", String(f2)), s3.setAttribute("data-row", String(i2)), s3.setAttribute("data-field", n3.field), s3.setAttribute("data-header", n3.header ?? n3.field), n3.type && s3.setAttribute("data-type", n3.type);
    let p2 = o2[n3.field];
    const w2 = Le(e2, n3);
    if (w2) try {
      p2 = w2(p2, o2);
    } catch (g2) {
      A(d, `Format error in column '${n3.field}': ${g2}`, e2.id);
    }
    const m2 = n3.__compiledView, b2 = n3.__viewTemplate, v2 = ke(e2, n3), C2 = n3.externalView;
    let _2 = false;
    if (v2) {
      const t3 = v2({ row: o2, value: p2, field: n3.field, column: n3, grid: e2, cellEl: s3 });
      "string" == typeof t3 ? (s3.innerHTML = K(t3), _2 = true) : t3 instanceof Node ? t3.parentElement !== s3 && (s3.textContent = "", s3.appendChild(t3)) : null == t3 && (s3.textContent = null == p2 ? "" : String(p2));
    } else if (C2) {
      const t3 = C2, i3 = document.createElement("div");
      i3.setAttribute("data-external-view", ""), i3.setAttribute("data-field", n3.field), s3.appendChild(i3);
      const r3 = { row: o2, value: p2, field: n3.field, column: n3, grid: e2 };
      if (t3.mount) try {
        t3.mount({ placeholder: i3, context: r3, spec: t3 });
      } catch (g2) {
        A("TBW063", `External view mount error for column '${n3.field}': ${g2}`, e2.id);
      }
      else queueMicrotask(() => {
        try {
          e2.dispatchEvent(new CustomEvent("mount-external-view", { bubbles: true, composed: true, detail: { placeholder: i3, spec: t3, context: r3 } }));
        } catch (g2) {
          A("TBW064", `External view event dispatch error for column '${n3.field}': ${g2}`, e2.id);
        }
      });
      i3.setAttribute("data-mounted", "");
    } else if (m2) {
      const e3 = m2({ row: o2, value: p2, field: n3.field, column: n3 }), t3 = m2.__blocked;
      s3.innerHTML = t3 ? "" : K(e3), _2 = true, t3 && (s3.textContent = "", s3.setAttribute("data-blocked-template", ""));
    } else if (b2) {
      const e3 = b2.innerHTML;
      /Reflect\.|\bProxy\b|ownKeys\(/.test(e3) ? (s3.textContent = "", s3.setAttribute("data-blocked-template", "")) : (s3.innerHTML = K(Q(e3, { row: o2, value: p2 })), _2 = true);
    } else w2 ? s3.textContent = null == p2 ? "" : String(p2) : "date" === n3.type ? s3.textContent = xe(p2) : "boolean" === n3.type ? s3.innerHTML = Ee(!!p2) : s3.textContent = null == p2 ? "" : String(p2);
    if (_2) {
      ee(s3);
      const e3 = s3.textContent || "";
      /Proxy|Reflect\.ownKeys/.test(e3) && (s3.textContent = e3.replace(/Proxy|Reflect\.ownKeys/g, "").trim(), /Proxy|Reflect\.ownKeys/.test(s3.textContent || "") && (s3.textContent = ""));
    }
    s3.hasAttribute("data-blocked-template") && (s3.textContent || "").trim().length && (s3.textContent = "");
    ("function" == typeof n3.editable ? n3.editable(o2) : n3.editable) ? s3.tabIndex = 0 : "boolean" === n3.type && (s3.hasAttribute("tabindex") || (s3.tabIndex = 0)), l2 === i2 && a2 === f2 ? (s3.classList.add("cell-focus"), s3.setAttribute("aria-selected", "true")) : s3.setAttribute("aria-selected", "false");
    const y2 = n3.cellClass;
    if (y2) try {
      const e3 = y2(o2[n3.field], o2, n3), t3 = "string" == typeof e3 ? e3.split(/\s+/) : e3;
      if (t3 && t3.length > 0) {
        let e4 = "";
        for (const o3 of t3) o3 && "string" == typeof o3 && (s3.classList.add(o3), e4 += (e4 ? " " : "") + o3);
        s3.setAttribute("data-dynamic-classes", e4);
      }
    } catch (g2) {
      A(c, `cellClass callback error for column '${n3.field}': ${g2}`, e2.id);
    }
    h2 && e2._afterCellRender?.({ row: o2, rowIndex: i2, column: n3, colIndex: f2, value: p2, cellElement: s3, rowElement: t2 }), u2.appendChild(s3);
  }
  t2.appendChild(u2);
}
function Ve(e2, t2, o2) {
  if (t2.target?.closest(".resize-handle")) return;
  const i2 = Te(o2.querySelector(".cell[data-row]"));
  if (i2 < 0) return;
  const n2 = e2._rows[i2];
  if (!n2) return;
  if (e2._dispatchRowClick?.(t2, i2, n2, o2)) return;
  const r2 = t2.target?.closest(".cell[data-col]");
  if (r2) {
    const o3 = Number(r2.getAttribute("data-col"));
    if (!isNaN(o3)) {
      if (e2._dispatchCellClick?.(t2, i2, o3, r2)) return;
      const n3 = e2._focusRow !== i2 || e2._focusCol !== o3;
      if (e2._focusRow = i2, e2._focusCol = o3, r2.classList.contains(z.EDITING)) {
        n3 && (Ae(e2._bodyEl ?? e2), r2.classList.add("cell-focus"));
        const o4 = t2.target, i3 = r2.contains(o4) && o4.matches(Oe) ? o4 : r2.querySelector(Oe);
        try {
          i3?.focus({ preventScroll: true });
        } catch {
        }
        return;
      }
      je(e2);
    }
  }
}
function Ue(e2) {
  "function" == typeof e2.commitActiveRowEdit && e2.commitActiveRowEdit();
}
function Ge(e2) {
  if (!e2) return false;
  const t2 = e2.tagName;
  return "INPUT" === t2 || "SELECT" === t2 || "TEXTAREA" === t2 || !!e2.isContentEditable;
}
function je(e2, t2) {
  if (e2._virtualization?.enabled) {
    const { rowHeight: t3, container: o3, viewportEl: i3 } = e2._virtualization, n3 = o3, r3 = i3?.clientHeight ?? n3?.clientHeight ?? 0;
    if (n3 && r3 > 0) {
      const o4 = e2._focusRow * t3;
      o4 < n3.scrollTop ? n3.scrollTop = o4 : o4 + t3 > n3.scrollTop + r3 && (n3.scrollTop = o4 - r3 + t3);
    }
  }
  const o2 = void 0 !== e2._activeEditRows && -1 !== e2._activeEditRows || !!e2._isGridEditMode;
  o2 || e2.refreshVirtualWindow(false), Ae(e2._bodyEl), Array.from(e2._bodyEl.querySelectorAll('[aria-selected="true"]')).forEach((e3) => {
    e3.setAttribute("aria-selected", "false");
  });
  const i2 = e2._focusRow, n2 = e2._virtualization.start ?? 0, r2 = e2._virtualization.end ?? e2._rows.length;
  if (i2 >= n2 && i2 < r2) {
    const r3 = e2._bodyEl.querySelectorAll(".data-grid-row")[i2 - n2];
    let s2 = r3?.children[e2._focusCol];
    if (s2 && s2.classList?.contains("cell") || (s2 = r3?.querySelector(`.cell[data-col="${e2._focusCol}"]`) ?? r3?.querySelector(".cell[data-col]")), s2) {
      s2.classList.add("cell-focus"), s2.setAttribute("aria-selected", "true");
      const i3 = e2.querySelector(".tbw-scroll-area");
      if (i3 && s2 && (!o2 || t2?.forceHorizontalScroll)) if (t2?.forceScrollLeft) i3.scrollLeft = 0;
      else if (t2?.forceScrollRight) i3.scrollLeft = i3.scrollWidth - i3.clientWidth;
      else {
        const t3 = e2._getHorizontalScrollOffsets?.(r3 ?? void 0, s2) ?? { left: 0, right: 0 };
        if (!t3.skipScroll) {
          const e3 = s2.getBoundingClientRect(), o3 = i3.getBoundingClientRect(), n3 = e3.left - o3.left + i3.scrollLeft, r4 = n3 + e3.width, l2 = i3.scrollLeft + t3.left, a2 = i3.scrollLeft + i3.clientWidth - t3.right;
          n3 < l2 ? i3.scrollLeft = n3 - t3.left : r4 > a2 && (i3.scrollLeft = r4 - i3.clientWidth + t3.right);
        }
      }
      if (o2 && s2.classList.contains(z.EDITING)) {
        const e3 = s2.querySelector(Oe);
        if (e3 && document.activeElement !== e3) try {
          e3.focus({ preventScroll: true });
        } catch {
        }
      } else if (o2 && !s2.contains(document.activeElement)) {
        s2.hasAttribute("tabindex") || s2.setAttribute("tabindex", "-1");
        try {
          s2.focus({ preventScroll: true });
        } catch {
        }
      } else o2 || document.activeElement !== e2 && e2.focus({ preventScroll: true });
    }
  }
}
Ne.innerHTML = '<div class="data-grid-row" role="row" part="row"></div>';
var Ye = /* @__PURE__ */ new WeakMap();
function Xe(e2, t2) {
  const o2 = Te(t2), i2 = (function(e3) {
    if (!e3) return -1;
    const t3 = e3.getAttribute("data-col");
    return t3 ? parseInt(t3, 10) : -1;
  })(t2);
  if (o2 < 0 || i2 < 0) return;
  e2._focusRow = o2, e2._focusCol = i2, Ae(e2._bodyEl), t2.classList.add("cell-focus"), t2.setAttribute("aria-selected", "true");
  const n2 = t2.closest("tbw-grid");
  n2 && document.activeElement !== n2 && n2.focus({ preventScroll: true });
}
function Ke(e2, t2, o2, i2) {
  let n2 = null;
  const r2 = o2.composedPath?.();
  if (n2 = r2 && r2.length > 0 ? r2[0] : o2.target, n2 && !t2.contains(n2)) {
    const e3 = document.elementFromPoint(o2.clientX, o2.clientY);
    e3 && (n2 = e3);
  }
  const s2 = n2?.closest?.("[data-col]"), l2 = n2?.closest?.(".data-grid-row"), a2 = n2?.closest?.(".header-row");
  let c2, d2, h2, u2, g2, f2;
  return s2 && (c2 = parseInt(s2.getAttribute("data-row") ?? "-1", 10), d2 = parseInt(s2.getAttribute("data-col") ?? "-1", 10), c2 >= 0 && d2 >= 0 && (h2 = e2._rows[c2], f2 = e2._visibleColumns[d2], u2 = f2?.field, g2 = h2 && u2 ? h2[u2] : void 0)), { type: i2, row: h2, rowIndex: void 0 !== c2 && c2 >= 0 ? c2 : void 0, colIndex: void 0 !== d2 && d2 >= 0 ? d2 : void 0, field: u2, value: g2, column: f2, originalEvent: o2, cellElement: s2 ?? void 0, rowElement: l2 ?? void 0, isHeader: !!a2, cell: void 0 !== c2 && void 0 !== d2 && c2 >= 0 && d2 >= 0 ? { row: c2, col: d2 } : void 0 };
}
function Ze(e2, t2, o2, i2) {
  t2.addEventListener("keydown", (t3) => (function(e3, t4) {
    if (e3._dispatchKeyDown?.(t4)) return;
    const o3 = e3._rows.length - 1, i3 = e3._visibleColumns.length - 1, n2 = void 0 !== e3._activeEditRows && -1 !== e3._activeEditRows, r2 = e3._visibleColumns[e3._focusCol], s2 = r2?.type, l2 = t4.composedPath?.() ?? [], a2 = l2.length ? l2[0] : t4.target;
    if ((!Ge(a2) || "Home" !== t4.key && "End" !== t4.key) && !(Ge(a2) && ("ArrowUp" === t4.key || "ArrowDown" === t4.key) && "INPUT" === a2.tagName && "number" === a2.type || Ge(a2) && ("ArrowLeft" === t4.key || "ArrowRight" === t4.key) || Ge(a2) && ("Enter" === t4.key || "Escape" === t4.key) || n2 && "select" === s2 && ("ArrowDown" === t4.key || "ArrowUp" === t4.key))) {
      switch (t4.key) {
        case "Tab":
          return t4.preventDefault(), t4.shiftKey ? e3._focusCol > 0 ? e3._focusCol -= 1 : e3._focusRow > 0 && (e3._activeEditRows === e3._focusRow && Ue(e3), e3._focusRow -= 1, e3._focusCol = i3) : e3._focusCol < i3 ? e3._focusCol += 1 : (Ue(e3), e3._focusRow < o3 && (e3._focusRow += 1, e3._focusCol = 0)), void je(e3);
        case "ArrowDown":
          n2 && Ue(e3), e3._focusRow = Math.min(o3, e3._focusRow + 1), t4.preventDefault();
          break;
        case "ArrowUp":
          n2 && Ue(e3), e3._focusRow = Math.max(0, e3._focusRow - 1), t4.preventDefault();
          break;
        case "ArrowRight": {
          const o4 = He(e3);
          e3._focusCol = o4 ? Math.max(0, e3._focusCol - 1) : Math.min(i3, e3._focusCol + 1), t4.preventDefault();
          break;
        }
        case "ArrowLeft": {
          const o4 = He(e3);
          e3._focusCol = o4 ? Math.min(i3, e3._focusCol + 1) : Math.max(0, e3._focusCol - 1), t4.preventDefault();
          break;
        }
        case "Home":
          return t4.ctrlKey || t4.metaKey ? (n2 && Ue(e3), e3._focusRow = 0, e3._focusCol = 0) : e3._focusCol = 0, t4.preventDefault(), void je(e3, { forceScrollLeft: true });
        case "End":
          return t4.ctrlKey || t4.metaKey ? (n2 && Ue(e3), e3._focusRow = o3, e3._focusCol = i3) : e3._focusCol = i3, t4.preventDefault(), void je(e3, { forceScrollRight: true });
        case "PageDown":
          e3._focusRow = Math.min(o3, e3._focusRow + 20), t4.preventDefault();
          break;
        case "PageUp":
          e3._focusRow = Math.max(0, e3._focusRow - 20), t4.preventDefault();
          break;
        case "Enter": {
          const o4 = e3._focusRow, i4 = e3._focusCol, n3 = e3._visibleColumns[i4], r3 = e3._rows[o4], s3 = n3?.field ?? "", l3 = s3 && r3 ? r3[s3] : void 0, a3 = e3.querySelector(`[data-row="${o4}"][data-col="${i4}"]`), c2 = new CustomEvent("cell-activate", { cancelable: true, detail: { rowIndex: o4, colIndex: i4, column: n3, field: s3, value: l3, row: r3, cellEl: a3, trigger: "keyboard", originalEvent: t4 } });
          if (e3.dispatchEvent(c2), c2.defaultPrevented) return void t4.preventDefault();
          break;
        }
        default:
          return;
      }
      je(e3);
    }
  })(e2, t3), { signal: i2 }), o2.addEventListener("mousedown", (t3) => (function(e3, t4, o3) {
    const i3 = Ke(e3, t4, o3, "mousedown");
    e3._dispatchCellMouseDown?.(i3) && Ye.set(e3, true);
  })(e2, o2, t3), { signal: i2 }), document.addEventListener("mousemove", (t3) => (function(e3, t4, o3) {
    if (!Ye.get(e3)) return;
    const i3 = Ke(e3, t4, o3, "mousemove");
    e3._dispatchCellMouseMove?.(i3);
  })(e2, o2, t3), { signal: i2 }), document.addEventListener("mouseup", (t3) => (function(e3, t4, o3) {
    if (!Ye.get(e3)) return;
    const i3 = Ke(e3, t4, o3, "mouseup");
    e3._dispatchCellMouseUp?.(i3), Ye.set(e3, false);
  })(e2, o2, t3), { signal: i2 });
}
var Qe;
function Je(e2) {
  Qe = e2;
}
var et = class {
  #o;
  #k = /* @__PURE__ */ new Set();
  #L = /* @__PURE__ */ new Map();
  constructor(e2) {
    this.#o = e2;
  }
  focusCell(e2, t2) {
    const o2 = this.#o, i2 = o2._rows.length - 1;
    if (i2 < 0) return;
    let n2;
    if ("string" == typeof t2) {
      if (n2 = o2._visibleColumns.findIndex((e3) => e3.field === t2), n2 < 0) return;
    } else n2 = t2;
    const r2 = o2._visibleColumns.length - 1;
    r2 < 0 || (o2._focusRow = Math.max(0, Math.min(e2, i2)), o2._focusCol = Math.max(0, Math.min(n2, r2)), je(o2));
  }
  get focusedCell() {
    const e2 = this.#o;
    if (0 === e2._rows.length || 0 === e2._visibleColumns.length) return null;
    const t2 = e2._visibleColumns[e2._focusCol];
    return { rowIndex: e2._focusRow, colIndex: e2._focusCol, field: t2?.field ?? "" };
  }
  scrollToRow(e2, t2) {
    const o2 = this.#o._virtualization;
    if (!o2.enabled) return;
    const i2 = o2.container;
    if (!i2) return;
    const n2 = this.#o._rows.length;
    if (0 === n2) return;
    const r2 = Math.max(0, Math.min(e2, n2 - 1)), s2 = t2?.align ?? "nearest", l2 = t2?.behavior ?? "instant";
    let a2, c2;
    const d2 = o2.positionCache;
    o2.variableHeights && d2 && d2.length > r2 ? (a2 = d2[r2].offset, c2 = d2[r2].height) : (a2 = r2 * o2.rowHeight, c2 = o2.rowHeight);
    const h2 = o2.viewportEl?.clientHeight ?? i2.clientHeight ?? 0;
    if (h2 <= 0) return;
    const u2 = i2.scrollTop, g2 = a2 + c2, f2 = u2 + h2;
    let p2;
    switch (s2) {
      case "start":
        p2 = a2;
        break;
      case "center":
        p2 = a2 - h2 / 2 + c2 / 2;
        break;
      case "end":
        p2 = g2 - h2;
        break;
      default:
        if (a2 >= u2 && g2 <= f2) return;
        p2 = a2 < u2 ? a2 : g2 - h2;
    }
    p2 = Math.max(0, p2), "smooth" === l2 ? i2.scrollTo({ top: p2, behavior: "smooth" }) : i2.scrollTop = p2;
  }
  scrollToRowById(e2, t2) {
    const o2 = this.#o._getRowEntry(e2);
    o2 && this.scrollToRow(o2.index, t2);
  }
  registerExternalFocusContainer(e2) {
    if (this.#k.has(e2)) return;
    this.#k.add(e2);
    const t2 = new AbortController(), o2 = t2.signal, i2 = this.#o;
    e2.addEventListener("focusin", () => {
      i2.dataset.hasFocus = "";
    }, { signal: o2 }), e2.addEventListener("focusout", (e3) => {
      const t3 = e3.relatedTarget;
      t3 && this.containsFocus(t3) || delete i2.dataset.hasFocus;
    }, { signal: o2 }), this.#L.set(e2, () => t2.abort());
  }
  unregisterExternalFocusContainer(e2) {
    this.#k.delete(e2);
    const t2 = this.#L.get(e2);
    t2 && (t2(), this.#L.delete(e2));
  }
  containsFocus(e2) {
    const t2 = e2 ?? document.activeElement;
    return !!t2 && (!!this.#o.contains(t2) || this.isInExternalFocusContainer(t2));
  }
  isInExternalFocusContainer(e2) {
    for (const t2 of this.#k) if (t2.contains(e2)) return true;
    return false;
  }
  destroy() {
    for (const e2 of this.#L.values()) e2();
    this.#L.clear(), this.#k.clear();
  }
};
var tt = "function" == typeof requestIdleCallback;
function ot(e2) {
  tt ? cancelIdleCallback(e2) : clearTimeout(e2);
}
function it(e2, t2) {
  if (t2) {
    const o2 = t2({ size: e2 });
    if ("string" == typeof o2) {
      const e3 = document.createElement("div");
      return e3.innerHTML = o2, e3;
    }
    return o2;
  }
  return (function(e3) {
    const t3 = document.createElement("div");
    return t3.className = `tbw-spinner tbw-spinner--${e3}`, t3.setAttribute("role", "progressbar"), t3.setAttribute("aria-label", "Loading"), t3;
  })(e2);
}
function nt(e2, t2) {
  if (t2) {
    if (e2.classList.add("tbw-row-loading"), e2.setAttribute("aria-busy", "true"), !e2.querySelector(".tbw-row-loading-overlay")) {
      const t3 = document.createElement("div");
      t3.className = "tbw-row-loading-overlay", t3.setAttribute("aria-hidden", "true");
      const o2 = document.createElement("div");
      o2.className = "tbw-row-loading-spinner", t3.appendChild(o2), e2.appendChild(t3);
    }
  } else e2.classList.remove("tbw-row-loading"), e2.removeAttribute("aria-busy"), e2.querySelector(".tbw-row-loading-overlay")?.remove();
}
function rt(e2) {
  let t2 = null, o2 = null, i2 = null, n2 = null;
  const r2 = (i3) => {
    if (!t2) return;
    const n3 = i3.clientX - t2.startX, r3 = Math.max(40, t2.startWidth + n3), s3 = e2._visibleColumns[t2.colIndex];
    s3.width = r3, s3.__userResized = true, s3.__renderedWidth = r3, null == o2 && (o2 = requestAnimationFrame(() => {
      o2 = null, e2.updateTemplate?.();
    })), e2.dispatchEvent(new CustomEvent("column-resize", { detail: { field: s3.field, width: r3 } }));
  };
  let s2 = false;
  const l2 = () => {
    const o3 = null !== t2;
    o3 && (s2 = true, requestAnimationFrame(() => {
      s2 = false;
    })), window.removeEventListener("mousemove", r2), window.removeEventListener("mouseup", l2), null !== i2 && (document.documentElement.style.cursor = i2, i2 = null), null !== n2 && (document.body.style.userSelect = n2, n2 = null), t2 = null, o3 && e2.requestStateChange && e2.requestStateChange();
  };
  return { get isResizing() {
    return null !== t2 || s2;
  }, start(o3, s3, a2) {
    o3.preventDefault();
    const c2 = e2._headerRowEl ?? e2.findHeaderRow?.();
    c2 && (function(t3, o4) {
      const i3 = o4.querySelectorAll(".cell");
      for (let n3 = 0; n3 < e2._visibleColumns.length; n3++) {
        if (n3 === t3) continue;
        const o5 = e2._visibleColumns[n3];
        if (null == o5.width && !o5.__userResized) {
          const e3 = i3[n3], t4 = e3?.getBoundingClientRect().width;
          t4 && (o5.width = Math.round(t4), o5.__userResized = true, o5.__renderedWidth = o5.width);
        }
      }
    })(s3, c2);
    const d2 = e2._visibleColumns[s3], h2 = "number" == typeof d2?.width ? d2.width : void 0, u2 = d2?.__renderedWidth ?? h2 ?? a2.getBoundingClientRect().width;
    t2 = { startX: o3.clientX, colIndex: s3, startWidth: u2 }, window.addEventListener("mousemove", r2), window.addEventListener("mouseup", l2), null === i2 && (i2 = document.documentElement.style.cursor), document.documentElement.style.cursor = "e-resize", null === n2 && (n2 = document.body.style.userSelect), document.body.style.userSelect = "none";
  }, resetColumn(t3) {
    const o3 = e2._visibleColumns[t3];
    o3 && (o3.__userResized = false, o3.__renderedWidth = void 0, o3.width = o3.__originalWidth, e2.updateTemplate?.(), e2.requestStateChange?.(), e2.dispatchEvent(new CustomEvent("column-resize-reset", { detail: { field: o3.field, width: o3.width } })));
  }, dispose() {
    l2();
  } };
}
var st = "data-animating";
var lt = { change: "--tbw-row-change-duration", insert: "--tbw-row-insert-duration", remove: "--tbw-row-remove-duration" };
var at = { change: 500, insert: 300, remove: 200 };
function ct(e2, t2) {
  const o2 = lt[t2], i2 = getComputedStyle(e2).getPropertyValue(o2);
  if (i2) {
    const e3 = (function(e4) {
      const t3 = e4.trim().toLowerCase();
      return t3.endsWith("ms") ? parseFloat(t3) : t3.endsWith("s") ? 1e3 * parseFloat(t3) : parseFloat(t3);
    })(i2);
    if (!isNaN(e3) && e3 > 0) return e3;
  }
  return at[t2];
}
function dt(e2, t2, o2) {
  if (t2 < 0) return Promise.resolve(false);
  const i2 = e2.findRenderedRowElement?.(t2);
  return i2 ? new Promise((e3) => {
    !(function(e4, t3, o3) {
      e4.removeAttribute(st), e4.offsetWidth, e4.setAttribute(st, t3);
      const i3 = ct(e4, t3);
      setTimeout(() => {
        "remove" !== t3 && e4.removeAttribute(st), o3?.();
      }, i3);
    })(i2, o2, () => e3(true));
  }) : Promise.resolve(false);
}
function ht(e2, t2) {
  if (t2) return t2(e2);
  const o2 = e2;
  return "id" in o2 && null != o2.id ? String(o2.id) : "_id" in o2 && null != o2._id ? String(o2._id) : void 0;
}
function ut(e2, t2, o2) {
  const i2 = ht(e2, o2);
  return void 0 === i2 && T("TBW040", 'Cannot determine row ID. Configure getRowId in gridConfig or ensure rows have an "id" property.', t2), i2;
}
var gt = class {
  #o;
  constructor(e2) {
    this.#o = e2;
  }
  resolveRowId(e2) {
    return ut(e2, this.#o.id, this.#o.effectiveConfig?.getRowId);
  }
  getRow(e2) {
    return this.#o._getRowEntry(e2)?.row;
  }
  getRowEntry(e2) {
    return this.#o._getRowEntry(e2);
  }
  updateRow(e2, t2, o2 = "api") {
    const i2 = this.#o, n2 = i2._getRowEntry(e2);
    n2 || T(a, `Row with ID "${e2}" not found. Ensure the row exists and getRowId is correctly configured.`, i2.id);
    const { row: r2, index: s2 } = n2, l2 = [];
    for (const [a2, c2] of Object.entries(t2)) {
      const e3 = r2[a2];
      e3 !== c2 && (l2.push({ field: a2, oldValue: e3, newValue: c2 }), r2[a2] = c2);
    }
    for (const { field: a2, oldValue: c2, newValue: d2 } of l2) i2.dispatchEvent(new CustomEvent("cell-change", { detail: { row: r2, rowId: e2, rowIndex: s2, field: a2, oldValue: c2, newValue: d2, changes: t2, source: o2 }, bubbles: true, composed: true }));
    l2.length > 0 && (We(i2), i2._requestSchedulerPhase(_e.VIRTUALIZATION, "updateRow"), i2._emitDataChange());
  }
  updateRows(e2, t2 = "api") {
    const o2 = this.#o;
    let i2 = false;
    for (const { id: n2, changes: r2 } of e2) {
      const e3 = o2._getRowEntry(n2);
      e3 || T(a, `Row with ID "${n2}" not found. Ensure the row exists and getRowId is correctly configured.`, o2.id);
      const { row: s2, index: l2 } = e3;
      for (const [a2, c2] of Object.entries(r2)) {
        const e4 = s2[a2];
        e4 !== c2 && (i2 = true, s2[a2] = c2, o2.dispatchEvent(new CustomEvent("cell-change", { detail: { row: s2, rowId: n2, rowIndex: l2, field: a2, oldValue: e4, newValue: c2, changes: r2, source: t2 }, bubbles: true, composed: true })));
      }
    }
    i2 && (We(o2), o2._requestSchedulerPhase(_e.VIRTUALIZATION, "updateRows"), o2._emitDataChange());
  }
  async insertRow(e2, t2, o2 = true) {
    const i2 = this.#o, n2 = Math.max(0, Math.min(e2, i2._rows.length));
    i2.sourceRows = [...i2.sourceRows, t2];
    const r2 = [...i2._rows];
    r2.splice(n2, 0, t2), i2._rows = r2, i2._sortState && (i2.__originalOrder = [...i2.__originalOrder, t2]), We(i2), i2._rebuildRowIdMap(), i2.__rowRenderEpoch++;
    for (let s2 = 0; s2 < i2._rowPool.length; s2++) i2._rowPool[s2].__epoch = -1;
    i2.refreshVirtualWindow(true), i2._emitPluginEvent("row-inserted", { row: t2, index: n2 }), i2._emitDataChange(), o2 && (await new Promise((e3) => requestAnimationFrame(() => e3())), await dt(i2, n2, "insert"));
  }
  async removeRow(e2, t2 = true) {
    const o2 = this.#o, i2 = o2._rows[e2];
    if (!i2) return;
    t2 && await dt(o2, e2, "remove");
    const n2 = o2._rows.indexOf(i2);
    if (n2 < 0) return i2;
    const r2 = [...o2._rows];
    r2.splice(n2, 1), o2._rows = r2;
    const s2 = o2.sourceRows.indexOf(i2);
    if (s2 >= 0) {
      const e3 = [...o2.sourceRows];
      e3.splice(s2, 1), o2.sourceRows = e3;
    }
    if (o2._sortState) {
      const e3 = o2.__originalOrder.indexOf(i2);
      if (e3 >= 0) {
        const t3 = [...o2.__originalOrder];
        t3.splice(e3, 1), o2.__originalOrder = t3;
      }
    }
    We(o2), o2._rebuildRowIdMap(), o2.__rowRenderEpoch++;
    for (let l2 = 0; l2 < o2._rowPool.length; l2++) o2._rowPool[l2].__epoch = -1;
    return o2.refreshVirtualWindow(true), o2._emitDataChange(), t2 && requestAnimationFrame(() => {
      const e3 = o2.querySelectorAll('[data-animating="remove"]');
      for (let t3 = 0; t3 < e3.length; t3++) e3[t3].removeAttribute("data-animating");
    }), i2;
  }
  async applyTransaction(e2, t2 = true) {
    const o2 = this.#o, i2 = { added: [], updated: [], removed: [] };
    if (e2.remove?.length) for (const { id: l2 } of e2.remove) {
      const e3 = o2._getRowEntry(l2);
      if (!e3) continue;
      const { row: n3 } = e3;
      if (t2) {
        const e4 = o2._rows.indexOf(n3);
        e4 >= 0 && await dt(o2, e4, "remove");
      }
      const r3 = o2._rows.indexOf(n3);
      if (r3 < 0) {
        i2.removed.push(n3);
        continue;
      }
      const s3 = [...o2._rows];
      s3.splice(r3, 1), o2._rows = s3;
      const a2 = o2.sourceRows.indexOf(n3);
      if (a2 >= 0) {
        const e4 = [...o2.sourceRows];
        e4.splice(a2, 1), o2.sourceRows = e4;
      }
      if (o2._sortState) {
        const e4 = o2.__originalOrder.indexOf(n3);
        if (e4 >= 0) {
          const t3 = [...o2.__originalOrder];
          t3.splice(e4, 1), o2.__originalOrder = t3;
        }
      }
      i2.removed.push(n3);
    }
    const n2 = new Set(e2.remove?.map((e3) => e3.id));
    if (e2.update?.length) for (const { id: l2, changes: a2 } of e2.update) {
      if (n2.has(l2)) continue;
      const e3 = o2._getRowEntry(l2);
      if (!e3) continue;
      const { row: t3, index: r3 } = e3;
      let s3 = false;
      for (const [i3, n3] of Object.entries(a2)) {
        const e4 = t3[i3];
        e4 !== n3 && (s3 = true, t3[i3] = n3, o2.dispatchEvent(new CustomEvent("cell-change", { detail: { row: t3, rowId: l2, rowIndex: r3, field: i3, oldValue: e4, newValue: n3, changes: a2, source: "api" }, bubbles: true, composed: true })));
      }
      s3 && i2.updated.push(t3);
    }
    if (e2.add?.length) for (const l2 of e2.add) {
      o2.sourceRows = [...o2.sourceRows, l2];
      const e3 = [...o2._rows];
      e3.push(l2), o2._rows = e3, o2._sortState && (o2.__originalOrder = [...o2.__originalOrder, l2]), i2.added.push(l2);
    }
    const r2 = i2.added.length > 0 || i2.removed.length > 0, s2 = i2.updated.length > 0;
    if (r2) {
      We(o2), o2._rebuildRowIdMap(), o2.__rowRenderEpoch++;
      for (let e3 = 0; e3 < o2._rowPool.length; e3++) o2._rowPool[e3].__epoch = -1;
      o2.refreshVirtualWindow(true);
    } else s2 && (We(o2), o2._requestSchedulerPhase(_e.VIRTUALIZATION, "applyTransaction"));
    if ((r2 || s2) && o2._emitDataChange(), t2 && i2.added.length > 0) {
      await new Promise((e3) => requestAnimationFrame(() => e3()));
      for (const e3 of i2.added) {
        const t3 = o2._rows.indexOf(e3);
        t3 >= 0 && await dt(o2, t3, "insert");
      }
    }
    if (t2 && i2.updated.length > 0) for (const l2 of i2.updated) {
      const e3 = o2._rows.indexOf(l2);
      e3 >= 0 && await dt(o2, e3, "change");
    }
    return t2 && i2.removed.length > 0 && requestAnimationFrame(() => {
      const e3 = o2.querySelectorAll('[data-animating="remove"]');
      for (let t3 = 0; t3 < e3.length; t3++) e3[t3].removeAttribute("data-animating");
    }), i2;
  }
  #O = null;
  #D = [];
  #I = null;
  applyTransactionAsync(e2) {
    return this.#O || (this.#O = { add: [], update: [], remove: [] }), e2.add && this.#O.add.push(...e2.add), e2.update && this.#O.update.push(...e2.update), e2.remove && this.#O.remove.push(...e2.remove), new Promise((e3) => {
      this.#D.push(e3), null === this.#I && (this.#I = requestAnimationFrame(() => {
        this.#I = null;
        const e4 = this.#O, t2 = this.#D;
        this.#O = null, this.#D = [], this.applyTransaction(e4, false).then((e5) => {
          for (const o2 of t2) o2(e5);
        });
      }));
    });
  }
};
function ft(e2, t2, o2) {
  const i2 = document.createElement(e2);
  if (t2) for (const n2 in t2) {
    const e3 = t2[n2];
    null != e3 && i2.setAttribute(n2, e3);
  }
  return i2;
}
function pt(e2, t2) {
  const o2 = document.createElement("div");
  if (e2 && (o2.className = e2), t2) for (const i2 in t2) {
    const e3 = t2[i2];
    null != e3 && o2.setAttribute(i2, e3);
  }
  return o2;
}
function wt(e2, t2, o2) {
  const i2 = document.createElement("button");
  if (e2 && (i2.className = e2), t2) for (const n2 in t2) {
    const e3 = t2[n2];
    null != e3 && i2.setAttribute(n2, e3);
  }
  return i2;
}
var mt = document.createElement("template");
function bt() {
  return mt.content.cloneNode(true);
}
function vt(e2) {
  const t2 = document.createDocumentFragment(), o2 = pt(e2.hasShell ? `${z.ROOT} has-shell` : z.ROOT);
  if (e2.hasShell && e2.shellHeader && e2.shellBody) o2.appendChild(e2.shellHeader), o2.appendChild(e2.shellBody);
  else {
    const e3 = pt("tbw-grid-content");
    e3.appendChild(bt()), o2.appendChild(e3);
  }
  return t2.appendChild(o2), t2;
}
function Ct(e2) {
  const t2 = pt("tbw-shell-header", { part: "shell-header", role: "presentation" });
  if (e2.title) {
    const o3 = pt("tbw-shell-title");
    o3.textContent = e2.title, t2.appendChild(o3);
  }
  const o2 = pt("tbw-shell-content", { part: "shell-content", role: "presentation", "data-light-dom-header-content": "" });
  t2.appendChild(o2);
  const i2 = pt("tbw-shell-toolbar", { part: "shell-toolbar", role: "presentation" });
  for (const n2 of e2.configButtons) n2.hasRender && i2.appendChild(pt("tbw-toolbar-content-slot", { "data-toolbar-content": n2.id }));
  for (const n2 of e2.apiButtons) n2.hasRender && i2.appendChild(pt("tbw-toolbar-content-slot", { "data-toolbar-content": n2.id }));
  if ((e2.configButtons.some((e3) => e3.hasRender) || e2.apiButtons.some((e3) => e3.hasRender)) && e2.hasPanels && i2.appendChild(pt("tbw-toolbar-separator")), e2.hasPanels) {
    const t3 = wt(e2.isPanelOpen ? "tbw-toolbar-btn active" : "tbw-toolbar-btn", { "data-panel-toggle": "", title: "Settings", "aria-label": "Toggle settings panel", "aria-pressed": String(e2.isPanelOpen), "aria-controls": "tbw-tool-panel" });
    t3.dataset.icon = "tool-panel", void 0 !== e2.toolPanelIcon && (t3.innerHTML = K(e2.toolPanelIcon)), i2.appendChild(t3);
  }
  return t2.appendChild(i2), t2;
}
function _t(e2) {
  const t2 = pt("tbw-shell-body"), o2 = e2.panels.length > 0, i2 = 1 === e2.panels.length, n2 = pt("tbw-grid-content");
  n2.appendChild(bt());
  let r2 = null;
  if (o2) {
    r2 = ft("aside", { class: e2.isPanelOpen ? "tbw-tool-panel open" : "tbw-tool-panel", part: "tool-panel", "data-position": e2.position, role: "presentation", id: "tbw-tool-panel" });
    const t3 = "left" === e2.position ? "right" : "left";
    r2.appendChild(pt("tbw-tool-panel-resize", { "data-resize-handle": "", "data-handle-position": t3, "aria-hidden": "true" }));
    const o3 = pt("tbw-tool-panel-content", { role: "presentation" }), n3 = pt("tbw-accordion");
    for (const r3 of e2.panels) {
      const t4 = pt(`tbw-accordion-section${r3.isExpanded ? " expanded" : ""}${i2 ? " single" : ""}`, { "data-section": r3.id }), o4 = wt("tbw-accordion-header", { "aria-expanded": String(r3.isExpanded), "aria-controls": `tbw-section-${r3.id}` });
      if (i2 && o4.setAttribute("aria-disabled", "true"), r3.icon) {
        const e3 = ft("span", { class: "tbw-accordion-icon" });
        e3.innerHTML = K(r3.icon), o4.appendChild(e3);
      }
      const s2 = ft("span", { class: "tbw-accordion-title" });
      if (s2.textContent = r3.title, o4.appendChild(s2), !i2) {
        const t5 = ft("span", { class: "tbw-accordion-chevron", "data-icon": "expand" });
        void 0 !== e2.expandIcon && (t5.innerHTML = K(e2.expandIcon)), o4.appendChild(t5);
      }
      t4.appendChild(o4), t4.appendChild(pt("tbw-accordion-content", { id: `tbw-section-${r3.id}`, role: "presentation" })), n3.appendChild(t4);
    }
    o3.appendChild(n3), r2.appendChild(o3);
  }
  return "left" === e2.position && r2 ? (t2.appendChild(r2), t2.appendChild(n2)) : (t2.appendChild(n2), r2 && t2.appendChild(r2)), t2;
}
function yt(e2) {
  return e2 ? "string" == typeof e2 ? K(e2) : e2.outerHTML : "";
}
function Rt(e2) {
  return !!e2?.header?.title || (!!e2?.header?.toolbarContents?.length || (!!e2?.toolPanels?.length || (!!e2?.headerContents?.length || (!!e2?.header?.lightDomContent?.length || !!e2?.header?.hasToolButtonsContainer))));
}
function St(e2, t2) {
  const o2 = e2.querySelector("tbw-grid-header");
  if (!o2) return;
  if (!t2.lightDomTitle) {
    const e3 = o2.getAttribute("title");
    e3 && (t2.lightDomTitle = e3);
  }
  const i2 = o2.querySelectorAll("tbw-grid-header-content");
  i2.length > 0 && 0 === t2.lightDomHeaderContent.length && (t2.lightDomHeaderContent = Array.from(i2)), o2.style.display = "none";
}
function Et(e2, t2, o2) {
  const i2 = e2.querySelector(":scope > tbw-grid-tool-buttons");
  if (!i2) return;
  t2.hasToolButtonsContainer = true;
  const n2 = "light-dom-toolbar-content";
  if (t2.lightDomToolbarContentIds.has(n2)) return;
  const r2 = { id: n2, order: 0, render: (e3) => {
    for (; i2.firstChild; ) e3.appendChild(i2.firstChild);
    return () => {
      for (; e3.firstChild; ) i2.appendChild(e3.firstChild);
    };
  } };
  t2.toolbarContents.set(n2, r2), t2.lightDomToolbarContentIds.add(n2), i2.style.display = "none";
}
function xt(e2, t2, o2) {
  e2.querySelectorAll(":scope > tbw-grid-tool-panel").forEach((e3) => {
    const i2 = e3, n2 = i2.getAttribute("id"), r2 = i2.getAttribute("title");
    if (!n2 || !r2) return void A("TBW070", `Tool panel missing required id or title attribute: id="${n2 ?? ""}", title="${r2 ?? ""}"`);
    const s2 = i2.getAttribute("icon") ?? void 0, l2 = i2.getAttribute("tooltip") ?? void 0, a2 = parseInt(i2.getAttribute("order") ?? "100", 10);
    let c2;
    const d2 = o2?.(i2);
    if (d2) c2 = d2;
    else {
      const e4 = K(i2.innerHTML.trim());
      c2 = (t3) => {
        const o3 = document.createElement("div");
        return o3.innerHTML = e4, t3.appendChild(o3), () => o3.remove();
      };
    }
    const h2 = t2.toolPanels.get(n2);
    if (h2) {
      if (d2) {
        h2.render = c2, h2.order = a2, h2.icon = s2, h2.tooltip = l2;
        const e4 = t2.panelCleanups.get(n2);
        e4 && (e4(), t2.panelCleanups.delete(n2));
      }
      return;
    }
    const u2 = { id: n2, title: r2, icon: s2, tooltip: l2, order: a2, render: c2 };
    t2.toolPanels.set(n2, u2), t2.lightDomToolPanelIds.add(n2), i2.style.display = "none";
  });
}
function Tt(e2, t2, o2) {
  const i2 = t2?.header?.toolbarContents ?? [], n2 = [...o2.toolbarContents.values()], r2 = new Set(i2.map((e3) => e3.id)), s2 = [...i2];
  for (const l2 of n2) r2.has(l2.id) || s2.push(l2);
  for (const l2 of s2) {
    if (o2.toolbarContentCleanups.has(l2.id)) continue;
    if (!l2.render) continue;
    const t3 = e2.querySelector(`[data-toolbar-content="${l2.id}"]`);
    if (!t3) continue;
    const i3 = l2.render(t3);
    i3 && o2.toolbarContentCleanups.set(l2.id, i3);
  }
}
function At(e2, t2) {
  const o2 = t2.lightDomHeaderContent.length > 0 && !t2.lightDomContentMoved, i2 = t2.headerContents.size > 0;
  if (!o2 && !i2) return;
  const n2 = e2.querySelector(".tbw-shell-content");
  if (!n2) return;
  if (o2) {
    for (const e3 of t2.lightDomHeaderContent) e3.style.display = "", n2.appendChild(e3);
    t2.lightDomContentMoved = true;
  }
  const r2 = [...t2.headerContents.values()].sort((e3, t3) => (e3.order ?? 100) - (t3.order ?? 100));
  for (const s2 of r2) {
    const e3 = t2.headerContentCleanups.get(s2.id);
    e3 && (e3(), t2.headerContentCleanups.delete(s2.id));
    let o3 = n2.querySelector(`[data-header-content="${s2.id}"]`);
    o3 || (o3 = document.createElement("div"), o3.setAttribute("data-header-content", s2.id), n2.appendChild(o3));
    const i3 = s2.render(o3);
    i3 && t2.headerContentCleanups.set(s2.id, i3);
  }
}
function Pt(e2, t2, o2) {
  if (t2.isPanelOpen) for (const [i2, n2] of t2.toolPanels) {
    const o3 = t2.expandedSections.has(i2), r2 = e2.querySelector(`[data-section="${i2}"]`), s2 = r2?.querySelector(".tbw-accordion-content");
    if (!r2 || !s2) continue;
    r2.classList.toggle("expanded", o3);
    const l2 = r2.querySelector(".tbw-accordion-header");
    if (l2 && l2.setAttribute("aria-expanded", String(o3)), o3) {
      if (0 === s2.children.length) {
        const e3 = n2.render(s2);
        e3 && t2.panelCleanups.set(i2, e3);
      }
    } else {
      const e3 = t2.panelCleanups.get(i2);
      e3 && (e3(), t2.panelCleanups.delete(i2)), s2.innerHTML = "";
    }
  }
}
function Ht(e2, t2) {
  const o2 = e2.querySelector("[data-panel-toggle]");
  o2 && (o2.classList.toggle("active", t2.isPanelOpen), o2.setAttribute("aria-pressed", String(t2.isPanelOpen)));
}
function Mt(e2, t2) {
  const o2 = e2.querySelector(".tbw-tool-panel");
  o2 && (o2.classList.toggle("open", t2.isPanelOpen), t2.isPanelOpen || (o2.style.width = ""));
}
function kt(e2) {
  for (const t2 of e2.toolbarContentCleanups.values()) t2();
  e2.toolbarContentCleanups.clear();
  for (const t2 of e2.panelCleanups.values()) t2();
  e2.panelCleanups.clear();
  for (const t2 of e2.headerContentCleanups.values()) t2();
  e2.headerContentCleanups.clear(), e2.lightDomContentMoved = false;
}
function Lt(e2, t2, o2) {
  const i2 = void 0 !== o2?.toolPanel ? yt(o2.toolPanel) : void 0, n2 = void 0 !== o2?.expand ? yt(o2.expand) : void 0, r2 = void 0 !== o2?.collapse ? yt(o2.collapse) : void 0, s2 = [...e2.header?.toolbarContents ?? []].sort((e3, t3) => (e3.order ?? 0) - (t3.order ?? 0)), l2 = [...e2.toolPanels ?? []].sort((e3, t3) => (e3.order ?? 100) - (t3.order ?? 100));
  return { headerOptions: { title: e2.header?.title ?? void 0, hasPanels: l2.length > 0, isPanelOpen: t2.isPanelOpen, toolPanelIcon: i2, configButtons: s2.map((e3) => ({ id: e3.id, hasElement: false, hasRender: !!e3.render })), apiButtons: [] }, bodyOptions: { position: e2.toolPanel?.position ?? "right", isPanelOpen: t2.isPanelOpen, expandIcon: n2, collapseIcon: r2, panels: l2.map((e3) => ({ id: e3.id, title: e3.title, icon: yt(e3.icon), isExpanded: t2.expandedSections.has(e3.id) })) } };
}
function Ot(e2, t2, o2, i2) {
  const n2 = Rt(t2), r2 = [], s2 = ["tbw-grid-header", "tbw-grid-tool-buttons", "tbw-grid-tool-panel", "tbw-grid-column", "tbw-grid-detail", "tbw-grid-responsive-card"];
  for (const l2 of s2) {
    e2.querySelectorAll(`:scope > ${l2}`).forEach((e3) => r2.push(e3));
  }
  e2.replaceChildren();
  for (const l2 of r2) e2.appendChild(l2);
  if (n2) {
    const { headerOptions: n3, bodyOptions: r3 } = Lt(t2, o2, i2), s3 = vt({ hasShell: true, shellHeader: Ct(n3), shellBody: _t(r3) });
    e2.appendChild(s3);
  } else {
    const t3 = vt({ hasShell: false });
    e2.appendChild(t3);
  }
  return n2;
}
function Dt(e2, t2, o2) {
  const i2 = e2.querySelector(`[data-section="${t2}"]`);
  i2 && i2.classList.toggle("expanded", o2);
}
mt.innerHTML = '\n  <div class="tbw-scroll-area">\n    <div class="rows-body-wrapper">\n      <div class="rows-body" role="grid">\n        <div class="header" role="rowgroup">\n          <div class="header-row" role="row" part="header-row"></div>\n        </div>\n        <div class="rows-container" role="presentation">\n          <div class="rows-viewport" role="presentation">\n            <div class="rows"></div>\n          </div>\n        </div>\n      </div>\n    </div>\n  </div>\n  <div class="faux-vscroll">\n    <div class="faux-vscroll-spacer"></div>\n  </div>\n  <div class="tbw-sr-only" aria-live="polite" aria-atomic="true"></div>\n';
var It = "tbw-grid-styles";
var zt = "";
var Nt = /* @__PURE__ */ new Map();
function qt() {
  const e2 = (function() {
    let e3 = document.getElementById(It);
    return e3 || (e3 = document.createElement("style"), e3.id = It, e3.setAttribute("data-tbw-grid", "true"), document.head.appendChild(e3)), e3;
  })(), t2 = Array.from(Nt.values()).join("\n");
  e2.textContent = `${zt}

/* Plugin Styles */
${t2}`;
}
async function $t(e2) {
  if (zt) return;
  if ("string" == typeof e2 && e2.length > 0) return zt = e2, void qt();
  await new Promise((e3) => setTimeout(e3, 50));
  const t2 = (function() {
    try {
      for (const e3 of Array.from(document.styleSheets)) try {
        const t3 = Array.from(e3.cssRules || []).map((e4) => e4.cssText).join("\n");
        if (t3.includes(".tbw-grid-root") && t3.includes("tbw-grid")) return t3;
      } catch {
        continue;
      }
    } catch (e3) {
      A("TBW120", `Failed to extract grid.css from document stylesheets: ${e3}`);
    }
    return null;
  })();
  t2 ? (zt = t2, qt()) : "undefined" != typeof process && false || A("TBW121", `Could not find grid.css in document.styleSheets. Grid styling will not work. Available stylesheets: ${Array.from(document.styleSheets).map((e3) => e3.href || "(inline)").join(", ")}`);
}
function Wt(e2) {
  e2.startY = null, e2.startX = null, e2.lastY = null, e2.lastX = null, e2.lastTime = null, e2.locked = false;
}
function Ft(e2) {
  e2.momentumRaf && (cancelAnimationFrame(e2.momentumRaf), e2.momentumRaf = 0);
}
function Bt(e2, t2) {
  if (null !== e2.lastTime) {
    performance.now() - e2.lastTime > 60 && (e2.velocityY = 0, e2.velocityX = 0);
  }
  (Math.abs(e2.velocityY) > 0.1 || Math.abs(e2.velocityX) > 0.1) && (function(e3, t3) {
    const o2 = 0.95, i2 = 1;
    let n2 = performance.now();
    const r2 = () => {
      const s2 = performance.now(), l2 = s2 - n2;
      n2 = s2;
      const a2 = l2 / 16, c2 = Math.pow(o2, a2);
      e3.velocityY *= c2, e3.velocityX *= c2;
      const d2 = 16 * e3.velocityY, h2 = 16 * e3.velocityX, u2 = Math.abs(d2) >= i2, g2 = Math.abs(h2) >= i2;
      u2 && (t3.fauxScrollbar.scrollTop += d2), g2 && t3.scrollArea && (t3.scrollArea.scrollLeft += h2), e3.momentumRaf = u2 || g2 ? requestAnimationFrame(r2) : 0;
    };
    e3.momentumRaf = requestAnimationFrame(r2);
  })(e2, t2), Wt(e2);
}
function Vt(e2, t2, o2, i2) {
  e2.addEventListener("pointerdown", (o3) => {
    "touch" === o3.pointerType && null === t2.activePointerId && (t2.activePointerId = o3.pointerId, e2.setPointerCapture(o3.pointerId), (function(e3, t3, o4) {
      Ft(o4), o4.startY = t3, o4.startX = e3, o4.lastY = t3, o4.lastX = e3, o4.lastTime = performance.now(), o4.velocityY = 0, o4.velocityX = 0, o4.locked = false;
    })(o3.clientX, o3.clientY, t2));
  }, { passive: true, signal: i2 }), e2.addEventListener("pointermove", (e3) => {
    if (e3.pointerId !== t2.activePointerId) return;
    const i3 = (function(e4, t3, o3, i4) {
      if (null === o3.lastY || null === o3.lastX) return false;
      const n2 = performance.now(), r2 = o3.lastY - t3, s2 = o3.lastX - e4;
      if (null !== o3.lastTime) {
        const e5 = n2 - o3.lastTime;
        e5 > 0 && (o3.velocityY = r2 / e5, o3.velocityX = s2 / e5);
      }
      if (o3.lastY = t3, o3.lastX = e4, o3.lastTime = n2, o3.locked) {
        const { scrollTop: e5, scrollHeight: t4, clientHeight: o4 } = i4.fauxScrollbar;
        if (r2 > 0 && e5 < t4 - o4 || r2 < 0 && e5 > 0 ? i4.fauxScrollbar.scrollTop += r2 : 0 !== r2 && window.scrollBy(0, r2), i4.scrollArea) {
          const { scrollLeft: e6, scrollWidth: t5, clientWidth: o5 } = i4.scrollArea;
          s2 > 0 && e6 < t5 - o5 || s2 < 0 && e6 > 0 ? i4.scrollArea.scrollLeft += s2 : 0 !== s2 && window.scrollBy(s2, 0);
        }
        return true;
      }
      const l2 = null !== o3.startY ? Math.abs(o3.startY - t3) : 0, a2 = null !== o3.startX ? Math.abs(o3.startX - e4) : 0;
      if (l2 < 3 && a2 < 3) return false;
      const c2 = l2 >= a2, { scrollHeight: d2, clientHeight: h2 } = i4.fauxScrollbar, u2 = d2 - h2 > 0;
      let g2 = false;
      if (i4.scrollArea) {
        const { scrollWidth: e5, clientWidth: t4 } = i4.scrollArea;
        g2 = e5 - t4 > 0;
      }
      return !!(c2 && u2 || !c2 && g2) && (o3.locked = true, i4.fauxScrollbar.scrollTop += r2, i4.scrollArea && (i4.scrollArea.scrollLeft += s2), true);
    })(e3.clientX, e3.clientY, t2, o2);
    i3 && e3.preventDefault();
  }, { passive: false, signal: i2 }), e2.addEventListener("pointerup", (e3) => {
    e3.pointerId === t2.activePointerId && (t2.activePointerId = null, Bt(t2, o2));
  }, { passive: true, signal: i2 }), e2.addEventListener("pointercancel", (e3) => {
    e3.pointerId === t2.activePointerId && (t2.activePointerId = null, Wt(t2));
  }, { passive: true, signal: i2 }), e2.addEventListener("lostpointercapture", (e3) => {
    e3.pointerId === t2.activePointerId && (t2.activePointerId = null, Wt(t2));
  }, { passive: true, signal: i2 });
}
var Ut = [{ property: "editable", pluginName: "editing", level: "column", description: "", isUsed: (e2) => true === e2 || "function" == typeof e2 }, { property: "editor", pluginName: "editing", level: "column", description: "" }, { property: "editorParams", pluginName: "editing", level: "column", description: "" }, { property: "group", pluginName: "groupingColumns", level: "column", description: "" }, { property: "pinned", pluginName: "pinnedColumns", level: "column", description: "", isUsed: (e2) => "left" === e2 || "right" === e2 || "start" === e2 || "end" === e2 }];
var Gt = [{ property: "rowEditable", pluginName: "editing", level: "config", description: "", isUsed: (e2) => "function" == typeof e2 }, { property: "columnGroups", pluginName: "groupingColumns", level: "config", description: "", isUsed: (e2) => Array.isArray(e2) && e2.length > 0 }];
function jt(e2) {
  return `import { ${Yt(e2)}Plugin } from '@toolbox-web/grid/plugins/${t2 = e2, t2.replace(/[A-Z]/g, (e3) => `-${e3.toLowerCase()}`)}';`;
  var t2;
}
function Yt(e2) {
  return e2.charAt(0).toUpperCase() + e2.slice(1);
}
function Xt(e2, t2) {
  return e2.some((e3) => e3.name === t2);
}
function Kt(e2, t2) {
  return e2 && "object" == typeof e2 ? "__rowCacheKey" in e2 ? e2.__rowCacheKey : "rowId" in e2 && null != e2.rowId ? `id:${e2.rowId}` : t2 ? `id:${t2(e2)}` : e2 : e2;
}
function Zt(e2, t2, o2) {
  const i2 = Kt(t2, o2);
  return "string" == typeof i2 ? e2.byKey.get(i2) : i2 && "object" == typeof i2 ? e2.byRef.get(i2) : void 0;
}
function Qt(e2, t2, o2) {
  if (t2 < 0 || t2 >= e2.length) return;
  const i2 = e2[t2], n2 = o2 - i2.height;
  if (0 !== n2) {
    i2.height = o2, i2.measured = true;
    for (let o3 = t2 + 1; o3 < e2.length; o3++) e2[o3].offset += n2;
  }
}
function Jt(e2, t2) {
  if (0 === e2.length) return -1;
  if (t2 <= 0) return 0;
  let o2 = 0, i2 = e2.length - 1;
  for (; o2 <= i2; ) {
    const n2 = Math.floor((o2 + i2) / 2), r2 = e2[n2], s2 = r2.offset + r2.height;
    if (t2 < r2.offset) i2 = n2 - 1;
    else {
      if (!(t2 >= s2)) return n2;
      o2 = n2 + 1;
    }
  }
  return Math.max(0, Math.min(o2, e2.length - 1));
}
function eo(e2, t2) {
  const { positionCache: o2, heightCache: i2, rows: n2, start: r2, end: s2, getPluginHeight: l2, getRowId: a2 } = e2;
  let c2 = false;
  t2.forEach((e3) => {
    const t3 = e3.dataset.rowIndex;
    if (!t3) return;
    const d3 = parseInt(t3, 10);
    if (d3 < r2 || d3 >= s2 || d3 >= n2.length) return;
    const h3 = n2[d3], u2 = l2?.(h3, d3);
    if (void 0 !== u2) {
      const e4 = o2[d3];
      return void ((!e4.measured || Math.abs(e4.height - u2) > 1) && (Qt(o2, d3, u2), c2 = true));
    }
    const g2 = e3.offsetHeight;
    if (g2 > 0) {
      const e4 = o2[d3];
      (!e4.measured || Math.abs(e4.height - g2) > 1) && (Qt(o2, d3, g2), (function(e5, t4, o3, i3) {
        const n3 = Kt(t4, i3);
        "string" == typeof n3 ? e5.byKey.set(n3, o3) : n3 && "object" == typeof n3 && e5.byRef.set(n3, o3);
      })(i2, h3, g2, a2), c2 = true);
    }
  });
  const d2 = c2 ? (function(e3) {
    let t3 = 0;
    for (const o3 of e3) o3.measured && t3++;
    return t3;
  })(o2) : 0, h2 = c2 ? (function(e3, t3) {
    let o3 = 0, i3 = 0;
    for (const n3 of e3) n3.measured && (o3 += n3.height, i3++);
    return i3 > 0 ? o3 / i3 : t3;
  })(o2, e2.defaultHeight) : 0;
  return { hasChanges: c2, measuredCount: d2, averageHeight: h2 };
}
function to(e2) {
  const { totalRows: t2, viewportHeight: o2, scrollTop: i2, rowHeight: n2, overscan: r2 } = e2, s2 = Math.ceil(o2 / n2);
  let l2 = Math.floor(i2 / n2) - r2;
  l2 < 0 && (l2 = 0);
  let a2 = l2 + s2 + 2 * r2;
  return a2 > t2 && (a2 = t2), a2 === t2 && l2 > 0 && (l2 = Math.max(0, a2 - s2 - 2 * r2)), { start: l2, end: a2, offsetY: l2 * n2, totalHeight: t2 * n2 };
}
function oo(e2, t2) {
  return e2 <= t2;
}
var io = class {
  #o;
  state;
  constructor(e2, t2) {
    this.#o = e2, this.state = { enabled: true, rowHeight: 28, bypassThreshold: 24, start: 0, end: 0, container: null, viewportEl: null, totalHeightEl: null, positionCache: null, heightCache: { byKey: /* @__PURE__ */ new Map(), byRef: /* @__PURE__ */ new WeakMap() }, averageHeight: 28, measuredCount: 0, variableHeights: false, cachedViewportHeight: 0, cachedFauxHeight: 0, cachedScrollAreaHeight: 0, scrollAreaEl: null, ...t2 };
  }
  updateCachedGeometry() {
    const e2 = this.state, t2 = e2.container, o2 = e2.viewportEl ?? t2;
    o2 && (e2.cachedViewportHeight = o2.clientHeight), t2 && (e2.cachedFauxHeight = t2.clientHeight);
    const i2 = e2.scrollAreaEl;
    i2 && (e2.cachedScrollAreaHeight = i2.clientHeight);
  }
  calculateTotalSpacerHeight(e2, t2 = false) {
    const o2 = this.state;
    let i2, n2, r2;
    if (t2) {
      const e3 = o2.container ?? this.#o._hostElement, t3 = o2.viewportEl ?? e3, s3 = o2.scrollAreaEl;
      i2 = e3?.clientHeight ?? 0, n2 = t3?.clientHeight ?? 0, r2 = s3 ? s3.clientHeight : i2, o2.cachedFauxHeight = i2, o2.cachedViewportHeight = n2, o2.cachedScrollAreaHeight = r2;
    } else i2 = o2.cachedFauxHeight, n2 = o2.cachedViewportHeight, r2 = o2.cachedScrollAreaHeight || i2;
    const s2 = r2 - n2, l2 = Math.max(0, i2 - r2);
    let a2;
    return a2 = o2.variableHeights && o2.positionCache ? (function(e3) {
      if (0 === e3.length) return 0;
      const t3 = e3[e3.length - 1];
      return t3.offset + t3.height;
    })(o2.positionCache) : e2 * o2.rowHeight, a2 + s2 + l2;
  }
  initializePositionCache() {
    const e2 = this.state;
    if (!e2.variableHeights) return;
    const t2 = this.#o, o2 = t2._rows, i2 = e2.rowHeight || 28, n2 = t2.effectiveConfig?.rowHeight, r2 = t2.effectiveConfig?.getRowId, s2 = r2 ? (e3) => r2(e3) : void 0;
    e2.positionCache = (function(e3, t3, o3, i3, n3) {
      const r3 = new Array(e3.length);
      let s3 = 0;
      for (let l3 = 0; l3 < e3.length; l3++) {
        const a2 = e3[l3];
        let c2 = n3?.(a2, l3), d2 = void 0 !== c2;
        void 0 === c2 && (c2 = Zt(t3, a2, i3.rowId), d2 = void 0 !== c2), void 0 === c2 && (c2 = o3, d2 = false), r3[l3] = { offset: s3, height: c2, measured: d2 }, s3 += c2;
      }
      return r3;
    })(o2, e2.heightCache, i2, { rowId: s2 }, (e3, o3) => {
      const i3 = t2._getPluginRowHeight(e3, o3);
      if (void 0 !== i3) return i3;
      if (n2) {
        const t3 = n2(e3, o3);
        if (void 0 !== t3 && t3 > 0) return t3;
      }
    });
    const l2 = (function(e3, t3, o3, i3) {
      let n3 = 0, r3 = 0;
      for (let s3 = 0; s3 < e3.length; s3++) {
        const o4 = e3[s3];
        if (o4.measured) {
          const e4 = i3?.(t3[s3], s3);
          void 0 === e4 && (r3 += o4.height, n3++);
        }
      }
      return { measuredCount: n3, averageHeight: n3 > 0 ? r3 / n3 : o3 };
    })(e2.positionCache, o2, i2, (e3, o3) => t2._getPluginRowHeight(e3, o3));
    e2.measuredCount = l2.measuredCount, l2.measuredCount > 0 && (e2.averageHeight = l2.averageHeight);
  }
  invalidateRowHeight(e2, t2) {
    const o2 = this.state;
    if (!o2.variableHeights) return;
    if (!o2.positionCache) return;
    const i2 = this.#o._rows;
    if (e2 < 0 || e2 >= i2.length) return;
    const n2 = i2[e2];
    let r2 = t2;
    void 0 === r2 && (r2 = this.#o._getPluginRowHeight(n2, e2)), void 0 === r2 && (r2 = o2.rowHeight);
    const s2 = o2.positionCache[e2];
    if (s2 && !(Math.abs(s2.height - r2) < 1) && (Qt(o2.positionCache, e2, r2), o2.totalHeightEl)) {
      const e3 = this.calculateTotalSpacerHeight(i2.length);
      o2.totalHeightEl.style.height = `${e3}px`;
    }
  }
  measureRenderedRowHeights(e2, t2) {
    const o2 = this.state;
    if (!o2.variableHeights) return;
    if (!o2.positionCache) return;
    const i2 = this.#o, n2 = i2._bodyEl;
    if (!n2) return;
    const r2 = n2.querySelectorAll(".data-grid-row"), s2 = i2.effectiveConfig?.getRowId, l2 = i2._rows, a2 = eo({ positionCache: o2.positionCache, heightCache: o2.heightCache, rows: l2, defaultHeight: o2.rowHeight, start: e2, end: t2, getPluginHeight: (e3, t3) => i2._getPluginRowHeight(e3, t3), getRowId: s2 ? (e3) => s2(e3) : void 0 }, r2);
    if (a2.hasChanges && (o2.measuredCount = a2.measuredCount, o2.averageHeight = a2.averageHeight, o2.totalHeightEl)) {
      const e3 = this.calculateTotalSpacerHeight(l2.length);
      o2.totalHeightEl.style.height = `${e3}px`;
    }
  }
  refreshVirtualWindow(e2 = false, t2 = false) {
    const o2 = this.state, i2 = this.#o, n2 = i2._bodyEl;
    if (!n2) return false;
    const r2 = i2._rows.length;
    if (!o2.enabled) return i2._renderVisibleRows(0, r2), t2 || i2._afterPluginRender(), true;
    if (r2 <= o2.bypassThreshold) return o2.start = 0, o2.end = r2, e2 && (n2.style.transform = "translateY(0px)"), i2._renderVisibleRows(0, r2, i2.__rowRenderEpoch), e2 && o2.variableHeights && this.initializePositionCache(), e2 && o2.totalHeightEl && (o2.totalHeightEl.style.height = `${this.calculateTotalSpacerHeight(r2, true)}px`), i2._updateAriaCounts(r2, i2._visibleColumns.length), t2 || i2._afterPluginRender(), true;
    const s2 = o2.container, l2 = o2.viewportEl ?? s2, a2 = e2 ? o2.cachedViewportHeight = l2.clientHeight : o2.cachedViewportHeight || (o2.cachedViewportHeight = l2.clientHeight), c2 = o2.rowHeight, d2 = s2.scrollTop;
    let h2;
    e2 && o2.variableHeights && this.initializePositionCache();
    const u2 = o2.positionCache;
    o2.variableHeights && u2 && u2.length > 0 ? (h2 = Jt(u2, d2), -1 === h2 && (h2 = 0)) : h2 = Math.floor(d2 / c2), h2 -= h2 % 2, h2 < 0 && (h2 = 0);
    const g2 = i2._adjustPluginVirtualStart(h2, d2, c2);
    let f2;
    if (void 0 !== g2 && g2 < h2 && (h2 = g2, h2 -= h2 % 2, h2 < 0 && (h2 = 0)), o2.variableHeights && u2 && u2.length > 0) {
      const e3 = a2 + 3 * c2;
      let t3 = 0;
      for (f2 = h2; f2 < r2 && t3 < e3; ) t3 += u2[f2].height, f2++;
      const o3 = Math.ceil(a2 / c2) + 3;
      f2 - h2 < o3 && (f2 = Math.min(h2 + o3, r2));
    } else {
      f2 = h2 + (Math.ceil(a2 / c2) + 3);
    }
    f2 > r2 && (f2 = r2);
    const p2 = o2.start, w2 = o2.end;
    if (!e2 && h2 === p2 && f2 === w2) return false;
    o2.start = h2, o2.end = f2;
    const m2 = e2 ? o2.cachedFauxHeight = s2.clientHeight : o2.cachedFauxHeight || (o2.cachedFauxHeight = s2.clientHeight);
    if (e2) {
      const e3 = o2.scrollAreaEl;
      e3 && (o2.cachedScrollAreaHeight = e3.clientHeight);
    }
    if (0 === m2 && a2 > 0) return i2._requestSchedulerPhase(_e.VIRTUALIZATION, "stale-refs-retry"), false;
    if (e2 && o2.totalHeightEl) {
      const e3 = this.calculateTotalSpacerHeight(r2);
      o2.totalHeightEl.style.height = `${e3}px`;
    }
    let b2;
    b2 = o2.variableHeights && u2 && u2[h2] ? u2[h2].offset : h2 * c2;
    const v2 = -(d2 - b2);
    return n2.style.transform = `translateY(${v2}px)`, i2._renderVisibleRows(h2, f2, i2.__rowRenderEpoch), e2 && o2.variableHeights && this.measureRenderedRowHeights(h2, f2), i2._updateAriaCounts(r2, i2._visibleColumns.length), e2 && !t2 && (i2._afterPluginRender(), queueMicrotask(() => {
      if (!o2.totalHeightEl) return;
      const e3 = this.calculateTotalSpacerHeight(r2);
      0 === o2.cachedFauxHeight && o2.cachedViewportHeight > 0 || (o2.totalHeightEl.style.height = `${e3}px`);
    })), true;
  }
};
var no = class {
  constructor(e2) {
    this.grid = e2;
  }
  plugins = [];
  getPlugins() {
    return this.plugins;
  }
  pluginMap = /* @__PURE__ */ new Map();
  cellRenderers = /* @__PURE__ */ new Map();
  headerRenderers = /* @__PURE__ */ new Map();
  cellEditors = /* @__PURE__ */ new Map();
  _hasAfterCellRender = false;
  _hasAfterRowRender = false;
  _hasProcessRows = false;
  _hasRowStructurePlugins = false;
  #z = /* @__PURE__ */ new Map();
  eventListeners = /* @__PURE__ */ new Map();
  queryHandlers = /* @__PURE__ */ new Map();
  attachAll(e2) {
    for (const t2 of e2) this.attach(t2);
  }
  attach(e2) {
    if ((function(e3, t2, o2) {
      const i2 = e3.name, n2 = e3.constructor.dependencies ?? [];
      for (const r2 of n2) {
        const e4 = r2.name, n3 = r2.required ?? true, s2 = r2.reason;
        if (!t2.some((t3) => t3.name === e4)) {
          const t3 = s2 ?? `${Yt(i2)}Plugin requires ${Yt(e4)}Plugin`, r3 = jt(e4);
          n3 && T("TBW020", `Plugin dependency error:

${t3}.

  → Add the plugin to your gridConfig.plugins array BEFORE ${Yt(i2)}Plugin:
    ${r3}
    plugins: [new ${Yt(e4)}Plugin(), new ${Yt(i2)}Plugin()]`, o2);
        }
      }
    })(e2, this.plugins, this.grid.getAttribute("id") ?? void 0), this.pluginMap.set(e2.constructor, e2), this.plugins.push(e2), e2.cellRenderers) for (const [t2, o2] of Object.entries(e2.cellRenderers)) this.cellRenderers.set(t2, o2);
    if (e2.headerRenderers) for (const [t2, o2] of Object.entries(e2.headerRenderers)) this.headerRenderers.set(t2, o2);
    if (e2.cellEditors) for (const [t2, o2] of Object.entries(e2.cellEditors)) this.cellEditors.set(t2, o2);
    this.registerQueryHandlers(e2), e2.attach(this.grid), this.#N();
    for (const t2 of this.plugins) t2 !== e2 && t2.onPluginAttached && t2.onPluginAttached(e2.name, e2);
  }
  registerQueryHandlers(e2) {
    const t2 = e2.constructor.manifest;
    if (t2?.queries) for (const o2 of t2.queries) {
      let t3 = this.queryHandlers.get(o2.type);
      t3 || (t3 = /* @__PURE__ */ new Set(), this.queryHandlers.set(o2.type, t3)), t3.add(e2);
    }
  }
  unregisterQueryHandlers(e2) {
    for (const [t2, o2] of this.queryHandlers) o2.delete(e2), 0 === o2.size && this.queryHandlers.delete(t2);
  }
  detachAll() {
    for (const e2 of this.plugins) for (const t2 of this.plugins) t2 !== e2 && t2.onPluginDetached && t2.onPluginDetached(e2.name);
    for (let e2 = this.plugins.length - 1; e2 >= 0; e2--) {
      const t2 = this.plugins[e2];
      this.unsubscribeAll(t2), this.unregisterQueryHandlers(t2), t2.detach();
    }
    this.plugins = [], this.pluginMap.clear(), this.cellRenderers.clear(), this.headerRenderers.clear(), this.cellEditors.clear(), this.eventListeners.clear(), this.queryHandlers.clear(), this._hasAfterCellRender = false, this._hasAfterRowRender = false, this._hasProcessRows = false, this.#z.clear();
  }
  getPlugin(e2) {
    return this.pluginMap.get(e2);
  }
  getPluginByName(e2) {
    return this.plugins.find((t2) => t2.name === e2 || t2.aliases?.includes(e2));
  }
  hasPlugin(e2) {
    return this.pluginMap.has(e2);
  }
  getAll() {
    return this.plugins;
  }
  getRegisteredPluginNames() {
    return this.plugins.map((e2) => e2.name);
  }
  getCellRenderer(e2) {
    return this.cellRenderers.get(e2);
  }
  getHeaderRenderer(e2) {
    return this.headerRenderers.get(e2);
  }
  getCellEditor(e2) {
    return this.cellEditors.get(e2);
  }
  getPluginStyles() {
    return this.plugins.filter((e2) => e2.styles).map((e2) => ({ name: e2.name, styles: e2.styles }));
  }
  processRows(e2) {
    if (!this._hasProcessRows) return e2;
    let t2 = e2;
    for (const o2 of this.#q("processRows")) {
      const e3 = o2.processRows(t2);
      e3 !== t2 && (t2 = e3);
    }
    return t2;
  }
  processColumns(e2) {
    let t2 = [...e2];
    for (const o2 of this.#q("processColumns")) t2 = o2.processColumns(t2);
    return t2;
  }
  beforeRender() {
    for (const e2 of this.plugins) e2.beforeRender?.();
  }
  afterRender() {
    for (const e2 of this.#q("afterRender")) e2.afterRender();
  }
  afterCellRender(e2) {
    for (const t2 of this.#q("afterCellRender")) t2.afterCellRender(e2);
  }
  hasAfterCellRenderHook() {
    return this._hasAfterCellRender;
  }
  afterRowRender(e2) {
    for (const t2 of this.#q("afterRowRender")) t2.afterRowRender(e2);
  }
  hasAfterRowRenderHook() {
    return this._hasAfterRowRender;
  }
  #N() {
    this._hasAfterCellRender = this.plugins.some((e2) => "function" == typeof e2.afterCellRender), this._hasAfterRowRender = this.plugins.some((e2) => "function" == typeof e2.afterRowRender), this._hasProcessRows = this.plugins.some((e2) => "function" == typeof e2.processRows), this._hasRowStructurePlugins = this.plugins.some((e2) => true === e2.constructor.manifest?.modifiesRowStructure), this.#z.clear();
  }
  #q(e2) {
    let t2 = this.#z.get(e2);
    if (t2) return t2;
    t2 = this.plugins.filter((t3) => e2 in t3 && "function" == typeof t3[e2]);
    const o2 = (t3) => {
      const o3 = t3.constructor.manifest?.hookPriority;
      if (!o3) return 0;
      return o3[e2] ?? 0;
    };
    return t2.some((e3) => 0 !== o2(e3)) && t2.sort((e3, t3) => o2(e3) - o2(t3)), this.#z.set(e2, t2), t2;
  }
  onScrollRender() {
    for (const e2 of this.#q("onScrollRender")) e2.onScrollRender();
  }
  getRowHeight(e2, t2) {
    for (const o2 of this.plugins) if ("function" == typeof o2.getRowHeight) {
      const i2 = o2.getRowHeight(e2, t2);
      if (void 0 !== i2) return i2;
    }
  }
  hasRowHeightPlugin() {
    for (const e2 of this.plugins) if ("function" == typeof e2.getRowHeight) return true;
    return false;
  }
  adjustVirtualStart(e2, t2, o2) {
    let i2 = e2;
    for (const n2 of this.plugins) if ("function" == typeof n2.adjustVirtualStart) {
      const r2 = n2.adjustVirtualStart(e2, t2, o2);
      r2 < i2 && (i2 = r2);
    }
    return i2;
  }
  renderRow(e2, t2, o2) {
    for (const i2 of this.plugins) if (i2.renderRow?.(e2, t2, o2)) return true;
    return false;
  }
  queryPlugins(e2) {
    const t2 = [], o2 = this.queryHandlers.get(e2.type);
    if (o2 && o2.size > 0) {
      for (const i2 of o2) {
        const o3 = i2.handleQuery?.(e2);
        void 0 !== o3 && t2.push(o3);
      }
      return t2;
    }
    for (const i2 of this.plugins) {
      const o3 = i2.handleQuery?.(e2);
      void 0 !== o3 && t2.push(o3);
    }
    return t2;
  }
  subscribe(e2, t2, o2) {
    let i2 = this.eventListeners.get(t2);
    i2 || (i2 = /* @__PURE__ */ new Map(), this.eventListeners.set(t2, i2)), i2.set(e2, o2);
  }
  unsubscribe(e2, t2) {
    const o2 = this.eventListeners.get(t2);
    o2 && (o2.delete(e2), 0 === o2.size && this.eventListeners.delete(t2));
  }
  unsubscribeAll(e2) {
    for (const [t2, o2] of this.eventListeners) o2.delete(e2), 0 === o2.size && this.eventListeners.delete(t2);
  }
  emitPluginEvent(e2, t2) {
    const o2 = this.eventListeners.get(e2);
    if (o2) for (const n2 of o2.values()) try {
      n2(t2);
    } catch (i2) {
      H("TBW024", `Error in plugin event handler for "${e2}": ${i2}`, this.grid.getAttribute("id") ?? void 0);
    }
  }
  onKeyDown(e2) {
    for (const t2 of this.#q("onKeyDown")) if (t2.onKeyDown(e2)) return true;
    return false;
  }
  onCellClick(e2) {
    for (const t2 of this.#q("onCellClick")) if (t2.onCellClick(e2)) return true;
    return false;
  }
  onRowClick(e2) {
    for (const t2 of this.#q("onRowClick")) if (t2.onRowClick(e2)) return true;
    return false;
  }
  onHeaderClick(e2) {
    for (const t2 of this.#q("onHeaderClick")) if (t2.onHeaderClick(e2)) return true;
    return false;
  }
  onScroll(e2) {
    for (const t2 of this.#q("onScroll")) t2.onScroll(e2);
  }
  onCellMouseDown(e2) {
    for (const t2 of this.#q("onCellMouseDown")) if (t2.onCellMouseDown(e2)) return true;
    return false;
  }
  onCellMouseMove(e2) {
    for (const t2 of this.#q("onCellMouseMove")) if (t2.onCellMouseMove(e2)) return true;
    return false;
  }
  onCellMouseUp(e2) {
    for (const t2 of this.#q("onCellMouseUp")) if (t2.onCellMouseUp(e2)) return true;
    return false;
  }
  getHorizontalScrollOffsets(e2, t2) {
    let o2 = 0, i2 = 0, n2 = false;
    for (const r2 of this.plugins) {
      const s2 = r2.getHorizontalScrollOffsets?.(e2, t2);
      s2 && (o2 += s2.left, i2 += s2.right, s2.skipScroll && (n2 = true));
    }
    return { left: o2, right: i2, skipScroll: n2 };
  }
  getToolPanels() {
    const e2 = [];
    for (const t2 of this.plugins) {
      const o2 = t2.getToolPanel?.();
      o2 && e2.push({ plugin: t2, panel: o2 });
    }
    return e2.sort((e3, t2) => (e3.panel.order ?? 0) - (t2.panel.order ?? 0));
  }
  getHeaderContents() {
    const e2 = [];
    for (const t2 of this.plugins) {
      const o2 = t2.getHeaderContent?.();
      o2 && e2.push({ plugin: t2, content: o2 });
    }
    return e2.sort((e3, t2) => (e3.content.order ?? 0) - (t2.content.order ?? 0));
  }
};
var ro = class _ro extends HTMLElement {
  static tagName = "tbw-grid";
  static version = "2.1.1";
  static #$ = 0;
  static adapters = [];
  static registerAdapter(e2) {
    this.adapters.push(e2);
  }
  static getAdapters() {
    return this.adapters;
  }
  static clearAdapters() {
    this.adapters = [];
  }
  static get observedAttributes() {
    return ["rows", "columns", "grid-config", "fit-mode", "loading"];
  }
  get #W() {
    return this;
  }
  #F = false;
  #r;
  #s;
  #B = [];
  get #m() {
    return this.#V?.effective ?? {};
  }
  #U = false;
  #G = false;
  #j = { rows: false, columns: false, gridConfig: false, fitMode: false };
  #Y;
  #X = 0;
  #K = null;
  #Z = false;
  #Q = false;
  #J = 0;
  #ee;
  #te = { startY: null, startX: null, lastY: null, lastX: null, lastTime: null, velocityY: 0, velocityX: 0, momentumRaf: 0, locked: false, activePointerId: null };
  #oe;
  #ie;
  #ne;
  #re;
  #se = { scrollTop: 0, scrollLeft: 0, scrollHeight: 0, scrollWidth: 0, clientHeight: 0, clientWidth: 0 };
  #le;
  #ae;
  #ce;
  #de;
  #he;
  #ue;
  get _pluginManager() {
    return this.#le;
  }
  #ge = false;
  #fe;
  #pe;
  #R;
  #V;
  #we = /* @__PURE__ */ (function() {
    return { toolPanels: /* @__PURE__ */ new Map(), headerContents: /* @__PURE__ */ new Map(), toolbarContents: /* @__PURE__ */ new Map(), hasToolButtonsContainer: false, lightDomHeaderContent: [], lightDomTitle: null, lightDomToolPanelIds: /* @__PURE__ */ new Set(), lightDomToolbarContentIds: /* @__PURE__ */ new Set(), apiToolPanelIds: /* @__PURE__ */ new Set(), apiHeaderContentIds: /* @__PURE__ */ new Set(), isPanelOpen: false, expandedSections: /* @__PURE__ */ new Set(), headerContentCleanups: /* @__PURE__ */ new Map(), panelCleanups: /* @__PURE__ */ new Map(), toolbarContentCleanups: /* @__PURE__ */ new Map(), lightDomContentMoved: false };
  })();
  #me;
  #be;
  #ve;
  #Ce = false;
  #_e = /* @__PURE__ */ new Set();
  #ye = /* @__PURE__ */ new Map();
  #Re;
  #Se = /* @__PURE__ */ new Map();
  #Ee = false;
  _rows = [];
  #xe = [];
  get _columns() {
    return this.#m.columns ?? [];
  }
  set _columns(e2) {
    this.#m.columns = e2, this.#Te = void 0;
  }
  #Te;
  get _visibleColumns() {
    return this.#Te ??= this._columns.filter((e2) => !e2.hidden);
  }
  _invalidateVisibleColumnsCache() {
    this.#Te = void 0;
  }
  _headerRowEl;
  _bodyEl;
  _rowPool = [];
  _resizeController;
  get _virtualization() {
    return this.#de.state;
  }
  set _virtualization(e2) {
    Object.assign(this.#de.state, e2);
  }
  _focusRow = 0;
  _focusCol = 0;
  _restoreFocusAfterRender = false;
  _sortState = null;
  _gridTemplate = "";
  __rowRenderEpoch = 0;
  __didInitialAutoSize = false;
  get __lightDomColumnsCache() {
    return this.#V?.lightDomColumnsCache;
  }
  set __lightDomColumnsCache(e2) {
    this.#V && (this.#V.lightDomColumnsCache = e2);
  }
  get __originalColumnNodes() {
    return this.#V?.originalColumnNodes;
  }
  set __originalColumnNodes(e2) {
    this.#V && (this.#V.originalColumnNodes = e2);
  }
  __originalOrder = [];
  __frameworkAdapter;
  __rowsBodyEl = null;
  get rows() {
    return this._rows;
  }
  set rows(e2) {
    const t2 = this.#B;
    this.#B = e2, t2 !== e2 && this.#Ae("rows");
  }
  get sourceRows() {
    return this.#B;
  }
  set sourceRows(e2) {
    this.#B = e2;
  }
  get columns() {
    return [...this._columns];
  }
  set columns(e2) {
    const t2 = this.#V?.getColumns();
    this.#V?.setColumns(e2), t2 !== e2 && this.#Ae("columns");
  }
  get gridConfig() {
    return this.#m;
  }
  set gridConfig(e2) {
    e2 && this.__frameworkAdapter?.processConfig && (e2 = this.__frameworkAdapter.processConfig(e2));
    const t2 = this.#V?.getGridConfig();
    this.#V?.setGridConfig(e2), t2 !== e2 && (this.#V.clearLightDomCache(), this.#Ae("gridConfig"));
  }
  get fitMode() {
    return this.#m.fitMode ?? "stretch";
  }
  set fitMode(e2) {
    const t2 = this.#V?.getFitMode();
    this.#V?.setFitMode(e2), t2 !== e2 && this.#Ae("fitMode");
  }
  get loading() {
    return this.#Ce;
  }
  set loading(e2) {
    const t2 = this.#Ce;
    this.#Ce = e2, e2 ? this.setAttribute("loading", "") : this.removeAttribute("loading"), t2 !== e2 && this.#Pe();
  }
  setRowLoading(e2, t2) {
    const o2 = this.#_e.has(e2);
    t2 ? this.#_e.add(e2) : this.#_e.delete(e2), o2 !== t2 && this.#He(e2, t2);
  }
  setCellLoading(e2, t2, o2) {
    let i2 = this.#ye.get(e2);
    const n2 = i2?.has(t2) ?? false;
    o2 ? (i2 || (i2 = /* @__PURE__ */ new Set(), this.#ye.set(e2, i2)), i2.add(t2)) : (i2?.delete(t2), 0 === i2?.size && this.#ye.delete(e2)), n2 !== o2 && this.#Me(e2, t2, o2);
  }
  isRowLoading(e2) {
    return this.#_e.has(e2);
  }
  isCellLoading(e2, t2) {
    return this.#ye.get(e2)?.has(t2) ?? false;
  }
  clearAllLoading() {
    this.loading = false;
    for (const e2 of this.#_e) this.#He(e2, false);
    this.#_e.clear();
    for (const [e2, t2] of this.#ye) for (const o2 of t2) this.#Me(e2, o2, false);
    this.#ye.clear();
  }
  get effectiveConfig() {
    return this.#m;
  }
  get disconnectSignal() {
    return this.#oe || (this.#oe = new AbortController()), this.#oe.signal;
  }
  constructor() {
    super(), this.#ke(), this.#r = new Promise((e2) => this.#s = e2), this.#de = new io(this), this.#he = new et(this), this.#ue = new gt(this), this.#Y = new ye(this), this.#Y.setInitialReadyResolver(() => this.#s?.()), this.#me = /* @__PURE__ */ (function(e2, t2) {
      let o2 = false;
      const i2 = { get isInitialized() {
        return o2;
      }, setInitialized(e3) {
        o2 = e3;
      }, get isPanelOpen() {
        return e2.isPanelOpen;
      }, get expandedSections() {
        return [...e2.expandedSections];
      }, openToolPanel() {
        if (e2.isPanelOpen) return;
        if (0 === e2.toolPanels.size) return void A("TBW071", "No tool panels registered", t2.id);
        if (e2.isPanelOpen = true, 0 === e2.expandedSections.size && e2.toolPanels.size > 0) {
          const t3 = [...e2.toolPanels.values()].sort((e3, t4) => (e3.order ?? 100) - (t4.order ?? 100))[0];
          t3 && e2.expandedSections.add(t3.id);
        }
        const o3 = t2._renderRoot;
        Ht(o3, e2), Mt(o3, e2), Pt(o3, e2, t2._accordionIcons), t2._emit("tool-panel-open", { sections: i2.expandedSections });
      }, closeToolPanel() {
        if (!e2.isPanelOpen) return;
        for (const t3 of e2.panelCleanups.values()) t3();
        e2.panelCleanups.clear();
        for (const t3 of e2.toolPanels.values()) t3.onClose?.();
        e2.isPanelOpen = false;
        const o3 = t2._renderRoot;
        Ht(o3, e2), Mt(o3, e2), t2._emit("tool-panel-close", {});
      }, toggleToolPanel() {
        e2.isPanelOpen ? i2.closeToolPanel() : i2.openToolPanel();
      }, toggleToolPanelSection(o3) {
        const i3 = e2.toolPanels.get(o3);
        if (!i3) return void A("TBW072", `Tool panel section "${o3}" not found`, t2.id);
        if (1 === e2.toolPanels.size) return;
        const n2 = t2._renderRoot, r2 = e2.expandedSections.has(o3);
        if (r2) {
          const t3 = e2.panelCleanups.get(o3);
          t3 && (t3(), e2.panelCleanups.delete(o3)), i3.onClose?.(), e2.expandedSections.delete(o3), Dt(n2, o3, false);
        } else {
          for (const [t3, i4] of e2.toolPanels) if (t3 !== o3 && e2.expandedSections.has(t3)) {
            const o4 = e2.panelCleanups.get(t3);
            o4 && (o4(), e2.panelCleanups.delete(t3)), i4.onClose?.(), e2.expandedSections.delete(t3), Dt(n2, t3, false);
            const r3 = n2.querySelector(`[data-section="${t3}"] .tbw-accordion-content`);
            r3 && (r3.innerHTML = "");
          }
          e2.expandedSections.add(o3), Dt(n2, o3, true), (function(e3, t3, o4) {
            const i4 = t3.toolPanels.get(o4);
            if (!i4?.render) return;
            const n3 = e3.querySelector(`[data-section="${o4}"] .tbw-accordion-content`);
            if (!n3) return;
            const r3 = i4.render(n3);
            r3 && t3.panelCleanups.set(o4, r3);
          })(n2, e2, o3);
        }
        t2._emit("tool-panel-section-toggle", { id: o3, expanded: !r2 });
      }, getToolPanels: () => [...e2.toolPanels.values()], registerToolPanel(i3) {
        e2.toolPanels.has(i3.id) ? A("TBW073", `Tool panel "${i3.id}" already registered`, t2.id) : (e2.toolPanels.set(i3.id, i3), o2 && t2.refreshShellHeader?.());
      }, unregisterToolPanel(i3) {
        if (e2.expandedSections.has(i3)) {
          const t3 = e2.panelCleanups.get(i3);
          t3 && (t3(), e2.panelCleanups.delete(i3)), e2.expandedSections.delete(i3);
        }
        e2.toolPanels.delete(i3), o2 && t2.refreshShellHeader?.();
      }, getHeaderContents: () => [...e2.headerContents.values()], registerHeaderContent(i3) {
        e2.headerContents.has(i3.id) ? A("TBW074", `Header content "${i3.id}" already registered`, t2.id) : (e2.headerContents.set(i3.id, i3), o2 && At(t2._renderRoot, e2));
      }, unregisterHeaderContent(o3) {
        const i3 = e2.headerContentCleanups.get(o3);
        i3 && (i3(), e2.headerContentCleanups.delete(o3));
        const n2 = e2.headerContents.get(o3);
        n2?.onDestroy?.(), e2.headerContents.delete(o3);
        const r2 = t2._renderRoot.querySelector(`[data-header-content="${o3}"]`);
        r2?.remove();
      }, getToolbarContents: () => [...e2.toolbarContents.values()].sort((e3, t3) => (e3.order ?? 0) - (t3.order ?? 0)), registerToolbarContent(i3) {
        e2.toolbarContents.has(i3.id) ? A("TBW075", `Toolbar content "${i3.id}" already registered`, t2.id) : (e2.toolbarContents.set(i3.id, i3), o2 && t2.refreshShellHeader?.());
      }, unregisterToolbarContent(i3) {
        const n2 = e2.toolbarContentCleanups.get(i3);
        n2 && (n2(), e2.toolbarContentCleanups.delete(i3));
        const r2 = e2.toolbarContents.get(i3);
        r2?.onDestroy && r2.onDestroy(), e2.toolbarContents.delete(i3), o2 && t2.refreshShellHeader?.();
      } };
      return i2;
    })(this.#we, this), this.#V = new Re(this);
  }
  async #ke() {
    await $t(`@layer tbw-base, tbw-plugins, tbw-theme;

@layer tbw-base{tbw-grid{--tbw-base-icon-size: 1em;--tbw-base-radius: .25em;--tbw-font-size: 1em;--tbw-font-size-sm: .9285em;--tbw-font-size-xs: .7857em;--tbw-font-size-2xs: .7142em;--tbw-spacing-xs: .25em;--tbw-spacing-sm: .375em;--tbw-spacing-md: .5em;--tbw-spacing-lg: .75em;--tbw-spacing-xl: 1em;--tbw-icon-size: var(--tbw-base-icon-size);--tbw-icon-size-sm: .875em;--tbw-checkbox-size: var(--tbw-base-icon-size);--tbw-toggle-size: 1.25em;--tbw-border-radius: var(--tbw-base-radius);--tbw-color-bg: transparent;--tbw-color-panel-bg: light-dark(#eeeeee, #222222);--tbw-color-fg: light-dark(#222222, #eeeeee);--tbw-color-fg-muted: light-dark(#555555, #aaaaaa);--tbw-color-accent: light-dark(#3b82f6, #3b82f6);--tbw-color-accent-fg: light-dark(#ffffff, #000000);--tbw-color-success: light-dark(hsl(122, 39%, 40%), hsl(122, 39%, 49%));--tbw-color-warning: light-dark(hsl(38, 92%, 50%), hsl(38, 92%, 50%));--tbw-color-error: light-dark(hsl(0, 65%, 51%), hsl(0, 65%, 55%));--tbw-color-danger: var(--tbw-color-error);--tbw-color-selection: light-dark(#fff7d6, #333333);--tbw-color-row-alt: var(--tbw-color-bg);--tbw-color-row-hover: light-dark(#f0f6ff, #1c1c1c);--tbw-color-header-bg: color-mix(in hsl, var(--tbw-color-panel-bg) 85%, var(--tbw-color-fg));--tbw-color-header-fg: color-mix(in hsl, var(--tbw-color-fg) 75%, var(--tbw-color-panel-bg));--tbw-color-border: light-dark(#d0d0d4, #454545);--tbw-color-border-strong: light-dark(#777777, #666666);--tbw-color-border-cell: var(--tbw-color-border);--tbw-color-border-header: var(--tbw-color-border);--tbw-color-shadow: light-dark(rgba(0, 0, 0, .1), rgba(0, 0, 0, .3));--tbw-font-family: inherit;--tbw-font-size-header: var(--tbw-font-size);--tbw-font-weight-header: bold;--tbw-cell-padding-header: var(--tbw-spacing-xs) var(--tbw-spacing-md);--tbw-cell-padding-v: var(--tbw-spacing-xs);--tbw-cell-padding-h: var(--tbw-spacing-md);--tbw-cell-padding: var(--tbw-cell-padding-v) var(--tbw-cell-padding-h);--tbw-cell-padding-input: var(--tbw-spacing-xs) var(--tbw-spacing-sm);--tbw-row-height: 1.75em;--tbw-header-height: 1.875em;--tbw-cell-white-space: nowrap;--tbw-border-width: 1px;--tbw-border-style: solid;--tbw-border-input: var(--tbw-border-width) var(--tbw-border-style) var(--tbw-color-border-strong);--tbw-border-header: var(--tbw-border-width) var(--tbw-border-style) var(--tbw-color-border-header);--tbw-row-divider: var(--tbw-border-width) var(--tbw-border-style) var(--tbw-color-border-cell);--tbw-row-hover-outline: 0;--tbw-color-active-row-bg: var(--tbw-color-selection);--tbw-active-row-outline: 0;--tbw-focus-outline-width: 2px;--tbw-focus-outline: var(--tbw-focus-outline-width) var(--tbw-border-style) var(--tbw-color-accent);--tbw-focus-outline-offset: -2px;--tbw-focus-background: rgba(from var(--tbw-color-accent) r g b / 12%);--tbw-range-border-color: var(--tbw-color-accent);--tbw-range-selection-bg: rgba(from var(--tbw-range-border-color) r g b / 12%);--tbw-resize-handle-width: 2px;--tbw-resize-handle-color: transparent;--tbw-resize-handle-color-hover: var(--tbw-color-accent);--tbw-resize-handle-border-radius: 0;--tbw-resize-indicator-width: 2px;--tbw-resize-indicator-color: var(--tbw-color-accent);--tbw-resize-indicator-opacity: .6;--tbw-transition-duration: .12s;--tbw-transition-ease: ease;--tbw-animation-duration: .2s;--tbw-animation-easing: ease-out;--tbw-animation-enabled: 1;--tbw-row-change-duration: .5s;--tbw-row-insert-duration: .3s;--tbw-row-remove-duration: .2s;--tbw-row-change-color: rgba(from var(--tbw-color-accent) r g b / 25%);--tbw-sort-indicator-color: var(--tbw-color-fg-muted);--tbw-sort-indicator-active-color: var(--tbw-color-accent);--tbw-sort-indicator-display: inline-flex;--tbw-sort-indicator-visibility: visible;--tbw-header-text-transform: none;--tbw-header-letter-spacing: normal;--tbw-color-header-separator: var(--tbw-color-border-cell);--tbw-density-scale: 1;--tbw-shell-header-height: 2.75em;--tbw-shell-header-bg: var(--tbw-color-panel-bg);--tbw-shell-header-border: var(--tbw-color-border);--tbw-shell-title-font-size: var(--tbw-font-size);--tbw-shell-title-font-weight: 600;--tbw-tool-panel-width: 17.5em;--tbw-tool-panel-bg: var(--tbw-color-panel-bg);--tbw-tool-panel-border: var(--tbw-color-border);--tbw-tool-panel-header-height: 2.5em;--tbw-tool-panel-transition: var(--tbw-animation-duration) var(--tbw-animation-easing);--tbw-toolbar-button-size: 2em;--tbw-toolbar-button-gap: var(--tbw-spacing-xs);--tbw-panel-padding: var(--tbw-spacing-lg);--tbw-panel-gap: var(--tbw-spacing-md);--tbw-menu-item-padding: var(--tbw-spacing-sm) var(--tbw-spacing-lg);--tbw-menu-item-gap: var(--tbw-spacing-md);--tbw-menu-min-width: 10rem;--tbw-button-padding: var(--tbw-spacing-sm) var(--tbw-spacing-lg);--tbw-button-padding-sm: var(--tbw-spacing-xs) var(--tbw-spacing-md);--tbw-input-height: var(--tbw-row-height);--tbw-input-padding: 0 var(--tbw-spacing-md);--tbw-detail-padding: var(--tbw-spacing-xl);--tbw-detail-max-height: 31.25rem;--tbw-indicator-size: var(--tbw-spacing-sm);--tbw-icon-expand: "▶";--tbw-icon-collapse: "▼";--tbw-icon-sort-asc: "▲";--tbw-icon-sort-desc: "▼";--tbw-icon-sort-none: "⇅";--tbw-icon-submenu-arrow: "▶";--tbw-icon-drag-handle: "⋮⋮";--tbw-icon-tool-panel: "☰";--tbw-icon-filter: "";--tbw-icon-filter-active: "";--tbw-icon-print: "🖨️";--tbw-icon-expand-mask: none;--tbw-icon-collapse-mask: none;--tbw-icon-sort-asc-mask: none;--tbw-icon-sort-desc-mask: none;--tbw-icon-sort-none-mask: none;--tbw-icon-submenu-arrow-mask: none;--tbw-icon-drag-handle-mask: none;--tbw-icon-tool-panel-mask: none;--tbw-icon-filter-mask: url("data:image/svg+xml,%3Csvg viewBox='0 0 16 16' width='12' height='12' xmlns='http://www.w3.org/2000/svg'%3E%3Cpath fill='currentColor' d='M6 10.5a.5.5 0 0 1 .5-.5h3a.5.5 0 0 1 0 1h-3a.5.5 0 0 1-.5-.5zm-2-3a.5.5 0 0 1 .5-.5h7a.5.5 0 0 1 0 1h-7a.5.5 0 0 1-.5-.5zm-2-3a.5.5 0 0 1 .5-.5h11a.5.5 0 0 1 0 1h-11a.5.5 0 0 1-.5-.5z'/%3E%3C/svg%3E");--tbw-icon-filter-active-mask: var(--tbw-icon-filter-mask);--tbw-icon-print-mask: none}}
@layer tbw-base{tbw-grid{color-scheme:inherit;position:relative;display:block;width:100%;height:100%;min-height:0;contain:content;font-family:var(--tbw-font-family);font-size:var(--tbw-font-size);font-feature-settings:"tnum","lnum";background:var(--tbw-color-bg);color:var(--tbw-color-fg);border:1px solid var(--tbw-color-border);border-radius:var(--tbw-border-radius);overflow:clip;outline:none;&,*{box-sizing:border-box}.tbw-grid-root{position:relative;display:flex;flex-direction:column;height:100%;&.has-shell{display:flex;flex-direction:column;height:100%}&:has(.selected){user-select:none}}.rows-body-wrapper{flex:1;min-height:0;display:flex;flex-direction:row;width:100%;min-width:fit-content}.rows-body{flex:1;min-width:0;min-height:0;display:flex;flex-direction:column;overflow:visible}.rows-container{display:flex;flex-direction:row;flex:1;min-height:0;overflow:visible}.rows-viewport{flex:1;min-width:0;position:relative;display:block;overflow:clip;touch-action:none;.rows{position:absolute;top:0;left:0;min-width:100%;will-change:transform;z-index:var(--tbw-z-layer-rows, 1)}}.faux-vscroll{position:sticky;inset-inline-end:0;flex-shrink:0;width:auto;overflow-y:auto;overflow-x:hidden;z-index:var(--tbw-z-layer-header, 30);touch-action:none}.faux-vscroll-spacer{width:1px}.tbw-sr-only{position:absolute;width:1px;height:1px;padding:0;margin:-1px;overflow:hidden;clip:rect(0,0,0,0);white-space:nowrap;border:0}&[data-has-focus]{.cell-focus,.row-focus{outline:var(--tbw-focus-outline);outline-offset:var(--tbw-focus-outline-offset)}}.sticky-left,.sticky-right{position:sticky;z-index:25}.sticky-left{box-shadow:1px 0 0 var(--tbw-color-border)}.sticky-right{box-shadow:-1px 0 0 var(--tbw-color-border)}}}
@layer tbw-base{tbw-grid{[data-icon=expand]:empty:before{content:var(--tbw-icon-expand)}[data-icon=collapse]:empty:before{content:var(--tbw-icon-collapse)}[data-icon=sort-asc]:empty:before{content:var(--tbw-icon-sort-asc)}[data-icon=sort-desc]:empty:before{content:var(--tbw-icon-sort-desc)}[data-icon=sort-none]:empty:before{content:var(--tbw-icon-sort-none)}[data-icon=submenu-arrow]:empty:before{content:var(--tbw-icon-submenu-arrow)}[data-icon=drag-handle]:empty:before{content:var(--tbw-icon-drag-handle)}[data-icon=tool-panel]:empty:before{content:var(--tbw-icon-tool-panel)}[data-icon=print]:empty:before{content:var(--tbw-icon-print)}[data-icon=filter]:empty:before{content:var(--tbw-icon-filter)}[data-icon=filter-active]:empty:before{content:var(--tbw-icon-filter-active)}[data-icon]:empty:before{display:inline-block;vertical-align:middle;-webkit-mask-size:contain;mask-size:contain;-webkit-mask-repeat:no-repeat;mask-repeat:no-repeat;-webkit-mask-position:center;mask-position:center}[data-icon=expand]:empty:before{-webkit-mask-image:var(--tbw-icon-expand-mask);mask-image:var(--tbw-icon-expand-mask)}[data-icon=collapse]:empty:before{-webkit-mask-image:var(--tbw-icon-collapse-mask);mask-image:var(--tbw-icon-collapse-mask)}[data-icon=sort-asc]:empty:before{-webkit-mask-image:var(--tbw-icon-sort-asc-mask);mask-image:var(--tbw-icon-sort-asc-mask)}[data-icon=sort-desc]:empty:before{-webkit-mask-image:var(--tbw-icon-sort-desc-mask);mask-image:var(--tbw-icon-sort-desc-mask)}[data-icon=sort-none]:empty:before{-webkit-mask-image:var(--tbw-icon-sort-none-mask);mask-image:var(--tbw-icon-sort-none-mask)}[data-icon=submenu-arrow]:empty:before{-webkit-mask-image:var(--tbw-icon-submenu-arrow-mask);mask-image:var(--tbw-icon-submenu-arrow-mask)}[data-icon=drag-handle]:empty:before{-webkit-mask-image:var(--tbw-icon-drag-handle-mask);mask-image:var(--tbw-icon-drag-handle-mask)}[data-icon=tool-panel]:empty:before{-webkit-mask-image:var(--tbw-icon-tool-panel-mask);mask-image:var(--tbw-icon-tool-panel-mask)}[data-icon=print]:empty:before{-webkit-mask-image:var(--tbw-icon-print-mask);mask-image:var(--tbw-icon-print-mask)}[data-icon=filter]:empty:before,[data-icon=filter-active]:empty:before{width:var(--tbw-icon-size, 1em);height:var(--tbw-icon-size, 1em);background:currentColor}[data-icon=filter]:empty:before{-webkit-mask-image:var(--tbw-icon-filter-mask);mask-image:var(--tbw-icon-filter-mask)}[data-icon=filter-active]:empty:before{-webkit-mask-image:var(--tbw-icon-filter-active-mask);mask-image:var(--tbw-icon-filter-active-mask)}}.tbw-context-menu [data-icon]:empty:before{display:inline-block;width:1em;height:1em;vertical-align:middle}.tbw-context-menu [data-icon=submenu-arrow]:empty:before{content:var(--tbw-icon-submenu-arrow, "▶")}}
@layer tbw-base{tbw-grid{.header{display:block;flex-shrink:0;z-index:var(--tbw-z-layer-header, 30);background:var(--tbw-color-header-bg);overflow:visible}.header-group-row{display:grid;grid-template-columns:var(--tbw-column-template);background:var(--tbw-color-header-bg);z-index:var(--tbw-z-layer-header, 30)}.header-group-cell{display:flex;align-items:center;justify-content:flex-start;padding:var(--tbw-cell-padding-header, 2px 8px);color:var(--tbw-color-header-group-fg, var(--tbw-color-header-fg));font-weight:var(--tbw-font-weight-header-group, var(--tbw-font-weight-header));justify-content:var(--tbw-align-header-group, var(--tbw-align-header, flex-start));&:not(:last-child){border-right:2px solid var(--tbw-color-border)}}.header-row{display:grid;grid-template-columns:var(--tbw-column-template);color:var(--tbw-color-header-fg);font-size:var(--tbw-font-size-header);min-height:var(--tbw-header-height);border-bottom:var(--tbw-border-header);z-index:var(--tbw-z-layer-header, 30);text-transform:var(--tbw-header-text-transform);letter-spacing:var(--tbw-header-letter-spacing);>.cell{display:flex;align-items:center;gap:4px;padding:var(--tbw-cell-padding-header, 2px 8px);background-color:var(--tbw-color-header-bg);font-weight:var(--tbw-font-weight-header);border-right:1px solid var(--tbw-color-border-cell);overflow:visible;min-width:0;>span:first-child{flex:1;min-width:0;overflow:hidden;text-overflow:ellipsis;white-space:nowrap;font-weight:inherit}>span[part~=sort-indicator]{flex-shrink:0;opacity:.6;color:var(--tbw-sort-indicator-color);display:var(--tbw-sort-indicator-display, inline-flex);visibility:var(--tbw-sort-indicator-visibility, visible);transition:opacity .15s,visibility 0s,display 0s allow-discrete;transition-behavior:allow-discrete}&:hover>span[part~=sort-indicator]{display:inline-flex;visibility:visible}&[aria-sort=ascending]>span[part~=sort-indicator],&[aria-sort=descending]>span[part~=sort-indicator]{display:inline-flex;visibility:visible;opacity:1;color:var(--tbw-sort-indicator-active-color)}&:last-child{border-right:0;.resize-handle{right:0;width:calc(var(--tbw-resize-hit-area, 18px) / 2)}}&.grouped.group-end:not(:last-child){border-right:2px solid var(--tbw-color-border)}&.resizable{position:relative}&.sticky-left,&.sticky-right{background:var(--tbw-color-header-bg);z-index:35}}}.header-group-row>.header-group-cell{&.sticky-left,&.sticky-right{background:var(--tbw-color-header-bg);z-index:35}}.sortable{cursor:pointer;user-select:none}.resize-handle{position:absolute;top:0;right:calc(var(--tbw-resize-hit-area, 18px) / -2);width:var(--tbw-resize-hit-area, 18px);height:100%;cursor:e-resize;user-select:none;touch-action:none;z-index:20;background:transparent;&:before{content:"";position:absolute;top:0;left:50%;transform:translate(-50%);width:var(--tbw-resize-handle-width);height:100%;background:var(--tbw-resize-handle-color);border-radius:var(--tbw-resize-handle-border-radius);transition:background .12s ease}&:after{content:"";position:absolute;top:100%;left:50%;transform:translate(-50%);width:var(--tbw-resize-handle-width);height:0;background:var(--tbw-resize-indicator-color, var(--tbw-color-accent));opacity:0;pointer-events:none;transition:opacity .12s ease,height 0s .12s;z-index:1000}&:hover{&:before{background:var(--tbw-resize-handle-color-hover)}&:after{height:100vh;opacity:var(--tbw-resize-indicator-opacity, .6);transition:opacity .12s ease,height 0s}}}}}
@layer tbw-base{tbw-grid{.data-grid-row{display:grid;grid-template-columns:var(--tbw-column-template);contain:layout style;&:nth-child(2n){background:var(--tbw-color-row-alt)}@media(hover:hover){&:hover{background:var(--tbw-color-row-hover)}}>.cell{display:block;padding:var(--tbw-cell-padding, 2px 8px);border-bottom:var(--tbw-row-divider);min-height:var(--tbw-row-height);align-content:center;border-right:1px solid var(--tbw-color-border-cell);overflow:hidden;min-width:0;white-space:var(--tbw-cell-white-space, nowrap);text-overflow:ellipsis;>*{overflow:hidden;text-overflow:ellipsis;white-space:inherit;min-width:0}&:last-child{border-right:0}&[data-type=boolean]{text-align:center;input[type=checkbox]{margin:0;width:var(--tbw-checkbox-size);height:var(--tbw-checkbox-size);vertical-align:middle}}&.selected:focus-visible,&:focus-visible:not(.cell-focus){outline:none}&.sticky-left,&.sticky-right{background:var(--tbw-color-panel-bg)}}}.selecting .data-grid-row>.cell{user-select:none}}}
@layer tbw-base{tbw-grid{.tbw-shell-header{display:flex;align-items:center;gap:8px;min-height:var(--tbw-shell-header-height);padding:0 8px;background:var(--tbw-shell-header-bg);border-bottom:1px solid var(--tbw-shell-header-border);flex-shrink:0}.tbw-shell-title{font-size:var(--tbw-shell-title-font-size);font-weight:var(--tbw-shell-title-font-weight);white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.tbw-shell-content{flex:1;display:flex;align-items:center;gap:12px;min-width:0;overflow:hidden}.tbw-shell-toolbar{display:flex;align-items:center;gap:var(--tbw-toolbar-button-gap);flex-shrink:0}.tbw-toolbar-btn{display:inline-flex;align-items:center;justify-content:center;width:var(--tbw-toolbar-button-size);height:var(--tbw-toolbar-button-size);padding:0;border:1px solid transparent;border-radius:var(--tbw-border-radius);background:transparent;color:var(--tbw-color-fg);cursor:pointer;font-size:16px;transition:background var(--tbw-transition-duration) var(--tbw-transition-ease),border-color var(--tbw-transition-duration) var(--tbw-transition-ease);&:hover{background:var(--tbw-color-row-hover)}&:focus-visible{outline:var(--tbw-focus-outline);outline-offset:var(--tbw-focus-outline-offset)}&.active{background:var(--tbw-focus-background);border-color:var(--tbw-color-accent)}&:disabled{opacity:.5;cursor:not-allowed}}.tbw-toolbar-separator{width:1px;height:20px;background:var(--tbw-color-border);margin:0 4px}.tbw-shell-body{position:relative;display:flex;flex:1;min-height:0;overflow:visible}.tbw-grid-content{flex:1;min-width:0;min-height:0;display:flex;flex-direction:row;overflow:hidden;touch-action:none}.tbw-scroll-area{flex:1;min-width:0;min-height:0;display:flex;flex-direction:column;overflow-x:auto;overflow-y:hidden;overflow-anchor:none}}}
@layer tbw-base{tbw-grid{.tbw-tool-panel{position:absolute;top:0;bottom:0;right:0;width:0;overflow:hidden;background:var(--tbw-tool-panel-bg);border-left:1px solid var(--tbw-tool-panel-border);transition:width var(--tbw-tool-panel-transition);display:flex;flex-direction:column;z-index:var(--tbw-z-layer-toolpanel, 31);box-shadow:-2px 0 8px var(--tbw-color-shadow);&[data-position=left]{right:auto;left:0;border-left:none;border-right:1px solid var(--tbw-tool-panel-border);box-shadow:2px 0 8px var(--tbw-color-shadow)}&.open{width:var(--tbw-tool-panel-width)}}.tbw-tool-panel-resize{position:absolute;top:0;bottom:0;width:6px;cursor:col-resize;background:transparent;z-index:10;transition:background var(--tbw-transition-duration) var(--tbw-transition-ease);&[data-handle-position=left]{left:0}&[data-handle-position=right]{right:0}&:hover,&.resizing{background:var(--tbw-color-accent)}}.tbw-tool-panel-header{display:flex;align-items:center;justify-content:space-between;min-height:var(--tbw-tool-panel-header-height);padding:0 12px;border-bottom:1px solid var(--tbw-tool-panel-border);flex-shrink:0}.tbw-tool-panel-title{font-weight:600;font-size:13px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.tbw-tool-panel-close{display:inline-flex;align-items:center;justify-content:center;width:24px;height:24px;padding:0;border:none;border-radius:var(--tbw-border-radius);background:transparent;color:var(--tbw-color-fg-muted);cursor:pointer;font-size:14px;&:hover{background:var(--tbw-color-row-hover);color:var(--tbw-color-fg)}}.tbw-tool-panel-content{flex:1;overflow:auto}.tbw-accordion{display:flex;flex-direction:column;gap:0}.tbw-accordion-section{border-bottom:1px solid var(--tbw-tool-panel-border);&:last-child{border-bottom:none}&.single .tbw-accordion-header{cursor:default;&:hover{background:transparent}}&.expanded{.tbw-accordion-chevron{transform:rotate(90deg)}.tbw-accordion-content{display:block}}}.tbw-accordion-header{display:flex;align-items:center;gap:8px;width:100%;padding:10px 12px;border:none;background:transparent;color:var(--tbw-color-fg);font-size:13px;font-weight:600;text-align:start;cursor:pointer;user-select:none;&:hover{background:var(--tbw-color-row-hover)}}.tbw-accordion-chevron{display:inline-flex;align-items:center;justify-content:center;width:16px;height:16px;font-size:10px;color:var(--tbw-color-fg-muted);transition:transform .15s ease;flex-shrink:0}.tbw-accordion-icon{display:inline-flex;align-items:center;justify-content:center;width:16px;height:16px;font-size:14px;flex-shrink:0}.tbw-accordion-title{flex:1;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}.tbw-accordion-content{display:none}}}
@layer tbw-base{.tbw-loading-overlay{position:absolute;inset:0;display:flex;align-items:center;justify-content:center;background:light-dark(rgba(255,255,255,.7),rgba(0,0,0,.5));z-index:1000;pointer-events:all;animation:tbw-fade-in .15s ease-out}.tbw-spinner{--tbw-spinner-size: 48px;--tbw-spinner-border-width: 3px;--tbw-spinner-color: var(--tbw-color-accent);--tbw-spinner-track-color: var(--tbw-color-border);width:var(--tbw-spinner-size);height:var(--tbw-spinner-size);border:var(--tbw-spinner-border-width) solid var(--tbw-spinner-track-color);border-top-color:var(--tbw-spinner-color);border-radius:50%;animation:tbw-spin .8s linear infinite}.tbw-spinner--large{--tbw-spinner-size: min(48px, calc(100% - 16px) )}.tbw-spinner--small{--tbw-spinner-size: calc(var(--tbw-row-height, 28px) * .6);--tbw-spinner-border-width: 2px}.data-grid-row.tbw-row-loading{position:relative;pointer-events:none}.tbw-row-loading-overlay{position:absolute;inset:0;background:light-dark(rgba(255,255,255,.7),rgba(0,0,0,.5));z-index:26;animation:tbw-fade-in .15s ease-out;pointer-events:none;display:flex;align-items:center}.tbw-row-loading-spinner{--_spinner-size: calc(var(--tbw-row-height, 28px) * .6);margin-left:var(--tbw-spacing-md);width:var(--_spinner-size);height:var(--_spinner-size);border:2px solid var(--tbw-spinner-track-color, var(--tbw-color-border));border-top-color:var(--tbw-spinner-color, var(--tbw-color-accent));border-radius:50%;animation:tbw-spin .8s linear infinite}.cell.tbw-cell-loading{position:relative;pointer-events:none;&:before{content:"";position:absolute;inset:0;background:light-dark(rgba(255,255,255,.7),rgba(0,0,0,.5));z-index:26;animation:tbw-fade-in .15s ease-out}&:after{--_spinner-size: calc(var(--tbw-row-height, 28px) * .5);content:"";position:absolute;left:var(--tbw-spacing-sm);top:0;bottom:0;margin:auto 0;width:var(--_spinner-size);height:var(--_spinner-size);border:2px solid var(--tbw-spinner-track-color, var(--tbw-color-border));border-top-color:var(--tbw-spinner-color, var(--tbw-color-accent));border-radius:50%;animation:tbw-spin .8s linear infinite;z-index:27}}}
@layer tbw-base{tbw-grid{.tbw-expanding{animation:tbw-expand var(--tbw-animation-duration) var(--tbw-animation-easing) forwards;overflow:hidden}.tbw-collapsing{animation:tbw-collapse var(--tbw-animation-duration) var(--tbw-animation-easing) forwards;overflow:hidden}&[data-animation-mode=off]{--tbw-animation-enabled: 0;--tbw-animation-duration: 0ms;.data-grid-row[data-animating]{animation:none}}}tbw-grid .data-grid-row[data-animating=change]{animation:tbw-row-change var(--tbw-row-change-duration) ease-out}tbw-grid .data-grid-row[data-animating=insert]{animation:tbw-row-insert var(--tbw-row-insert-duration) ease-out;will-change:max-height,opacity}tbw-grid .data-grid-row[data-animating=remove]{animation:tbw-row-remove var(--tbw-row-remove-duration) ease-out forwards;will-change:max-height,opacity,transform;pointer-events:none}}@keyframes tbw-expand{0%{opacity:0;max-height:0;transform:translateY(-8px)}to{opacity:1;max-height:500px;transform:translateY(0)}}@keyframes tbw-collapse{0%{opacity:1;max-height:500px;transform:translateY(0)}to{opacity:0;max-height:0;transform:translateY(-8px)}}@keyframes tbw-row-change{0%{background-color:transparent}20%{background-color:var(--tbw-row-change-color)}to{background-color:transparent}}@keyframes tbw-row-insert{0%{opacity:0;max-height:0;overflow:hidden}to{opacity:1;max-height:var(--tbw-row-height, 28px);overflow:hidden}}@keyframes tbw-row-remove{0%{opacity:1;transform:translateY(0);max-height:var(--tbw-row-height, 28px)}to{opacity:0;max-height:0;transform:translateY(-8px)}}@keyframes tbw-spin{to{transform:rotate(360deg)}}@keyframes tbw-fade-in{0%{opacity:0}to{opacity:1}}
@layer tbw-base{@media(forced-colors:active){tbw-grid{--tbw-color-border: CanvasText;--tbw-color-border-strong: CanvasText;--tbw-color-border-cell: CanvasText;--tbw-color-border-header: CanvasText;--tbw-color-fg: CanvasText;--tbw-color-bg: Canvas;--tbw-color-panel-bg: Canvas;--tbw-color-header-bg: Canvas;--tbw-color-header-fg: CanvasText;--tbw-color-accent: Highlight;--tbw-color-accent-fg: HighlightText;--tbw-color-selection: Highlight;--tbw-color-row-hover: Highlight;--tbw-focus-outline: 2px solid Highlight;--tbw-range-border-color: Highlight;.cell:focus,.cell.active-cell{outline:2px solid Highlight!important;outline-offset:-2px}.data-grid-row[aria-selected=true]{background:Highlight!important;color:HighlightText!important}}}@media(prefers-reduced-motion:reduce){tbw-grid[data-animation-mode=reduced-motion]{--tbw-animation-enabled: 0;--tbw-animation-duration: 0ms;.data-grid-row[data-animating]{animation:none}}}}
`);
  }
  getPlugin(e2) {
    return this.#le?.getPlugin(e2);
  }
  getPluginByName(e2) {
    return this.#le?.getPluginByName(e2);
  }
  requestRender() {
    this.#Y.requestPhase(_e.ROWS, "plugin:requestRender");
  }
  requestColumnsRender() {
    this.#Y.requestPhase(_e.COLUMNS, "plugin:requestColumnsRender");
  }
  requestRenderWithFocus() {
    this._restoreFocusAfterRender = true, this.#Y.requestPhase(_e.ROWS, "plugin:requestRenderWithFocus");
  }
  updateTemplate() {
    I(this);
  }
  requestAfterRender() {
    this.#Y.requestPhase(_e.STYLE, "plugin:requestAfterRender");
  }
  requestVirtualRefresh() {
    this._virtualization.start = -1, this.refreshVirtualWindow(false);
  }
  #Le() {
    this.#le = new no(this);
    const e2 = this.#m?.plugins, t2 = Array.isArray(e2) ? e2 : [], o2 = this.#m?.features;
    let i2 = [];
    o2 && Qe && (i2 = Qe(o2));
    const n2 = i2.length > 0 ? [...i2, ...t2] : t2;
    this.#le.attachAll(n2);
  }
  #Oe() {
    !(function(e2) {
      let t2 = false;
      for (const { name: o2, styles: i2 } of e2) Nt.has(o2) || (Nt.set(o2, i2), t2 = true);
      t2 && qt();
    })(this.#le?.getPluginStyles() ?? []);
  }
  #De() {
    const e2 = this.#m?.plugins, t2 = Array.isArray(e2) ? e2 : [], o2 = this.#m?.features ?? void 0, i2 = o2 !== this.#ce;
    if ((this.#ae === t2 || void 0 !== this.#ae && this.#ae.length === t2.length && this.#ae.every((e3, o3) => e3 === t2[o3])) && !i2) return void (this.#ae = t2);
    this.#le && this.#le.detachAll();
    for (const r2 of this.#we.toolPanels.keys()) {
      const e3 = this.#we.lightDomToolPanelIds.has(r2), t3 = this.#we.apiToolPanelIds.has(r2);
      if (!e3 && !t3) {
        const e4 = this.#we.panelCleanups.get(r2);
        e4 && (e4(), this.#we.panelCleanups.delete(r2)), this.#we.toolPanels.delete(r2);
      }
    }
    for (const r2 of this.#we.headerContents.keys()) {
      if (this.#we.apiHeaderContentIds.has(r2)) continue;
      const e3 = this.#we.headerContentCleanups.get(r2);
      e3 && (e3(), this.#we.headerContentCleanups.delete(r2)), this.#we.headerContents.delete(r2);
    }
    this.#Le(), this.#Oe(), this.#ae = t2, this.#ce = o2, this.#Ie(), this.#ze();
    const n2 = this.#Z;
    if (this.#Z = this.#le?.getAll().some((e3) => e3.onScroll) ?? false, !n2 && this.#Z) {
      const e3 = this.#W.querySelector(".tbw-grid-content") ?? this.#W.querySelector(".tbw-grid-root");
      this.#Ne(e3);
    }
  }
  #qe() {
    this.#le?.detachAll();
  }
  #ze() {
    if (!this.#le) return;
    const e2 = this.#le.getToolPanels();
    for (const { panel: o2 } of e2) this.#we.toolPanels.has(o2.id) || this.#we.toolPanels.set(o2.id, o2);
    const t2 = this.#le.getHeaderContents();
    for (const { content: o2 } of t2) this.#we.headerContents.has(o2.id) || this.#we.headerContents.set(o2.id, o2);
  }
  #$e() {
    const e2 = _ro.getAdapters();
    if (0 === e2.length && !this.__frameworkAdapter) return;
    const t2 = this.__frameworkAdapter;
    return (o2) => {
      if (t2?.createToolPanelRenderer) {
        const e3 = t2.createToolPanelRenderer(o2);
        if (e3) return e3;
      }
      for (const t3 of e2) if (t3.createToolPanelRenderer) {
        const e3 = t3.createToolPanelRenderer(o2);
        if (e3) return e3;
      }
    };
  }
  connectedCallback() {
    this.hasAttribute("tabindex") || (this.tabIndex = 0), this.hasAttribute("version") || this.setAttribute("version", _ro.version), this.id || (this.id = "tbw-grid-" + ++_ro.#$), this._rows = Array.isArray(this.#B) ? [...this.#B] : [], this.#oe && (this.#oe.abort(), this.#ge = false), this.#oe = new AbortController(), this.#re && (ot(this.#re), this.#re = void 0), this.#We(), this.#V.parseLightDomColumns(this), this.#V.merge(), this.#Le();
    const e2 = this.#m?.plugins;
    var t2, o2;
    this.#ae = Array.isArray(e2) ? e2 : [], this.#ce = this.#m?.features ?? void 0, this.#ze(), this.#F || (this.#Fe(), this.#Oe(), this.#F = true), this.#Be(), this.#re = (t2 = () => {
      this.#Ve();
    }, o2 = { timeout: 100 }, tt ? requestIdleCallback(t2, o2) : window.setTimeout(() => {
      const e3 = Date.now();
      t2({ didTimeout: false, timeRemaining: () => Math.max(0, 50 - (Date.now() - e3)) });
    }, 1));
  }
  disconnectedCallback() {
    this.#re && (ot(this.#re), this.#re = void 0), this.#J && (clearTimeout(this.#J), this.#J = 0), this.#qe(), (function(e2) {
      for (const t2 of e2.headerContentCleanups.values()) t2();
      e2.headerContentCleanups.clear();
      for (const t2 of e2.panelCleanups.values()) t2();
      e2.panelCleanups.clear();
      for (const t2 of e2.toolbarContentCleanups.values()) t2();
      e2.toolbarContentCleanups.clear();
      for (const t2 of e2.toolbarContents.values()) t2.onDestroy?.();
      if (e2.isPanelOpen) for (const t2 of e2.expandedSections) {
        const o2 = e2.toolPanels.get(t2);
        o2?.onClose?.();
      }
      e2.isPanelOpen = false, e2.expandedSections.clear(), e2.toolPanels.clear(), e2.headerContents.clear(), e2.toolbarContents.clear(), e2.lightDomHeaderContent = [], e2.lightDomToolPanelIds.clear(), e2.lightDomToolbarContentIds.clear(), e2.lightDomContentMoved = false;
    })(this.#we), this.#me.setInitialized(false), this.#be?.(), this.#be = void 0, this.#ve?.(), this.#ve = void 0, Ft(this.#te), this.#oe && (this.#oe.abort(), this.#oe = void 0), this.#fe?.abort(), this.#fe = void 0, this.#ge = false, this._resizeController && this._resizeController.dispose(), this.#ie && (this.#ie.disconnect(), this.#ie = void 0), this.#ne && (this.#ne.disconnect(), this.#ne = void 0, this.#Ue = false), We(this), this.#Ge.clear(), this._virtualization.heightCache?.byKey.clear(), this.#ae = void 0, this.#ce = void 0;
    for (let e2 = 0; e2 < this._rowPool.length; e2++) this._rowPool[e2].remove();
    this._rowPool.length = 0, this.__rowsBodyEl = null, this.#U = false;
  }
  attributeChangedCallback(e2, t2, o2) {
    if ("loading" === e2) {
      const e3 = null !== o2 && "false" !== o2;
      return void (this.loading !== e3 && (this.loading = e3));
    }
    if (t2 !== o2 && o2 && "null" !== o2 && "undefined" !== o2) if ("rows" === e2 || "columns" === e2 || "grid-config" === e2) try {
      const t3 = JSON.parse(o2);
      "rows" === e2 ? this.rows = t3 : "columns" === e2 ? this.columns = t3 : "grid-config" === e2 && (this.gridConfig = t3);
    } catch {
      A("TBW130", `Invalid JSON for '${e2}' attribute: ${o2}`, this.id);
    }
    else "fit-mode" === e2 && (this.fitMode = o2);
  }
  #Be() {
    const e2 = this.#W.querySelector(".tbw-grid-content") ?? this.#W.querySelector(".tbw-grid-root");
    if (this._headerRowEl = e2?.querySelector(".header-row"), this._virtualization.totalHeightEl = e2?.querySelector(".faux-vscroll-spacer"), this._virtualization.viewportEl = e2?.querySelector(".rows-viewport"), this._bodyEl = e2?.querySelector(".rows"), this.__rowsBodyEl = e2?.querySelector(".rows-body"), this.#me.isInitialized) {
      At(this.#W, this.#we), Tt(this.#W, this.#m?.shell, this.#we);
      const e3 = this.#m?.shell?.toolPanel?.defaultOpen;
      e3 && this.#we.toolPanels.has(e3) && (this.openToolPanel(), this.#we.expandedSections.add(e3)), this.#we.isPanelOpen && (Mt(this.#W, this.#we), Pt(this.#W, this.#we, (this.#m, this.#m)), Ht(this.#W, this.#we));
    }
    if (this.setAttribute("data-upgraded", ""), this.#U = true, this._resizeController = rt(this), this.#je(), this.#Ne(e2), this.#ge) return;
    this.#ge = true;
    const t2 = this.disconnectSignal;
    Ze(this, this, this.#W, t2), this.#Ie(), queueMicrotask(() => this.#Ye()), this.#Y.requestPhase(_e.FULL, "afterConnect");
  }
  #Ie() {
    const e2 = this.#m.rowHeight, t2 = this.#le.hasRowHeightPlugin();
    "function" == typeof e2 || t2 ? this._virtualization.variableHeights || (this._virtualization.variableHeights = true, this._virtualization.rowHeight = "number" == typeof e2 && e2 > 0 ? e2 : this._virtualization.rowHeight || 28, this.#de.initializePositionCache(), "function" != typeof e2 && (this.#Q = true)) : !t2 && "function" != typeof e2 && this._virtualization.variableHeights ? (this._virtualization.variableHeights = false, this._virtualization.positionCache = null) : "number" == typeof e2 && e2 > 0 ? (this._virtualization.rowHeight = e2, this._virtualization.variableHeights = false) : requestAnimationFrame(() => this.#Xe());
  }
  #Xe() {
    if (this.#le.hasRowHeightPlugin()) return;
    const e2 = this._bodyEl?.querySelector(".data-grid-row");
    if (!e2) return;
    if (e2.style.getPropertyValue("--tbw-row-height")) return;
    const t2 = this.#Ke(), o2 = e2.querySelectorAll(".cell");
    let i2 = 0;
    o2.forEach((e3) => {
      const t3 = e3.offsetHeight;
      t3 > i2 && (i2 = t3);
    });
    const n2 = e2.getBoundingClientRect(), r2 = Math.max(n2.height, i2), s2 = this._virtualization.rowHeight, l2 = t2 > 0 && Math.abs(t2 - s2) > 1;
    (l2 || r2 > 0 && r2 - s2 > 1) && (this._virtualization.rowHeight = l2 ? Math.max(t2, r2) : r2, this.#Y.requestPhase(_e.VIRTUALIZATION, "measureRowHeight"));
  }
  #Ke() {
    const e2 = getComputedStyle(this).getPropertyValue("--tbw-row-height").trim();
    if (!e2) return 0;
    if (e2.endsWith("px")) return parseFloat(e2) || 0;
    const t2 = this._bodyEl?.querySelector('.data-grid-row:not([style*="--tbw-row-height"]) > .cell');
    if (!t2) return 0;
    const o2 = parseFloat(getComputedStyle(t2).minHeight);
    return o2 > 0 ? o2 : 0;
  }
  #Ze() {
    const e2 = this._bodyEl?.querySelector(".data-grid-row");
    if (!e2) return;
    const t2 = e2.querySelectorAll(".cell");
    let o2 = 0;
    t2.forEach((e3) => {
      const t3 = e3.offsetHeight;
      t3 > o2 && (o2 = t3);
    });
    const i2 = e2.getBoundingClientRect(), n2 = Math.max(i2.height, o2);
    if (n2 > 0) {
      if (Math.abs(n2 - this._virtualization.rowHeight) > 1 && (this._virtualization.rowHeight = n2), this.#de.initializePositionCache(), this._virtualization.totalHeightEl) {
        const e3 = this.#de.calculateTotalSpacerHeight(this._rows.length);
        this._virtualization.totalHeightEl.style.height = `${e3}px`;
      }
    }
  }
  #Ne(e2) {
    this.#fe?.abort(), this.#fe = new AbortController();
    const t2 = this.#fe.signal, o2 = e2?.querySelector(".faux-vscroll"), i2 = e2?.querySelector(".rows");
    if (this._virtualization.container = o2 ?? this, this.#Z = this.#le?.getAll().some((e3) => e3.onScroll) ?? false, o2 && i2) {
      o2.addEventListener("scroll", () => {
        if (!this._virtualization.enabled && !this.#Z) return;
        const e4 = o2.scrollTop, t3 = this._virtualization.rowHeight;
        if (this._rows.length <= this._virtualization.bypassThreshold) i2.style.transform = `translateY(${-e4}px)`;
        else {
          const o3 = this._virtualization.positionCache;
          let n4, r4;
          if (this._virtualization.variableHeights && o3 && o3.length > 0) {
            n4 = Jt(o3, e4), -1 === n4 && (n4 = 0);
            const i3 = n4 - n4 % 2;
            r4 = o3[i3]?.offset ?? i3 * t3;
          } else {
            n4 = Math.floor(e4 / t3);
            r4 = (n4 - n4 % 2) * t3;
          }
          const s3 = -(e4 - r4);
          i2.style.transform = `translateY(${s3}px)`;
        }
        this.#K = e4, this.#X || (this.#X = requestAnimationFrame(() => {
          this.#X = 0, null !== this.#K && (this.#Qe(this.#K), this.#K = null);
        }));
      }, { passive: true, signal: t2 });
      const e3 = this.#W.querySelector(".tbw-scroll-area");
      this.#pe = e3, this._virtualization.scrollAreaEl = e3, e3 && this.#Z && e3.addEventListener("scroll", () => {
        const t3 = this.#se;
        t3.scrollTop = o2.scrollTop, t3.scrollLeft = e3.scrollLeft, t3.scrollHeight = o2.scrollHeight, t3.scrollWidth = e3.scrollWidth, t3.clientHeight = o2.clientHeight, t3.clientWidth = e3.clientWidth, this.#le?.onScroll(t3);
      }, { passive: true, signal: t2 });
      const n3 = this.#W.querySelector(".tbw-grid-content"), r3 = this.#pe;
      n3 && (n3.addEventListener("wheel", (e4) => {
        try {
          if (n3.querySelector("select:open")) return;
        } catch {
        }
        const t3 = e4.shiftKey || Math.abs(e4.deltaX) > Math.abs(e4.deltaY);
        if (t3 && r3) {
          const t4 = e4.shiftKey ? e4.deltaY : e4.deltaX, { scrollLeft: o3, scrollWidth: i3, clientWidth: n4 } = r3;
          (t4 > 0 && o3 < i3 - n4 || t4 < 0 && o3 > 0) && (e4.preventDefault(), r3.scrollLeft += t4);
        } else if (!t3) {
          const { scrollTop: t4, scrollHeight: i3, clientHeight: n4 } = o2;
          (e4.deltaY > 0 && t4 < i3 - n4 || e4.deltaY < 0 && t4 > 0) && (e4.preventDefault(), o2.scrollTop += e4.deltaY);
        }
      }, { passive: false, signal: t2 }), Vt(n3, this.#te, { fauxScrollbar: o2, scrollArea: r3 }, t2));
    }
    var n2, r2, s2;
    this._bodyEl && (n2 = this, r2 = this._bodyEl, s2 = t2, r2.addEventListener("mousedown", (e3) => {
      const t3 = e3.target.closest(".cell[data-col]");
      if (!t3) return;
      if (t3.classList.contains(z.EDITING)) return;
      const o3 = e3.target;
      o3.draggable || o3.closest('[draggable="true"]') || e3.preventDefault(), Xe(n2, t3);
    }, { signal: s2 }), r2.addEventListener("click", (e3) => {
      const t3 = e3.target.closest(".data-grid-row");
      if (t3 && Ve(n2, e3, t3), !document.activeElement?.closest(`.cell.${z.EDITING}`)) {
        const t4 = e3.target.closest("tbw-grid");
        t4 && t4.focus({ preventScroll: true });
      }
    }, { signal: s2 }), r2.addEventListener("dblclick", (e3) => {
      const t3 = e3.target.closest(".data-grid-row");
      t3 && Ve(n2, e3, t3);
    }, { signal: s2 })), this.#ie?.disconnect(), this._virtualization.viewportEl && (this.#ie = new ResizeObserver(() => {
      this.#Je(), this.#Y.requestPhase(_e.VIRTUALIZATION, "resize-observer");
    }), this.#ie.observe(this._virtualization.viewportEl)), this.#W.addEventListener("focusin", () => {
      this.dataset.hasFocus = "";
    }, { signal: t2 }), this.#W.addEventListener("focusout", (e3) => {
      const t3 = e3.relatedTarget;
      t3 && (this.#W.contains(t3) || this.#he.isInExternalFocusContainer(t3)) || delete this.dataset.hasFocus;
    }, { signal: t2 });
  }
  #Ue = false;
  #et() {
    if (this.#Ue) return;
    const e2 = this._bodyEl?.querySelector(".data-grid-row");
    e2 && (this.#Ue = true, this.#ne?.disconnect(), this.#ne = new ResizeObserver(() => {
      this.#Xe();
    }), this.#ne.observe(e2));
  }
  addEventListener(e2, t2, o2) {
    super.addEventListener(e2, t2, o2);
  }
  removeEventListener(e2, t2, o2) {
    super.removeEventListener(e2, t2, o2);
  }
  on(e2, t2) {
    const o2 = (e3) => {
      t2(e3.detail, e3);
    };
    return this.addEventListener(e2, o2), () => this.removeEventListener(e2, o2);
  }
  #tt(e2, t2) {
    this.#U && this.dispatchEvent(new CustomEvent(e2, { detail: t2, bubbles: true, composed: true }));
  }
  _emitDataChange() {
    this.#tt("data-change", { rowCount: this._rows.length, sourceRowCount: this.#B.length });
  }
  #Ye() {
    const e2 = this._bodyEl?.querySelectorAll(".data-grid-row");
    e2?.forEach((e3, t2) => {
      const o2 = t2 === this._focusRow;
      e3.setAttribute("aria-selected", String(o2)), e3.querySelectorAll(".cell").forEach((e4, t3) => {
        e4.setAttribute("aria-selected", String(o2 && t3 === this._focusCol));
      });
    });
  }
  #Ae(e2) {
    this.#j[e2] = true, this.#G || (this.#G = true, queueMicrotask(() => this.#ot()));
  }
  #ot() {
    if (!this.#G || !this.#U) return void (this.#G = false);
    const e2 = this.#j;
    if (this.#G = false, this.#j = { rows: false, columns: false, gridConfig: false, fitMode: false }, e2.gridConfig) return this.#it(), void (e2.rows && this.#nt());
    e2.columns && this.#rt(), e2.rows && this.#nt(), e2.fitMode && this.#st();
  }
  #nt() {
    this._rows = Array.isArray(this.#B) ? [...this.#B] : [], this.#Ee = true, this.#Y.requestPhase(_e.ROWS, "applyRowsUpdate");
  }
  #lt() {
    this.#Ee && this._rebuildRowIdMap();
  }
  _rebuildRowIdMap() {
    this.#Ee = false, this.#Se.clear();
    const e2 = this.#m.getRowId;
    for (let t2 = 0; t2 < this._rows.length; t2++) {
      const o2 = ht(this._rows[t2], e2);
      void 0 !== o2 && this.#Se.set(o2, { row: this._rows[t2], index: t2 });
    }
  }
  #rt() {
    We(this), this.#V.merge(), this.#je();
  }
  #st() {
    this.#V.merge();
    if ("fixed" === this.#m.fitMode) this.__didInitialAutoSize = false, L(this);
    else {
      for (let e2 = 0; e2 < this._columns.length; e2++) {
        const t2 = this._columns[e2];
        !t2.__userResized && t2.__autoSized && delete t2.width;
      }
      I(this);
    }
  }
  #it() {
    St(this, this.#we), Et(this, this.#we);
    const e2 = !!this.#W.querySelector(".has-shell"), t2 = !!this.#W.querySelector(".tbw-tool-panel"), o2 = this.#W.querySelectorAll(".tbw-accordion-section").length, i2 = this.#W.querySelector(".tbw-tool-panel")?.dataset.position ?? "right";
    this.#V.parseLightDomColumns(this), this.#V.merge(), this.#De(), this.#Ie(), xt(this, this.#we, this.#$e()), this.#V.markSourcesChanged(), this.#V.merge();
    const n2 = Rt(this.#m?.shell), r2 = (this.#m?.shell?.toolPanels?.length ?? 0) > 0, s2 = this.#m?.shell?.toolPanels?.length ?? 0, l2 = this.#m?.shell?.toolPanel?.position ?? "right";
    if (e2 !== n2 || !t2 && r2 || t2 && s2 !== o2 || t2 && i2 !== l2) return kt(this.#we), this.#Fe(), this.#Oe(), this.#Be(), void this._rebuildRowIdMap();
    e2 && this.#at(), this.#we.isPanelOpen && Pt(this.#W, this.#we, (this.#m, this.#m)), this._rebuildRowIdMap(), this.#Y.requestPhase(_e.COLUMNS, "applyGridConfigUpdate");
  }
  #at() {
    const e2 = this.#W.querySelector(".tbw-shell-header");
    if (!e2) return;
    const t2 = this.#m.shell?.header?.title ?? this.#we.lightDomTitle;
    let o2 = e2.querySelector(".tbw-shell-title");
    t2 ? (o2 || (o2 = document.createElement("h2"), o2.className = "tbw-shell-title", o2.setAttribute("part", "shell-title"), e2.insertBefore(o2, e2.firstChild)), o2.textContent = t2) : o2 && o2.remove();
  }
  #ct() {
    if (this.__rowRenderEpoch++, this.#le) {
      const e2 = this.#xe.length > 0 ? this.#xe : this._columns, t2 = e2.filter((e3) => !e3.hidden), o2 = e2.filter((e3) => e3.hidden), i2 = this.#le.processColumns([...t2]);
      if (i2 !== t2) {
        const n2 = new Set(i2.map((e3) => e3.field));
        !t2.some((e3) => n2.has(e3.field)) && i2.length > 0 ? this._columns = [...i2, ...o2] : this._columns = this.#dt(e2, i2, o2);
      } else this._columns = [...e2];
    }
  }
  #dt(e2, t2, o2) {
    if (0 === o2.length) return t2;
    const i2 = /* @__PURE__ */ new Map();
    for (const l2 of t2) i2.set(l2.field, l2);
    const n2 = new Set(e2.map((e3) => e3.field)), r2 = [];
    for (const l2 of t2) n2.has(l2.field) || r2.push(l2);
    const s2 = [];
    for (const l2 of e2) {
      const e3 = i2.get(l2.field);
      e3 ? s2.push(e3) : l2.hidden && s2.push(l2);
    }
    return s2.push(...r2), s2;
  }
  #ht() {
    We(this);
    const e2 = this.#B;
    if (!Array.isArray(e2)) return this._rows = [], this._rebuildRowIdMap(), this._virtualization.variableHeights && this.#de.initializePositionCache(), void this._emitDataChange();
    if (!this._sortState && this.#m.initialSort) {
      const { field: e3, direction: t3 } = this.#m.initialSort;
      this._columns.find((t4) => t4.field === e3) && (this._sortState = { field: e3, direction: "desc" === t3 ? -1 : 1 });
    }
    this._sortState && (this.__originalOrder = this._rows);
    const t2 = (function(e3, t3, o3 = false) {
      if (!e3._sortState) return t3;
      const i2 = e3.effectiveConfig?.sortHandler ?? re;
      if (i2 === re) return o3 || (e3.__originalOrder = t3.slice()), le(t3, e3._sortState.field, e3._sortState.direction, e3._columns), t3;
      o3 || (e3.__originalOrder = t3);
      const n2 = i2(t3, e3._sortState, e3._columns);
      return n2 && "function" == typeof n2.then ? t3 : n2;
    })(this, this._sortState ? [...e2] : e2, true), o2 = this.#le?.processRows(t2) ?? t2;
    this._rows = o2, this._rebuildRowIdMap(), this._emitDataChange();
  }
  #ut(e2) {
    const t2 = { ...o, ...e2.animation }, i2 = t2.mode ?? "reduced-motion";
    let n2 = 1;
    false === i2 || "off" === i2 ? n2 = 0 : true !== i2 && "on" !== i2 || (n2 = 1), this.style.setProperty("--tbw-animation-duration", `${t2.duration}ms`), this.style.setProperty("--tbw-animation-easing", t2.easing ?? "ease-out"), this.style.setProperty("--tbw-animation-enabled", String(n2)), this.dataset.animationMode = "boolean" == typeof i2 ? i2 ? "on" : "off" : i2;
  }
  _renderVisibleRows(e2, t2, o2 = this.__rowRenderEpoch) {
    if (this.#ee || (this.#ee = (e3, t3, o3) => this.#le?.renderRow(e3, t3, o3) ?? false), (function(e3, t3, o3, i2, n2) {
      const r2 = Math.max(0, o3 - t3), s2 = e3._bodyEl, l2 = e3._visibleColumns, a2 = l2.length;
      let c2 = e3.__cachedHeaderRowCount;
      for (void 0 === c2 && (c2 = e3.querySelector(".header-group-row") ? 2 : 1, e3.__cachedHeaderRowCount = c2); e3._rowPool.length < r2; ) {
        const t4 = $e();
        e3._rowPool.push(t4);
      }
      if (e3._rowPool.length > r2) {
        for (let t4 = r2; t4 < e3._rowPool.length; t4++) {
          const o4 = e3._rowPool[t4];
          o4.parentNode === s2 && o4.remove();
        }
        e3._rowPool.length = r2;
      }
      const d2 = n2 && false !== e3.__hasRenderRowPlugins, h2 = e3._hasAfterRowRenderHook?.() ?? false, u2 = e3._virtualization?.variableHeights && "function" == typeof e3.effectiveConfig?.rowHeight ? e3.effectiveConfig.rowHeight : null;
      for (let f2 = 0; f2 < r2; f2++) {
        const o4 = t3 + f2, r3 = e3._rows[o4], p2 = e3._rowPool[f2];
        if (p2.setAttribute("aria-rowindex", String(o4 + c2 + 1)), d2 && n2(r3, p2, o4)) {
          p2.__epoch = i2, p2.__rowDataRef = r3, p2.parentNode !== s2 && s2.appendChild(p2);
          continue;
        }
        const w2 = p2.__epoch, m2 = p2.__rowDataRef;
        let b2 = p2.children.length;
        b2 > a2 && p2.lastElementChild?.classList.contains("tbw-row-loading-overlay") && b2--;
        const v2 = w2 === i2 && b2 === a2, C2 = m2 !== r3, _2 = !!e3._isGridEditMode;
        let y2 = false;
        if (v2 && C2) {
          for (let e4 = 0; e4 < a2; e4++) if (l2[e4].externalView && !p2.querySelector(`.cell[data-col="${e4}"] [data-external-view]`)) {
            y2 = true;
            break;
          }
        }
        if (!v2 || y2) {
          const t4 = De(p2), n3 = _2 && !C2 || e3._activeEditRows === o4;
          t4 && !n3 ? (p2.__isCustomRow && (p2.className = "data-grid-row", p2.setAttribute("role", "row"), p2.__isCustomRow = false), Ie(p2), Be(e3, p2, r3, o4), p2.__epoch = i2, p2.__rowDataRef = r3) : t4 && n3 ? (Fe(e3, p2, r3, o4), p2.__rowDataRef = r3) : (p2.__isCustomRow && (p2.className = "data-grid-row", p2.setAttribute("role", "row"), p2.__isCustomRow = false), Be(e3, p2, r3, o4), p2.__epoch = i2, p2.__rowDataRef = r3);
        } else if (C2) {
          const t4 = De(p2), n3 = e3._activeEditRows === o4;
          t4 && !n3 ? (Ie(p2), Be(e3, p2, r3, o4), p2.__epoch = i2, p2.__rowDataRef = r3) : (Fe(e3, p2, r3, o4), p2.__rowDataRef = r3);
        } else {
          const t4 = De(p2), n3 = _2 || e3._activeEditRows === o4;
          t4 && !n3 ? (Ie(p2), Be(e3, p2, r3, o4), p2.__epoch = i2, p2.__rowDataRef = r3) : Fe(e3, p2, r3, o4);
        }
        let R2 = false;
        const S2 = e3._changedRowIdSet;
        if (S2 && S2.size > 0) try {
          const t4 = e3.getRowId?.(r3);
          t4 && (R2 = S2.has(t4));
        } catch {
        }
        R2 !== p2.classList.contains("changed") && p2.classList.toggle("changed", R2);
        const E2 = e3.effectiveConfig?.rowClass;
        if (E2) {
          const t4 = p2.getAttribute("data-dynamic-classes");
          if (t4) {
            const e4 = t4.split(" ");
            for (let t5 = 0; t5 < e4.length; t5++) e4[t5] && p2.classList.remove(e4[t5]);
          }
          try {
            const e4 = E2(r3), t5 = "string" == typeof e4 ? e4.split(/\s+/) : e4;
            if (t5 && t5.length > 0) {
              let e5 = "";
              for (let o5 = 0; o5 < t5.length; o5++) {
                const i3 = t5[o5];
                i3 && "string" == typeof i3 && (p2.classList.add(i3), e5 += (e5 ? " " : "") + i3);
              }
              p2.setAttribute("data-dynamic-classes", e5);
            } else p2.removeAttribute("data-dynamic-classes");
          } catch (g2) {
            A("TBW060", `rowClass callback error: ${g2}`, e3.id), p2.removeAttribute("data-dynamic-classes");
          }
        }
        if (u2) {
          const e4 = u2(r3, o4);
          void 0 !== e4 && e4 > 0 ? p2.style.setProperty("--tbw-row-height", `${e4}px`) : p2.style.removeProperty("--tbw-row-height");
        }
        h2 && e3._afterRowRender?.({ row: r3, rowIndex: o4, rowElement: p2 }), p2.parentNode !== s2 && s2.appendChild(p2);
      }
    })(this, e2, t2, o2, this.#ee), this.#_e.size > 0) for (const i2 of this.#_e) this.#He(i2, true);
  }
  #gt = { rowCount: -1, colCount: -1, ariaLabel: void 0, ariaDescribedBy: void 0 };
  _updateAriaCounts(e2, t2) {
    !(function(e3, t3, o2, i2, n2) {
      if (i2 === e3.rowCount && n2 === e3.colCount) return false;
      const r2 = e3.rowCount;
      e3.rowCount = i2, e3.colCount = n2, t3 && (t3.setAttribute("aria-rowcount", String(i2)), t3.setAttribute("aria-colcount", String(n2))), i2 !== r2 && o2 && (i2 > 0 ? o2.setAttribute("role", "rowgroup") : o2.removeAttribute("role"));
    })(this.#gt, this.__rowsBodyEl, this._bodyEl, e2, t2);
  }
  _requestSchedulerPhase(e2, t2) {
    this.#Y.requestPhase(e2, t2);
  }
  _getPluginRowHeight(e2, t2) {
    return this.#le?.getRowHeight?.(e2, t2);
  }
  _adjustPluginVirtualStart(e2, t2, o2) {
    return this.#le?.adjustVirtualStart(e2, t2, o2);
  }
  _afterPluginRender() {
    this.#le?.afterRender();
  }
  _emitPluginEvent(e2, t2) {
    this.#le?.emitPluginEvent(e2, t2);
  }
  _schedulerMergeConfig() {
    this.#V.parseLightDomColumns(this), this.#V.merge(), this.#De(), (function(e2, t2, o2) {
      const i2 = Ut, n2 = Gt, r2 = /* @__PURE__ */ new Map();
      function s2(e3, t3, o3, i3, n3 = false) {
        r2.has(e3) || r2.set(e3, { description: t3, importHint: o3, fields: [], isConfigProperty: n3 });
        const s3 = r2.get(e3);
        s3.fields.includes(i3) || s3.fields.push(i3);
      }
      for (const a2 of n2) {
        const o3 = e2[a2.property];
        if ((a2.isUsed ? a2.isUsed(o3) : void 0 !== o3) && !Xt(t2, a2.pluginName)) {
          const e3 = a2.description || `the "${a2.property}" ${a2.level} property`;
          s2(a2.pluginName, e3, jt(a2.pluginName), a2.property, true);
        }
      }
      const l2 = e2.columns;
      if (l2 && l2.length > 0) for (const a2 of l2) for (const e3 of i2) {
        const o3 = a2[e3.property];
        if ((e3.isUsed ? e3.isUsed(o3) : void 0 !== o3) && !Xt(t2, e3.pluginName)) {
          const t3 = a2.field || "<unknown>", o4 = e3.description || `the "${e3.property}" ${e3.level} property`;
          s2(e3.pluginName, o4, jt(e3.pluginName), t3);
        }
      }
      if (r2.size > 0) {
        const e3 = [];
        for (const [t3, { description: o3, importHint: i3, fields: n3, isConfigProperty: s3 }] of r2) if (s3) e3.push(`Config uses ${o3}, but the required plugin is not loaded.
  → Add the plugin to your gridConfig.plugins array:
    ${i3}
    plugins: [new ${Yt(t3)}Plugin(), ...]`);
        else {
          const r3 = n3.slice(0, 3).join(", ") + (n3.length > 3 ? `, ... (${n3.length} total)` : "");
          e3.push(`Column(s) [${r3}] use ${o3}, but the required plugin is not loaded.
  → Add the plugin to your gridConfig.plugins array:
    ${i3}
    plugins: [new ${Yt(t3)}Plugin(), ...]`);
        }
        T([...r2.values()].some((e4) => e4.isConfigProperty) ? "TBW002" : "TBW001", `Configuration error:

${e3.join("\n\n")}

This validation helps catch misconfigurations early. The properties listed above require their respective plugins to function.`, o2);
      }
    })(this.#m, this.#le?.getPlugins() ?? [], this.id), (function(e2, t2) {
      const o2 = [], i2 = [];
      for (const n2 of e2) {
        const e3 = n2.constructor.manifest;
        if (e3?.configRules) for (const t3 of e3.configRules) {
          const e4 = n2.config;
          if (t3.check(e4)) {
            const e5 = `[${Yt(n2.name)}Plugin] Configuration warning: ${t3.message}`;
            "error" === t3.severity ? o2.push(e5) : i2.push(e5);
          }
        }
      }
      if (i2.length > 0 && Se()) for (const n2 of i2) A("TBW004", n2, t2);
      o2.length > 0 && T("TBW003", `Configuration error:

${o2.join("\n\n")}`, t2);
    })(this.#le?.getPlugins() ?? [], this.id), (function(e2, t2) {
      if (!Se()) return;
      const o2 = new Set(e2.map((e3) => e3.name)), i2 = /* @__PURE__ */ new Set();
      for (const n2 of e2) {
        const e3 = n2.constructor.manifest;
        if (e3?.incompatibleWith) {
          for (const r2 of e3.incompatibleWith) if (o2.has(r2.name)) {
            const e4 = [n2.name, r2.name].sort().join("↔");
            if (i2.has(e4)) continue;
            i2.add(e4), A("TBW022", `${Yt(n2.name)}Plugin and ${Yt(r2.name)}Plugin are both loaded, but they are currently incompatible.

  → ${r2.reason}

  Consider removing one of these plugins to avoid unexpected behavior.`, t2);
          }
        }
      }
    })(this.#le?.getPlugins() ?? [], this.id), this.#ft(), this.#xe = [...this._columns];
  }
  _schedulerProcessColumns() {
    this.#ct();
  }
  _schedulerProcessRows() {
    this.#ht();
  }
  _schedulerRenderHeader() {
    be(this);
  }
  _schedulerUpdateTemplate() {
    I(this);
  }
  _schedulerAfterRender() {
    this.#le?.afterRender(), this._virtualization.enabled && this._virtualization.totalHeightEl && queueMicrotask(() => {
      if (!this._virtualization.totalHeightEl) return;
      const e2 = this.#de.calculateTotalSpacerHeight(this._rows.length);
      this._virtualization.totalHeightEl.style.height = `${e2}px`;
    });
    "fixed" !== this.#m.fitMode || this.__didInitialAutoSize || (this.__didInitialAutoSize = true, L(this)), this._restoreFocusAfterRender && (this._restoreFocusAfterRender = false, je(this)), this._virtualization.enabled && !this.#Ue && this.#et(), this.#Q && (this.#Q = false, requestAnimationFrame(() => {
      requestAnimationFrame(() => {
        this.#Ze();
      });
    })), this.#Ce && this.#Pe();
  }
  get _schedulerIsConnected() {
    return this.isConnected && this.#U;
  }
  get _hostElement() {
    return this;
  }
  get _renderRoot() {
    return this.#W;
  }
  _emit(e2, t2) {
    this.#tt(e2, t2);
  }
  get _accordionIcons() {
    return { expand: this.#m?.icons?.expand ?? n.expand, collapse: this.#m?.icons?.collapse ?? n.collapse };
  }
  get _shellState() {
    return this.#we;
  }
  _clearRowPool() {
    this._rowPool.length = 0, this._bodyEl && (this._bodyEl.innerHTML = ""), this.__rowRenderEpoch++;
  }
  _setup() {
    this.#je();
  }
  _applyAnimationConfig(e2) {
    this.#ut(e2);
  }
  #ft() {
    !(function(e2, t2, o2, i2) {
      if (!t2) return false;
      let n2 = false;
      const r2 = (function(e3, t3) {
        const o3 = e3?.gridAriaLabel;
        return o3 || (e3?.shell?.header?.title ?? t3?.lightDomTitle ?? void 0);
      })(o2, i2);
      r2 !== e2.ariaLabel && (e2.ariaLabel = r2, r2 ? t2.setAttribute("aria-label", r2) : t2.removeAttribute("aria-label"), n2 = true);
      const s2 = o2?.gridAriaDescribedBy;
      s2 !== e2.ariaDescribedBy && (e2.ariaDescribedBy = s2, s2 ? t2.setAttribute("aria-describedby", s2) : t2.removeAttribute("aria-describedby"), n2 = true);
    })(this.#gt, this.__rowsBodyEl, this.#m, this.#we);
  }
  #Pe() {
    const e2 = this.querySelector(".tbw-grid-root");
    var t2;
    e2 && (this.#Ce ? (this.#Re || (this.#Re = (function(e3) {
      const t3 = document.createElement("div");
      return t3.className = "tbw-loading-overlay", t3.setAttribute("role", "status"), t3.setAttribute("aria-live", "polite"), t3.appendChild(it("large", e3)), t3;
    })(this.#m?.loadingRenderer)), (function(e3, t3) {
      e3.appendChild(t3);
    })(e2, this.#Re)) : (t2 = this.#Re, t2?.remove()));
  }
  #He(e2, t2) {
    this.#lt();
    const o2 = this.#Se.get(e2);
    if (!o2) return;
    const i2 = this.findRenderedRowElement?.(o2.index);
    i2 && nt(i2, t2);
  }
  #Me(e2, t2, o2) {
    this.#lt();
    const i2 = this.#Se.get(e2);
    if (!i2) return;
    const n2 = this.findRenderedRowElement?.(i2.index);
    if (!n2) return;
    const r2 = this._visibleColumns.findIndex((e3) => e3.field === t2);
    if (r2 < 0) return;
    const s2 = n2.children[r2];
    s2 && (function(e3, t3) {
      t3 ? (e3.classList.add("tbw-cell-loading"), e3.setAttribute("aria-busy", "true")) : (e3.classList.remove("tbw-cell-loading"), e3.removeAttribute("aria-busy"));
    })(s2, o2);
  }
  #je() {
    if (this.isConnected && this._headerRowEl && this._bodyEl) {
      if (this.#V.parseLightDomColumns(this), this.#R) {
        const e2 = this.#R;
        this.#R = void 0, this.#V.merge();
        const t2 = this.#le?.getAll() ?? [];
        this.#V.applyState(e2, t2);
      }
      this._bodyEl && (this._bodyEl.style.display = "", this._bodyEl.style.gridTemplateColumns = ""), this.#Y.requestPhase(_e.FULL, "setup");
    }
  }
  #Qe(e2) {
    let t2 = 0, o2 = 0, i2 = 0, n2 = 0, r2 = 0;
    if (this.#Z) {
      const e3 = this._virtualization.container, s2 = this.#pe;
      t2 = s2?.scrollLeft ?? 0, o2 = e3?.scrollHeight ?? 0, i2 = s2?.scrollWidth ?? 0, n2 = e3?.clientHeight ?? 0, r2 = s2?.clientWidth ?? 0;
    }
    if (this.refreshVirtualWindow(false) && this.#le?.onScrollRender(), this._virtualization.variableHeights && (this.#J && clearTimeout(this.#J), this.#J = window.setTimeout(() => {
      this.#J = 0, this.#de.measureRenderedRowHeights(this._virtualization.start, this._virtualization.end);
    }, 100)), this.#Z) {
      const s2 = this.#se;
      s2.scrollTop = e2, s2.scrollLeft = t2, s2.scrollHeight = o2, s2.scrollWidth = i2, s2.clientHeight = n2, s2.clientWidth = r2, this.#le?.onScroll(s2);
    }
  }
  findHeaderRow() {
    return this.#W.querySelector(".header-row");
  }
  findRenderedRowElement(e2) {
    const t2 = this._virtualization, o2 = e2 - t2.start;
    return o2 >= 0 && o2 < this._rowPool.length && o2 < t2.end - t2.start ? this._rowPool[o2] : null;
  }
  _dispatchCellClick(e2, t2, o2, i2) {
    const n2 = this._rows[t2], r2 = this._visibleColumns[o2];
    if (!n2 || !r2) return false;
    const s2 = r2.field, l2 = n2[s2], a2 = new CustomEvent("cell-activate", { cancelable: true, bubbles: true, composed: true, detail: { rowIndex: t2, colIndex: o2, column: r2, field: s2, value: l2, row: n2, cellEl: i2, trigger: "pointer", originalEvent: e2 } });
    if (this.dispatchEvent(a2), a2.defaultPrevented) return true;
    const c2 = { row: n2, rowIndex: t2, colIndex: o2, column: r2, field: s2, value: l2, cellEl: i2, originalEvent: e2 }, d2 = this.#le?.onCellClick(c2) ?? false;
    return this.#tt("cell-click", c2), d2;
  }
  _dispatchRowClick(e2, t2, o2, i2) {
    if (!o2) return false;
    const n2 = { rowIndex: t2, row: o2, rowEl: i2, originalEvent: e2 }, r2 = this.#le?.onRowClick(n2) ?? false;
    return this.#tt("row-click", n2), r2;
  }
  _dispatchHeaderClick(e2, t2, o2) {
    if (!t2) return false;
    const i2 = { colIndex: this._columns.indexOf(t2), field: t2.field, column: t2, headerEl: o2, originalEvent: e2 };
    return this.#le?.onHeaderClick(i2) ?? false;
  }
  _dispatchKeyDown(e2) {
    return this.#le?.onKeyDown(e2) ?? false;
  }
  _getHorizontalScrollOffsets(e2, t2) {
    return this.#le?.getHorizontalScrollOffsets(e2, t2) ?? { left: 0, right: 0 };
  }
  query(e2, t2) {
    return this.#le?.queryPlugins({ type: e2, context: t2 }) ?? [];
  }
  _dispatchCellMouseDown(e2) {
    return this.#le?.onCellMouseDown(e2) ?? false;
  }
  _dispatchCellMouseMove(e2) {
    this.#le?.onCellMouseMove(e2);
  }
  _dispatchCellMouseUp(e2) {
    this.#le?.onCellMouseUp(e2);
  }
  _afterCellRender(e2) {
    this.#le?.afterCellRender(e2);
  }
  _hasAfterCellRenderHook() {
    return this.#le?.hasAfterCellRenderHook() ?? false;
  }
  _afterRowRender(e2) {
    this.#le?.afterRowRender(e2);
  }
  _hasAfterRowRenderHook() {
    return this.#le?.hasAfterRowRenderHook() ?? false;
  }
  async ready() {
    return this.#r;
  }
  async forceLayout() {
    return 0 === this.#Y.pendingPhase && this.#Y.requestPhase(_e.FULL, "forceLayout"), this.#Y.whenReady();
  }
  async getConfig() {
    return Object.freeze({ ...this.#m || {} });
  }
  getRowId(e2) {
    return ut(e2, this.id, this.#m.getRowId);
  }
  getRow(e2) {
    return this.#ue.getRow(e2);
  }
  _getRowEntry(e2) {
    return this.#lt(), this.#Se.get(e2);
  }
  updateRow(e2, t2, o2 = "api") {
    this.#ue.updateRow(e2, t2, o2);
  }
  updateRows(e2, t2 = "api") {
    this.#ue.updateRows(e2, t2);
  }
  animateRow(e2, t2) {
    return dt(this, e2, t2);
  }
  animateRows(e2, t2) {
    return (function(e3, t3, o2) {
      return Promise.all(t3.map((t4) => dt(e3, t4, o2))).then((e4) => e4.filter(Boolean).length);
    })(this, e2, t2);
  }
  animateRowById(e2, t2) {
    return (function(e3, t3, o2) {
      const i2 = e3._rows ?? [], n2 = e3.getRowId;
      if (!n2) return Promise.resolve(false);
      const r2 = i2.findIndex((e4) => {
        if (null == e4) return false;
        try {
          return n2(e4) === t3;
        } catch {
          return false;
        }
      });
      return r2 < 0 ? Promise.resolve(false) : dt(e3, r2, o2);
    })(this, e2, t2);
  }
  async insertRow(e2, t2, o2 = true) {
    return this.#ue.insertRow(e2, t2, o2);
  }
  async removeRow(e2, t2 = true) {
    return this.#ue.removeRow(e2, t2);
  }
  async applyTransaction(e2, t2 = true) {
    return this.#ue.applyTransaction(e2, t2);
  }
  applyTransactionAsync(e2) {
    return this.#ue.applyTransactionAsync(e2);
  }
  focusCell(e2, t2) {
    this.#he.focusCell(e2, t2);
  }
  get focusedCell() {
    return this.#he.focusedCell;
  }
  scrollToRow(e2, t2) {
    this.#he.scrollToRow(e2, t2);
  }
  scrollToRowById(e2, t2) {
    this.#he.scrollToRowById(e2, t2);
  }
  setColumnVisible(e2, t2) {
    const o2 = this.#V.setColumnVisible(e2, t2);
    return o2 && this.requestStateChange(), o2;
  }
  toggleColumnVisibility(e2) {
    const t2 = this.#V.toggleColumnVisibility(e2);
    return t2 && this.requestStateChange(), t2;
  }
  isColumnVisible(e2) {
    return this.#V.isColumnVisible(e2);
  }
  showAllColumns() {
    this.#V.showAllColumns(), this.requestStateChange();
  }
  getAllColumns() {
    return this.#V.getAllColumns();
  }
  setColumnOrder(e2) {
    this.#V.setColumnOrder(e2), this.requestStateChange();
  }
  getColumnOrder() {
    return this.#V.getColumnOrder();
  }
  getColumnState() {
    const e2 = this.#le?.getAll() ?? [];
    return this.#V.collectState(e2);
  }
  set columnState(e2) {
    e2 && (this.#R = e2, this.#V.initialColumnState = e2, this.#F && this.#pt(e2));
  }
  get columnState() {
    return this.getColumnState();
  }
  #pt(e2) {
    const t2 = this._columns, o2 = this._sortState, i2 = this.#le?.getAll() ?? [];
    if (this.#V.applyState(e2, i2), this.#wt(t2, o2)) return this.#Te = void 0, I(this), void this.#Y.requestPhase(_e.STYLE, "column-width");
    this.#je();
  }
  #wt(e2, t2) {
    const o2 = this._columns, i2 = this._sortState;
    if (t2?.field !== i2?.field || t2?.direction !== i2?.direction) return false;
    if (e2.length !== o2.length) return false;
    for (let n2 = 0; n2 < e2.length; n2++) {
      const t3 = e2[n2], i3 = o2[n2];
      if (t3.field !== i3.field) return false;
      if (!!t3.hidden != !!i3.hidden) return false;
    }
    return true;
  }
  get sortModel() {
    return this._sortState ? { field: this._sortState.field, direction: 1 === this._sortState.direction ? "asc" : "desc" } : null;
  }
  sort(e2, t2) {
    if (null === e2) {
      if (!this._sortState) return;
      const e3 = this._sortState.field;
      this._sortState = null, this.__rowRenderEpoch++;
      for (let t3 = 0; t3 < this._rowPool.length; t3++) this._rowPool[t3].__epoch = -1;
      return this._rows = this.__originalOrder.slice(), this.__originalOrder = [], be(this), this.refreshVirtualWindow(true), this.dispatchEvent(new CustomEvent("sort-change", { detail: { field: e3, direction: 0 } })), void this.requestStateChange?.();
    }
    const o2 = this._columns.find((t3) => t3.field === e2);
    o2 && (t2 ? (this._sortState || (this.__originalOrder = this._rows.slice()), ue(this, o2, "desc" === t2 ? -1 : 1)) : he(this, o2));
  }
  requestStateChange() {
    const e2 = this.#le?.getAll() ?? [];
    this.#V.requestStateChange(e2);
  }
  resetColumnState() {
    this.#R = void 0, this.__originalOrder = [];
    const e2 = this.#le?.getAll() ?? [];
    this.#V.resetState(e2), this.#V.merge(), this.#je();
  }
  get isToolPanelOpen() {
    return this.#me.isPanelOpen;
  }
  get defaultRowHeight() {
    return this._virtualization.rowHeight;
  }
  get expandedToolPanelSections() {
    return this.#me.expandedSections;
  }
  openToolPanel() {
    this.#me.openToolPanel();
  }
  closeToolPanel() {
    this.#me.closeToolPanel();
  }
  toggleToolPanel() {
    this.#me.toggleToolPanel();
  }
  toggleToolPanelSection(e2) {
    this.#me.toggleToolPanelSection(e2);
  }
  getToolPanels() {
    return this.#me.getToolPanels();
  }
  registerToolPanel(e2) {
    this.#we.apiToolPanelIds.add(e2.id), this.#me.registerToolPanel(e2);
  }
  unregisterToolPanel(e2) {
    this.#we.apiToolPanelIds.delete(e2), this.#me.unregisterToolPanel(e2);
  }
  getHeaderContents() {
    return this.#me.getHeaderContents();
  }
  registerHeaderContent(e2) {
    this.#we.apiHeaderContentIds.add(e2.id), this.#me.registerHeaderContent(e2);
  }
  unregisterHeaderContent(e2) {
    this.#we.apiHeaderContentIds.delete(e2), this.#me.unregisterHeaderContent(e2);
  }
  getToolbarContents() {
    return this.#me.getToolbarContents();
  }
  registerToolbarContent(e2) {
    this.#me.registerToolbarContent(e2);
  }
  unregisterToolbarContent(e2) {
    this.#me.unregisterToolbarContent(e2);
  }
  #mt = false;
  refreshShellHeader() {
    this.#mt || (this.#mt = true, queueMicrotask(() => {
      if (this.#mt = false, !this.isConnected) return;
      this.#We(), this.#V.markSourcesChanged(), this.#V.merge(), kt(this.#we);
      (function(e2, t2, o2, i2) {
        const n2 = e2.querySelector(`.${z.ROOT}`), r2 = n2?.querySelector(".tbw-grid-content");
        if (!n2 || !r2) return Ot(e2, t2, o2, i2);
        r2.remove();
        const s2 = Rt(t2);
        if (n2.replaceChildren(), s2) {
          n2.className = `${z.ROOT} has-shell`;
          const { headerOptions: e3, bodyOptions: s3 } = Lt(t2, o2, i2), l2 = Ct(e3), a2 = _t(s3), c2 = a2.querySelector(".tbw-grid-content");
          c2 && c2.replaceWith(r2), n2.appendChild(l2), n2.appendChild(a2);
        } else n2.className = z.ROOT, n2.appendChild(r2);
        return s2;
      })(this.#W, this.#m?.shell, { isPanelOpen: this.#we.isPanelOpen, expandedSections: this.#we.expandedSections }, this.#m?.icons) && (this.#bt(), this.#me.setInitialized(true)), this.#vt();
    }));
  }
  #vt() {
    const e2 = this.#W.querySelector(".tbw-grid-content") ?? this.#W.querySelector(".tbw-grid-root");
    if (this._headerRowEl = e2?.querySelector(".header-row"), this._virtualization.totalHeightEl = e2?.querySelector(".faux-vscroll-spacer"), this._virtualization.viewportEl = e2?.querySelector(".rows-viewport"), this._bodyEl = e2?.querySelector(".rows"), this.__rowsBodyEl = e2?.querySelector(".rows-body"), this.#me.isInitialized) {
      At(this.#W, this.#we), Tt(this.#W, this.#m?.shell, this.#we);
      const e3 = this.#m?.shell?.toolPanel?.defaultOpen;
      e3 && this.#we.toolPanels.has(e3) && (this.openToolPanel(), this.#we.expandedSections.add(e3)), this.#we.isPanelOpen && (Mt(this.#W, this.#we), Pt(this.#W, this.#we, (this.#m, this.#m)), Ht(this.#W, this.#we));
    }
    this._resizeController = rt(this), this.#Ne(e2), this.#Y.requestPhase(_e.COLUMNS, "shellRefresh");
  }
  #Ge = /* @__PURE__ */ new Map();
  registerStyles(e2, t2) {
    let o2 = this.#Ge.get(e2);
    o2 || (o2 = new CSSStyleSheet(), this.#Ge.set(e2, o2)), o2.replaceSync(t2), this.#Ct();
  }
  unregisterStyles(e2) {
    this.#Ge.delete(e2) && this.#Ct();
  }
  getRegisteredStyles() {
    return Array.from(this.#Ge.keys());
  }
  #Ct() {
    const e2 = Array.from(this.#Ge.values()), t2 = document.adoptedStyleSheets.filter((e3) => !Array.from(this.#Ge.values()).includes(e3));
    document.adoptedStyleSheets = [...t2, ...e2];
  }
  registerExternalFocusContainer(e2) {
    this.#he.registerExternalFocusContainer(e2);
  }
  unregisterExternalFocusContainer(e2) {
    this.#he.unregisterExternalFocusContainer(e2);
  }
  containsFocus(e2) {
    return this.#he.containsFocus(e2);
  }
  #We() {
    St(this, this.#we), Et(this, this.#we), xt(this, this.#we, this.#$e());
  }
  #_t() {
    const e2 = this.#W.querySelector(".tbw-shell-header");
    if (!e2) return;
    kt(this.#we);
    const t2 = (function(e3, t3, o3) {
      const i3 = e3?.header?.title ?? t3.lightDomTitle ?? "", n2 = !!i3;
      let r2 = "";
      void 0 !== o3 && (r2 = "string" == typeof o3 ? K(o3) : o3.outerHTML);
      const s2 = e3?.header?.toolbarContents ?? [], l2 = [...t3.toolbarContents.values()], a2 = new Set(s2.map((e4) => e4.id)), c2 = [...s2];
      for (const w2 of l2) a2.has(w2.id) || c2.push(w2);
      const d2 = c2.length > 0, h2 = t3.toolPanels.size > 0, u2 = d2 && h2, g2 = [...c2].sort((e4, t4) => (e4.order ?? 0) - (t4.order ?? 0));
      let f2 = "";
      for (const w2 of g2) f2 += `<div class="tbw-toolbar-content-slot" data-toolbar-content="${w2.id}"></div>`;
      if (u2 && (f2 += '<div class="tbw-toolbar-separator"></div>'), h2) {
        const e4 = t3.isPanelOpen;
        f2 += `<button class="${e4 ? "tbw-toolbar-btn active" : "tbw-toolbar-btn"}" data-panel-toggle data-icon="tool-panel" title="Settings" aria-label="Toggle settings panel" aria-pressed="${e4}" aria-controls="tbw-tool-panel">${r2}</button>`;
      }
      return `
    <div class="tbw-shell-header" part="shell-header" role="presentation">
      ${n2 ? `<div class="tbw-shell-title">${p2 = i3, p2 && "string" == typeof p2 ? p2.replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;").replace(/"/g, "&quot;").replace(/'/g, "&#39;") : ""}</div>` : ""}
      <div class="tbw-shell-content" part="shell-content" role="presentation" data-light-dom-header-content></div>
      <div class="tbw-shell-toolbar" part="shell-toolbar" role="presentation">
        ${f2}
      </div>
    </div>
  `;
      var p2;
    })(this.#m.shell, this.#we, this.#m.icons?.toolPanel), o2 = document.createElement("div");
    o2.innerHTML = t2;
    const i2 = o2.firstElementChild;
    i2 && (e2.replaceWith(i2), this.#bt(), Tt(this.#W, this.#m?.shell, this.#we));
  }
  #Ve() {
    const e2 = () => {
      const e3 = this.#we.lightDomTitle, t3 = this.#we.hasToolButtonsContainer;
      this.#We();
      const o2 = this.#we.lightDomTitle, i2 = this.#we.hasToolButtonsContainer;
      (o2 && !e3 || i2 && !t3) && (this.#V.markSourcesChanged(), this.#V.merge(), this.#_t());
    }, t2 = () => {
      this.__lightDomColumnsCache = void 0, this.#je();
    };
    this.#V.registerLightDomHandler("tbw-grid-header", e2), this.#V.registerLightDomHandler("tbw-grid-tool-buttons", e2), this.#V.registerLightDomHandler("tbw-grid-tool-panel", e2), this.#V.registerLightDomHandler("tbw-grid-column", t2), this.#V.registerLightDomHandler("tbw-grid-detail", t2), this.#V.observeLightDOM(this);
  }
  refreshColumns() {
    this.__lightDomColumnsCache = void 0, We(this), this.#V.parseLightDomColumns(this);
    const e2 = this.#we.lightDomTitle, t2 = this.#we.hasToolButtonsContainer;
    this.#We();
    const o2 = this.#we.lightDomTitle, i2 = this.#we.hasToolButtonsContainer;
    (o2 && !e2 || i2 && !t2) && (this.#V.markSourcesChanged(), this.#V.merge(), this.#_t()), this.#Y.requestPhase(_e.COLUMNS, "refreshColumns");
  }
  #Je() {
    this.#de.updateCachedGeometry();
  }
  refreshVirtualWindow(e2 = false, t2 = false) {
    return this.#de.refreshVirtualWindow(e2, t2);
  }
  invalidateRowHeight(e2, t2) {
    this.#de.invalidateRowHeight(e2, t2);
  }
  #Fe() {
    this.#We(), this.#V.markSourcesChanged(), this.#V.merge();
    const e2 = this.#m?.shell;
    Ot(this.#W, e2, { isPanelOpen: this.#we.isPanelOpen, expandedSections: this.#we.expandedSections }, this.#m?.icons) && (this.#bt(), this.#me.setInitialized(true));
  }
  #bt() {
    !(function(e2, t2, o2, i2) {
      const n2 = e2.querySelector(".tbw-shell-toolbar");
      n2 && n2.addEventListener("click", (e3) => {
        e3.target.closest("[data-panel-toggle]") && i2.onPanelToggle();
      });
      const r2 = e2.querySelector(".tbw-accordion");
      r2 && r2.addEventListener("click", (e3) => {
        const t3 = e3.target.closest(".tbw-accordion-header");
        if (t3) {
          const e4 = t3.closest("[data-section]"), o3 = e4?.getAttribute("data-section");
          o3 && i2.onSectionToggle(o3);
        }
      });
    })(this.#W, this.#m, this.#we, { onPanelToggle: () => this.toggleToolPanel(), onSectionToggle: (e2) => this.toggleToolPanelSection(e2) }), this.#be?.(), this.#be = (function(e2, t2, o2) {
      const i2 = e2.querySelector(".tbw-tool-panel"), n2 = e2.querySelector("[data-resize-handle]"), r2 = e2.querySelector(".tbw-shell-body");
      if (!i2 || !n2 || !r2) return () => {
      };
      const s2 = t2?.toolPanel?.position ?? "right";
      let l2 = 0, a2 = 0, c2 = 0, d2 = false;
      const h2 = (e3) => {
        if (!d2) return;
        e3.preventDefault();
        const t3 = "left" === s2 ? e3.clientX - l2 : l2 - e3.clientX, o3 = Math.min(c2, Math.max(200, a2 + t3));
        i2.style.width = `${o3}px`;
      }, u2 = () => {
        if (!d2) return;
        d2 = false, n2.classList.remove("resizing"), i2.style.transition = "", document.body.style.cursor = "", document.body.style.userSelect = "";
        const e3 = i2.getBoundingClientRect().width;
        o2(e3), document.removeEventListener("mousemove", h2), document.removeEventListener("mouseup", u2);
      }, g2 = (e3) => {
        e3.preventDefault(), d2 = true, l2 = e3.clientX, a2 = i2.getBoundingClientRect().width, c2 = r2.getBoundingClientRect().width - 20, n2.classList.add("resizing"), i2.style.transition = "none", document.body.style.cursor = "col-resize", document.body.style.userSelect = "none", document.addEventListener("mousemove", h2), document.addEventListener("mouseup", u2);
      };
      return n2.addEventListener("mousedown", g2), () => {
        n2.removeEventListener("mousedown", g2), document.removeEventListener("mousemove", h2), document.removeEventListener("mouseup", u2);
      };
    })(this.#W, this.#m?.shell, (e2) => {
      this.style.setProperty("--tbw-tool-panel-width", `${e2}px`);
    }), this.#ve?.(), this.#ve = (function(e2, t2, o2, i2) {
      if (!t2?.toolPanel?.closeOnClickOutside) return () => {
      };
      const n2 = (e3) => {
        if (!o2.isPanelOpen) return;
        const t3 = e3.target;
        t3 && (t3.closest(".tbw-tool-panel") || t3.closest("[data-panel-toggle]") || i2());
      };
      return e2.addEventListener("mousedown", n2), () => e2.removeEventListener("mousedown", n2);
    })(this, this.#m?.shell, this.#we, () => this.closeToolPanel());
  }
};
function so(e2) {
  const t2 = document.createElement("tbw-grid");
  return e2 && (t2.gridConfig = e2), t2;
}
function lo(e2, t2, o2) {
  let i2 = document, n2 = false;
  return "boolean" == typeof t2 ? n2 = t2 : t2 && (i2 = t2, n2 = !!o2), n2 ? customElements.whenDefined(ro.tagName).then(() => i2.querySelector(e2)) : i2.querySelector(e2);
}
customElements.get(ro.tagName) || customElements.define(ro.tagName, ro), globalThis.DataGridElement = ro;
var ao = { CELL_CHANGE: "cell-change", CELL_COMMIT: "cell-commit", ROW_COMMIT: "row-commit", EDIT_OPEN: "edit-open", EDIT_CLOSE: "edit-close", CHANGED_ROWS_RESET: "changed-rows-reset", MOUNT_EXTERNAL_VIEW: "mount-external-view", MOUNT_EXTERNAL_EDITOR: "mount-external-editor", SORT_CHANGE: "sort-change", COLUMN_RESIZE: "column-resize", CELL_ACTIVATE: "cell-activate", GROUP_TOGGLE: "group-toggle", COLUMN_STATE_CHANGE: "column-state-change", DATA_CHANGE: "data-change" };
var co = { SELECTION_CHANGE: "selection-change", TREE_EXPAND: "tree-expand", TREE_LOAD_START: "tree-load-start", TREE_LOAD_END: "tree-load-end", TREE_LOAD_ERROR: "tree-load-error", FILTER_CHANGE: "filter-change", SORT_MODEL_CHANGE: "sort-model-change", EXPORT_START: "export-start", EXPORT_COMPLETE: "export-complete", CLIPBOARD_COPY: "clipboard-copy", CLIPBOARD_PASTE: "clipboard-paste", CONTEXT_MENU_OPEN: "context-menu-open", CONTEXT_MENU_CLOSE: "context-menu-close", HISTORY_CHANGE: "history-change", SERVER_LOADING: "server-loading", SERVER_ERROR: "server-error", COLUMN_VISIBILITY_CHANGE: "column-visibility-change", COLUMN_REORDER: "column-reorder", DETAIL_EXPAND: "detail-expand", GROUP_EXPAND: "group-expand" };
function ho(e2) {
  return null == e2 || "" === e2 || "number" == typeof e2 && isNaN(e2);
}
var uo = { sum: (e2, t2) => {
  let o2 = 0;
  for (let i2 = 0; i2 < e2.length; i2++) {
    const n2 = e2[i2][t2];
    if (ho(n2)) continue;
    const r2 = Number(n2);
    isNaN(r2) || (o2 += r2);
  }
  return o2;
}, avg: (e2, t2) => {
  if (!e2.length) return 0;
  let o2 = 0, i2 = 0;
  for (let n2 = 0; n2 < e2.length; n2++) {
    const r2 = e2[n2][t2];
    if (ho(r2)) continue;
    const s2 = Number(r2);
    isNaN(s2) || (o2 += s2, i2++);
  }
  return i2 > 0 ? o2 / i2 : 0;
}, count: (e2) => e2.length, min: (e2, t2) => {
  if (!e2.length) return 0;
  let o2 = 1 / 0;
  for (let i2 = 0; i2 < e2.length; i2++) {
    const n2 = e2[i2][t2];
    if (ho(n2)) continue;
    const r2 = Number(n2);
    isNaN(r2) || r2 < o2 && (o2 = r2);
  }
  return o2 === 1 / 0 ? 0 : o2;
}, max: (e2, t2) => {
  if (!e2.length) return 0;
  let o2 = -1 / 0;
  for (let i2 = 0; i2 < e2.length; i2++) {
    const n2 = e2[i2][t2];
    if (ho(n2)) continue;
    const r2 = Number(n2);
    isNaN(r2) || r2 > o2 && (o2 = r2);
  }
  return o2 === -1 / 0 ? 0 : o2;
}, first: (e2, t2) => e2[0]?.[t2], last: (e2, t2) => e2[e2.length - 1]?.[t2] };
var go = /* @__PURE__ */ new Map();
var fo = { register(e2, t2) {
  go.set(e2, t2);
}, unregister(e2) {
  go.delete(e2);
}, get(e2) {
  if (void 0 !== e2) return "function" == typeof e2 ? e2 : go.get(e2) ?? uo[e2];
}, run(e2, t2, o2, i2) {
  const n2 = this.get(e2);
  return n2 ? n2(t2, o2, i2) : void 0;
}, has: (e2) => go.has(e2) || e2 in uo, list: () => [...Object.keys(uo), ...go.keys()] };
var po = { sum: (e2) => {
  let t2 = 0;
  for (let o2 = 0; o2 < e2.length; o2++) t2 += e2[o2];
  return t2;
}, avg: (e2) => {
  if (!e2.length) return 0;
  let t2 = 0;
  for (let o2 = 0; o2 < e2.length; o2++) t2 += e2[o2];
  return t2 / e2.length;
}, count: (e2) => e2.length, min: (e2) => {
  if (!e2.length) return 0;
  let t2 = e2[0];
  for (let o2 = 1; o2 < e2.length; o2++) e2[o2] < t2 && (t2 = e2[o2]);
  return t2;
}, max: (e2) => {
  if (!e2.length) return 0;
  let t2 = e2[0];
  for (let o2 = 1; o2 < e2.length; o2++) e2[o2] > t2 && (t2 = e2[o2]);
  return t2;
}, first: (e2) => e2[0] ?? 0, last: (e2) => e2[e2.length - 1] ?? 0 };
function wo(e2) {
  return po[e2] ?? po.sum;
}
function mo(e2, t2) {
  return wo(e2)(t2);
}
export {
  ie as BaseGridPlugin,
  w as C,
  y as D,
  t as DEFAULT_A11Y_MESSAGES,
  o as DEFAULT_ANIMATION_CONFIG,
  n as DEFAULT_GRID_ICONS,
  ao as DGEvents,
  ro as DataGridElement,
  h as E,
  Oe as F,
  e as FitModeEnum,
  $ as GridCSSVars,
  z as GridClasses,
  N as GridDataAttrs,
  ro as GridElement,
  q as GridSelectors,
  m as M,
  v as N,
  p as P,
  co as PluginEvents,
  no as PluginManager,
  _e as RenderPhase,
  b as T,
  r as a,
  fo as aggregatorRegistry,
  oo as b,
  re as builtInSort,
  to as c,
  so as createGrid,
  T as d,
  ne as defaultComparator,
  je as e,
  C as f,
  s as g,
  wo as getValueAggregator,
  _ as h,
  nt as i,
  Q as j,
  Pe as k,
  u as l,
  g as m,
  H as n,
  f as o,
  Te as p,
  Ae as q,
  lo as queryGrid,
  Me as r,
  mo as runValueAggregator,
  K as s,
  Je as setFeatureResolver,
  oe as t,
  P as u,
  S as v,
  A as w,
  R as x,
  E as y
};
//# sourceMappingURL=@toolbox-web_grid.js.map
