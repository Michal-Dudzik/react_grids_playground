import { ColumnConfig } from '../../core/types';
import { ExportParams } from './types';
/** CSV export options */
export interface CsvOptions {
    /** Field delimiter (default: ',') */
    delimiter?: string;
    /** Line separator (default: '\n') */
    newline?: string;
    /** Whether to quote strings containing special characters (default: true) */
    quoteStrings?: boolean;
    /** Add UTF-8 BOM for Excel compatibility (default: false) */
    bom?: boolean;
}
/**
 * Format a value for CSV output.
 * Handles null, Date, objects, and strings with special characters.
 */
export declare function formatCsvValue(value: any, quote?: boolean): string;
/**
 * Build CSV content from rows and columns.
 */
export declare function buildCsv(rows: any[], columns: ColumnConfig[], params: ExportParams, options?: CsvOptions): string;
/**
 * Download a Blob as a file.
 */
export declare function downloadBlob(blob: Blob, fileName: string): void;
/**
 * Download CSV content as a file.
 */
export declare function downloadCsv(content: string, fileName: string): void;
