import { ColumnConfig } from '../../core/types';
import { ExportParams } from './types';
/**
 * Build Excel XML content from rows and columns.
 * Uses XML Spreadsheet 2003 format for broad compatibility.
 */
export declare function buildExcelXml(rows: any[], columns: ColumnConfig[], params: ExportParams): string;
/**
 * Download Excel XML content as a file.
 */
export declare function downloadExcel(content: string, fileName: string): void;
