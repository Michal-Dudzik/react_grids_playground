import { TreeRow } from './types';
/**
 * Detects if the data has a tree structure by checking for children arrays.
 */
export declare function detectTreeStructure(rows: readonly TreeRow[], childrenField?: string): boolean;
/**
 * Attempts to infer the children field name from common patterns.
 * Returns the first field that contains an array with items.
 */
export declare function inferChildrenField(rows: readonly TreeRow[]): string | null;
/**
 * Calculates the maximum depth of the tree.
 * Useful for layout calculations and virtualization.
 */
export declare function getMaxDepth(rows: readonly TreeRow[], childrenField?: string, currentDepth?: number): number;
/**
 * Counts total nodes in the tree (including all descendants).
 */
export declare function countNodes(rows: readonly TreeRow[], childrenField?: string): number;
